# SIGNula — Autopilot Run (`autopilot/2026-06-30`)

> Machine state lives in `.dev-team/autopilot.json` (authoritative for the loop). This file is the human-readable narrative + trajectory.
> Mission: **production-ready + feature-complete (in scope)**. Gate policy: **hybrid**. Convergence cap: **60 cycles**.

## Goal
Drive SIGNula — a "universal" all-in-one CIAM / single-sign-on identity platform (PHP 8.4, MySQLi + prepared statements, MariaDB; Dreamhost shared hosting, no Composer in prod) — to production-ready and as feature-complete as its scope allows. Reconcile the codebase against the project brief (`.claude/CLAUDE.md`) and the GitHub issue tracker (40 open / 41 closed), close in-scope feature gaps, harden security, and fix correctness/syntax/lint defects. Auto-commit per cycle on the run branch; **never push** (user pushes manually).

## Definition of done
- [ ] **Builds & runs from a clean checkout** (install wizard completes) — *VERIFY-established*
- [ ] **Core auth flow works end-to-end** (register → login → MFA → session → logout) — *VERIFY-established*
- [ ] **Tests cover the core flows and pass** (current: 342 unit green; add coverage for Auth/Database/EmailService/payment providers/WebAuthn — B-020)
- [ ] **No unresolved Critical/High security finding** (SECURE reached dry; the 11 deferred issues #22–#34/#73 + FG-013 WebAuthn signature verification resolved)
- [ ] **No High-impact correctness/UX backlog item remains** (11 High B-items resolved or explicitly gated)
- [ ] **Docs enough to run it** (README/install present; PROJECT_PROGRESS/CHANGELOG/SECURITY_SETUP kept current)
- [ ] **Feature-complete in scope** — every High in-scope feature gap built or queued (FG-002a + FG-009 built; FG-001/FG-003/FG-004 queued for approval; FG-013 → SECURE)

**Out of code-scope (surfaced to user, NOT counted against done):** operational deployment needing the user's prod environment/secrets — #83 prod DB creds, #84 email-provider creds, #85 cron, #70 live-payment creds, #71 staging deploy, #79 HTTPS/.htaccess finalize, #77 monitoring, #78 backups; plus manual QA (#72 E2E, #75 cross-device, #74 load) and legal review (#81).

## Constraints
PHP 8.3+ (8.4 target); MySQLi + prepared statements only; all config in `tblSettings` (encrypted when `isSensitive`); AES-256 + Argon2id; no Composer in prod (self-host libs); `DIRECTORY_SEPARATOR`/predefined constants; `SIGNULA_INIT` guard on all source; clean URLs (no visible `.php`); private dirs `_`-prefixed outside web root.
**Hands-off (confirmed at bootstrap):** DB connection credentials & secrets; git push / remote / `.git/config`. **Gate-protected by default (Bucket-3, never autonomous):** destructive/data-dropping migrations; live-payment credential wiring. `.claude/` tooling files left untouched.

## Skills in play
dev-team-autopilot (conductor) · iterate / security / featurefind / review (modes) · dev-team-stripe (reserved for the gated billing epic FG-003/G-002 once approved).

## House style
Emoji-annotated docblocks; prepared statements via the `Database` wrapper; modular `require_once` with guard checks; `DIRECTORY_SEPARATOR` everywhere (0 literal-`/` requires); PSR-12 + project `phpcs.xml`; PHPUnit 10 tests in `_tests/`. Adopt-later: `declare(strict_types=1)` (0/271 today — POLISH).

## Stage plan
1. **DISCOVER** ✅ (bootstrap) — Codebase Map + scored backlog (23 B-) + feature-gap ledger (17 FG-) + issue reconciliation.
2. **STABILIZE** ⏳ — fix High correctness/robustness/quality B-items + establish safety-net tests. *Exit: no High correctness/test gap.*
3. **SECURE** — Phase-0 (threat model/scanners/fixtures), then one purple-team cycle per iteration over the 11 deferred issues + FG-013 + new findings. *Exit: no unresolved Critical/High; rounds dry.*
4. **COMPLETE** — build autonomy-eligible gaps (FG-009, FG-002a, FG-011, FG-005, FG-008, + B-019, B-022); queue FG-001/FG-003/FG-004 etc. *Exit: every High in-scope gap built-or-queued.*
5. **POLISH** — phpcbf whitespace, strict comparisons, strict_types, complexity, a11y (#76). *Exit: no High UX/perf item.*
6. **VERIFY** — independent review (report-only) at the shipped commit. *Exit: complete pass overall PASS.*

## Current status
Bootstrap + spec-authoring done. **G-001..G-004 user-approved** (specs in `.dev-team/specs/`). Phase: **STABILIZE** (safety floor leads before the security-critical feature builds). Build sequence:
1. ✅ **STABILIZE-1 DONE** (cycle 3): phpstan+phpcs now run; unknown-class FPs 647→74; baseline ≈3632 real phpstan errors; 5 SIGNULA_INIT guards; 342 tests green. **NEXT → STABILIZE-2.**
2. ✅ **STABILIZE-2 DONE** (cycle 4): +48 safety-net tests (342→390 green). Surfaced 2 HIGH — **B-027** (`Database::insert()` missing → WebAuthn registration broken) + **FG-013** (WebAuthn accepts unsigned assertions — auth-bypass, PoC-confirmed). **SAFETY FLOOR → next cycles (5–6) fix WebAuthn BEFORE the feature campaign:** ✅ **cycle 5 DONE**: B-027 (added `Database::insert()`/`insertId()` — unbroke 7 callers incl. PasswordlessLogin + 4 email + AccountManager) + B-028 (fixtures→canonical lowercase `active`; BaseController already correct). ✅ **cycle 6 DONE**: FG-013 closed (real WebAuthn verification — CBOR/COSE→PEM, openssl_verify, sign-count clone-detect; **red-team HOLDS** across 7 vectors; +2 pre-existing bug fixes). ✅ **cycle 7 DONE**: Integration suite runnable + green-on-rerun; fixed 3 broken core flows (error-logging, email-queue, backup-MFA) + user registration. **The "100% complete beta" had significant hidden breakage — fixing the foundation before features is correct.** ✅ **cycle 8 DONE**: security cleanup — B-024 CORS, #29 X-Frame, B-030/B-031 WebAuthn, 13 deferred MEDIUMs #22–#34, mig 027; + fixed the `session_start` flake (suite now 407 / **0 warnings**, 30+ runs). ✅ **cycle 9 DONE**: B-033 fresh install 74→0 errors + installer un-ignored. ✅ **cycle 10 DONE**: B-040 fixed the systemic `Database::getAffectedRows()` -1 bug (repaired WebAuthn + 12 callers); Integration now 71/0 green. ✅ **cycles 3-13 DONE — FOUNDATION PHASE COMPLETE.** Repaired: tooling/static-analysis, +48 safety-net tests + reliable integration gate, WebAuthn (registration + auth-bypass, red-teamed), silently-broken core flows (registration/email/MFA/error-log/getAffectedRows/activity-logging), fresh install 74→0 + wizard, 13 security MEDIUMs (#22-#34) + CORS/CSRF/headers, multi-org tables. Unit 407 + integration 5/5 green. Docs synced (cycle 13). **FEATURE CAMPAIGN IN PROGRESS (phase COMPLETE): G-003 JWT** — ✅ S1 crypto · ✅ S2 TokenService · ✅ S3 endpoints + Bearer wiring · ✅ S4 red-team (cycle 17: 12 vectors held; 2 bypasses found+fixed+re-attacked — kid-traversal-forgery HIGH + B-049 spray MED; unit 444, integration 90/90; auth entrypoint hardened). ✅ **S5 docs done (cycle 18) — G-003 JWT API auth FULLY BUILT.** **NEXT: G-001 IdP Phase A** (OAuth2/OIDC provider, reuses G-003 signing). THEN **G-001 IdP Phase A** (reuses this signing) → **G-002 billing TEST-MODE** → **G-004 compliance L1** → **CAP-API (#86)**. THEN **G-001 IdP Phase A** (reuses this signing) → **G-002 billing TEST-MODE** → **G-004 compliance L1** → **CAP-API (#86)**. Deferred to POLISH: B-044 (wizard DELIMITER — CLI-load works), B-045, B-035/B-036/B-038/B-042 (migration hygiene), #76 a11y; + a full SECURE Phase-0 purple-team sweep. broken-installer + B-038/B-034 (clean-install / migration reconciliation — HIGH, blocks clean-checkout done criterion); B-037 flake hardening. THEN feature campaign G-003 → G-001 → G-002 → G-004. THEN CAP-API capstone (#86).
**CAPSTONE (user directive 2026-06-30):** once all queued cycles + the feature campaign are complete, run a thorough **API completeness + security pass** ensuring every feature (auth, MFA, OAuth, WebAuthn, IdP/G-001, billing/G-002, compliance/G-004, partners, usage…) has secure, consistent, authz-enforced, rate-limited REST endpoints, AND **complete the OpenAPI/Swagger spec** to document them all. Tracked by issue #80 (OpenAPI) + a new API-completeness/security issue.
3. **G-003** JWT auth (S1–S5), then **G-001** IdP Phase A (A1–A5), then **G-002** billing (test-mode, fix B-025 first), then **G-004** compliance (L1 first).
4. Interleave **SECURE** fixes for any High finding (incl. B-024 CORS, the 11 deferred email/admin/api issues, FG-013 WebAuthn).
New findings this cycle: **B-024** (CORS `*`+credentials), **B-025** (billing schema drift), suggested **FG-018** (SCIM provisioning, Phase C of G-001).

## Decision log
- **2026-06-30 — Bootstrap.** User-confirmed: hybrid gate · in-scope feature ambition · hands-off = DB creds + git push · cap = 60. Dev-team artifacts under `.dev-team/` (root `SECURITY.md` is the GitHub disclosure policy; `PROJECT_PROGRESS.md` is the user's own doc — neither clobbered).
- **2026-06-30 — JWT API auth (B-006/FG-004 → G-003) gated**, not autonomous: vendoring a JWT lib is a dependency decision and the auth path is security-critical → user controls the *how*.
- **2026-06-30 — Recurring billing (#67/FG-003 → G-002) gated** as payment-touching (Bucket-3); route via dev-team-stripe when approved. B-007 (placeholder payment redirects) tied to it — verify wiring-only before any autonomous touch.
- **2026-06-30 — SIGNula-as-IdP (FG-001 → G-001) gated** as a large-architectural epic (today SIGNula only *consumes* OAuth; issuing SAML/OIDC/OAuth2 tokens to relying parties is the single biggest capability gap).

## Checkpoint log
- **cycle 29 (G-001 A5 hardening — the 3 queued findings + refresh grant)** — hardened the provider before the red-team. **B-058** (HIGH): `TokenService::refresh(refreshToken, ?authenticatedClientID)` now enforces client-binding via a single `$authenticatedClientID !== $clientID` check placed BEFORE reuse-detection/spend — correctly covers all 4 cases (first-party↔first-party OK; cross-client, RP-token-at-the-first-party-endpoint, and first-party-token-at-the-RP-endpoint all REJECT with a MED security alert; deliberately NO family-revoke on a pure authz mismatch — that would let an attacker DoS a real user by replaying an observed token with a wrong client attached). First-party `/api/v1/auth/refresh` now correctly refuses RP-bound tokens. Refresh minting gated behind `offline_access` (mintPair `withRefresh` lever). Then WIRED the **refresh_token grant** into /oauth/token (client-bound, rotation+reuse-detection) + advertised it in discovery. **B-059**: `SecurityUtils::checkRateLimit` per-IP + per-client on /oauth/token + /oauth/revoke (429 + Retry-After; reused jwt.rate_limit.* under new buckets — no migration). **B-057**: `sanitizeRedirectUrl()` on the MFA `redirect_after_login` at set-time (Auth.php) + consume-time (mfa/verify.php), with `is_scalar()` guards. Fixed a real regression (AuthLoginTest exercises Auth::login in-process → added a verbatim `sanitizeRedirectUrl` stub to _tests/bootstrap.php; conductor functionally verified the stub rejects https:///`//`/javascript:). +22 tests; unit 524, integration 179→**201** green (conductor reviewed the binding-check placement + offline_access gating + re-ran both). Red-team focus items noted (scope re-check on refresh, IP-key spoofability, HTTP-level 429). **Next: cycle 30 = MANDATORY independent RED-TEAM (Fable/Opus) of the whole provider; then cycle 31 = B-060 settings repair; then docs + button.**
- **cycle 28 (G-001 A4 — userinfo + discovery + revocation + Connected-Apps)** — SIGNula is now a DISCOVERABLE OIDC provider a standard client library can auto-configure against. Built: (1) **/oauth/userinfo** (`OAuthUserInfoService` + thin `userinfo.php`) — auth via untrusted-peek-of-`aud`→`verifyAccessToken(['aud'=>peeked])` (the RS256 sig proves WE minted it with that aud; a first-party/forged token is refused) → claims GATED STRICTLY by granted scope (always `sub`=pairwise `computeSubject`; email/email_verified only for 'email'; profile claims only for 'profile'; NO over-disclosure). The userinfo `sub` byte-matches the id_token `sub` (tested). (2) **/.well-known/openid-configuration** (directory + index.php + .htaccess, same directory-index trick as jwks.json) advertising the real endpoints, pairwise+public subject types, RS256, S256, authorization_code only (refresh_token withheld pending B-058). (3) **/oauth/revoke** (RFC 7009 — `OAuthRevocationService`, reuses `authenticateClient`) revoking ONLY tokens owned by the authenticated client (cross-client revoke returns 200 but does nothing — tested); + `TokenService::revokeClientForUser(userID,clientID)`. (4) **Connected-Apps** settings page (`connected-apps.php`) — lists active `tblOAuthConsents`, CSRF-protected per-app revoke that stamps `revokedAt` + kills that client's token family (`OAuthAuthorizeService::revokeConsent`). No migration needed. +6 unit +23 integration; unit 518→**524**, integration 156→**179** green (conductor reviewed the no-over-disclosure gating + cross-client-revoke ownership + peek-then-verify + re-ran both). **Surfaced B-060** (HIGH — 13 `settings/` account-management pages fatal on load via undefined `requireLogin()`/`SIGNULA_ROOT` + missing header/footer; the account UI the owner explicitly wants is broken; the builder avoided the broken pattern in connected-apps.php). **Next: cycle 29 = A5 = hardening (B-057/B-058/B-059) + MANDATORY RED-TEAM; then cycle 30 = B-060 settings repair; then the button.**
- **cycle 27 (G-001 A3 — the OAuth2/OIDC token endpoint + id_token)** — SIGNula now mints real tokens. Built `/oauth/token` (`web/public_html/oauth/token.php` thin controller + `web/private_html/auth/OAuthTokenService.php` engine) exchanging an A2 auth code for an access token + OIDC **id_token** (+ refresh). Applied the **3 G-003 gotcha fixes**: (a) new `Jwt::signIdToken()` — header `typ=JWT`, NO scope claim, `iss=oidc.issuer`, `aud`=client_id forced, `exp=oidc.id_token_ttl`; (b) new `TokenService::issueForClient()` — access token `aud=client_id` + refresh row records `clientID` (threaded an optional `clientID` through `mintPair`, INSERT + bind-types `'ississss'` extended additively — first-party `issueTokens` unchanged, clientID NULL); (c) `refresh()` now SELECTs `clientID` and re-derives `aud` so a rotated RP token keeps `aud=client_id`. Security (conductor adversarially reviewed): **atomic single-use** (`UPDATE…SET consumedAt=NOW() WHERE codeHash=? AND consumedAt IS NULL` + `getAffectedRows()===1`), **replay → family-scoped revoke** (new `tblOAuthAuthCodes.issuedFamilyID`, migration 032, links the code to exactly the family it minted — precise, not a broad user/client revoke; raises a security alert), **PKCE S256 constant-time** (`hash_equals` on base64url(sha256(verifier))), **exact redirect_uri** (`hash_equals`), **code↔client binding**, **expiry**, `Cache-Control:no-store`, RS256 via the foundation. `sub` = PAIRWISE per client (`OAuthClientManager::computeSubject`). +3 unit +16 integration (id_token verifies against the key with iss/sub[pairwise]/aud/nonce/auth_time; a different client → a different sub; replay rejected + first-exchange tokens revoked; expired/bad-PKCE/redirect-mismatch/cross-client/bad-secret all rejected; public client needs no secret). Unit 515→**518**, integration 140→**156** green; migration 032 applies clean (conductor re-ran both + reviewed the consume/replay/PKCE/bind-type diffs). **Surfaced B-058** (HIGH — `refresh()` lacks client-binding → the `refresh_token` grant was deliberately NOT wired yet; fix in A4/A5 before shipping it) + **B-059** (Low — no rate-limit on /oauth/token, fold into A5). **Next: cycle 28 = A4 (/userinfo + discovery + revocation + Connected-Apps).**
- **cycle 26 (G-001 A2 — the OAuth2/OIDC authorization endpoint)** — FEATURE CAMPAIGN RESUMED (safety floor closed). Built SIGNula-as-IdP stage A2 on top of A1's model: new `web/public_html/oauth/authorize-idp.php` (thin GET/POST controller at `/oauth/authorize-idp` via the existing `.htaccess` clean-URL rewrite — deliberately a NEW file, does NOT clobber the RP-side `oauth/authorize.php`) + a testable engine `web/private_html/auth/OAuthAuthorizeService.php` (`validateAuthorizeRequest`/`issueAuthorizationCode`/`hasCoveringConsent`/`buildRedirectUrl`). Security (the #1 OAuth-IdP surface — conductor adversarially reviewed): **exact-match redirect_uri with fatal-BEFORE-redirect ordering** (unknown/inactive client + redirect_uri-mismatch render a LOCAL error page, never a redirect; only after redirect_uri is a proven exact match do protocol errors 302 back), **mandatory PKCE S256** (rejects missing/plain for public/pkceRequired clients), **CSRF** on the consent POST, **never-trust-POST** (full re-validation from `$_POST`; tampered redirect_uri re-fails to the local page), **fail-closed deny**, single-use auth code stored **hashed** (sha256 → tblOAuthAuthCodes.codeHash, raw code only in the redirect), **state echoed** on every path, consent persisted in tblOAuthConsents (auto-skip on covering consent). Return-after-login uses the relative `/login?redirect=` pattern (builder found `Auth::requireAuth`'s absolute URL is rejected by `sanitizeRedirectUrl()`). NO token issuance yet (A3). +26 unit +11 integration tests; unit 489→**515**, integration 129→**140** green (conductor re-ran both + reviewed the fatal-ordering/PKCE/CSRF/never-trust-POST paths). **Surfaced B-057** (MED open-redirect on the MFA login path — pre-existing, unrelated to A2, queued for A5 hardening). **Next: cycle 27 = A3 /oauth/token + id_token.**
- **cycle 25 (safety floor CLOSED — B-053: admins are no longer 403'd)** — `Auth::getCurrentUser()` was missing the 4 admin/partner-context keys the 50 admin/partner sites gate on, so every gate failed closed. Enriched it (ADDITIVE): `isSuperAdmin` (real tblUsers column from migration 009, cast to bool) added to the SELECT; `partnerID` = primary active `tblPartnerTeamMembers` membership (root-preferred, else lowest id; null if none) via one extra prepared query; `isPartnerAdmin` = has-active-membership; `isAdmin` = `isSuperAdmin || has-membership`. Null-safe, enriched array cached, no AccessControl wire-in (dodges an Auth↔SessionManager↔AccessControl cycle). 4 new integration tests (3 personas + multi-membership ordering + cache). Unit 489, integration 125→**129** green (conductor re-ran both + reviewed the diff: prepared statements, additive, null-guarded). **⟹ THE SAFETY FLOOR IS CLOSED.** The cluster that cycle-21 opened — B-051 (SessionManager) + B-052 (settings load) + B-055 (login session) + B-054/B-056 (6 missing `_backend/*` files) + B-053 (admin flags) — is fully fixed: **the admin/partner UI and login now work end-to-end**, a large chunk of the owner's 'fully working SIGNula.id' goal. **Next: cycle 26 = resume G-001 A2 (/oauth/authorize-idp + PKCE(S256) + consent screen) — the OAuth authorization endpoint the 'Sign in with SIGNula.id' button drives.**
- **cycle 24 (safety floor — B-054 + B-056: the 74 admin/partner pages finally LOAD)** — the 74 pages require SIX `_backend/*` files that were never committed; B-051 fixed SessionManager, this cycle fixed the rest. **B-054**: `Database.php` — the pages call `Database::getInstance()->prepare(...)` (raw-mysqli style, 262 sites) but the real `Database` class is 100%-static with no `getInstance()`, and a second `class Database` would redeclare-fatal against config.php's. Solution: added `getInstance()` to the EXISTING static class returning a new `DatabaseConnection` adapter (proxies to the shared mysqli via `__call`/`__get` + real `getConnection()`), and made `_backend/Database.php` a collision-free require-shim. **B-056** (surfaced mid-cycle): 4 MORE missing `_backend` shims (`ActivityLogger` ×10, `APIKeyManager`, `RateLimiter`, `ServiceFeeManager` — real classes all under `private_html/`) → created all 4 as guarded require-shims (folded into this cycle so it delivers the WHOLE 'pages load' outcome, not a partial fix). All 4 real classes have zero internal requires (no deeper rabbit hole). Conductor independently verified: all 7 `_backend/*` requires resolve, shims declare no class, full bootstrap chain (config→Database→SessionManager→AccessControl→ActivityLogger→…) loads with no fatal, both suites re-run green. Unit 481→**489**, integration 120→**125**. **The admin/partner UI + login now work e2e** (B-051+B-052+B-055+B-054+B-056 together). **Next: cycle 25 = B-053 (admin flags missing from getCurrentUser → admins 403'd), the last safety-floor item; then G-001 A2.**
- **cycle 23 (safety floor — B-055: login now actually creates a session)** — Sonnet fixed the core-auth breakage. `Auth::completeLogin()`'s `tblUserSessions` INSERT bound `$expiresAt` (a datetime string) with type `i` → coerced to `2026` → INSERT threw on the connection's forced `STRICT_ALL_TABLES` sql_mode → `completeLogin()` returned false → but `login()` ignored it and reported `success:true`. **This broke EVERY login** (the pre-existing `AuthLoginTest` only ever asserted the phantom `success===true`, never that a session row existed). Fix: bind type `'sissssssssii'`→`'sissssssssis'`; `login()` now returns a real failure when `completeLogin()` fails; the 3 OTHER callers that discarded the boolean via the `loginOAuth()` wrapper (oauth/callback.php ×3, mfa/verify.php ×1 — they redirected to /dashboard on a phantom login) are guarded too. New integration test (`_tests/Integration/Auth/AuthCompleteLoginSessionTest.php`, 2 tests) asserts a real `tblUserSessions` row with a correct future `expiresAt` + failure-propagation; **conductor independently proved the test fails on the pre-fix bind (reverted the 1 char → test failed → restored)**. Unit 473, integration 113→**115** green (conductor re-ran both). **Next: cycle 24 = B-054 (`_backend/Database.php` missing + `Database::getInstance()` absent → 74 pages still fatal), then cycle 25 = B-053 (admin flags), then G-001 A2.**
- **cycle 22 (safety floor — B-052 + B-051 fixed; 3 more HIGH surfaced)** — Sonnet fixed the 2 HIGH hidden-broken issues from cycle 21. **B-052**: `loadSettings()` now try/catches the per-row decrypt — a blank/undecryptable `isSensitive` row is logged + skipped and the loop continues (fresh install went 10/369 → 369/369 settings loaded). **B-051**: created `web/_backend/SessionManager.php` (3 static facade methods over `Auth::isAuthenticated`/`getCurrentUserID`/`getCurrentUser`; ctor absorbs the `$db` arg the 72 call sites pass) + fixed `AccessControl::loadPartnerMemberships` SELECT to real columns aliased back (`p.partnerName AS companyName, p.accountTier AS tier, p.isActive AS partnerStatus`). Unit 466→**473**, integration 110→**113** green (both re-run independently by the conductor). **Surfaced 3 MORE HIGH hidden-broken issues (same class, all code-confirmed)** → new safety floor before G-001 A2: **B-055** (login's `tblUserSessions` INSERT binds `$expiresAt` string as `i` → coerced to `2026` → INSERT fails on strict MySQL → `completeLogin()` returns false but `login()` ignores it → **login silently creates no session**; 1-char fix `…ii`→`…is`), **B-054** (`_backend/Database.php` ALSO never committed + 79 sites call non-existent `Database::getInstance()` → the 74 pages fatal one require past B-051), **B-053** (admin flags `isSuperAdmin`/`isAdmin`/`partnerID`/`isPartnerAdmin` absent from `Auth::getCurrentUser()` SELECT → 50 sites → admins 403'd). **Next: cycle 23 = B-055 (core-auth, do first), then B-054, then B-053 — the admin/partner UI + login must actually work before resuming G-001 A2.**
- **cycle 0 (DISCOVER)** — docs-only: PROJECT.md + FEATURES.md written; 0 PHP parse errors; 342 unit tests green; tooling config bug found (B-001). Commit: bootstrap.
- **cycle 3 (STABILIZE-1)** — B-001/B-002/B-005 PASS: `composer analyze`/`check-style` run; phpstan unknown-class FPs 647→74 (74 genuine); 5 guards added; 342 tests green, no regression. EmailDripProcessor guard placed after its config-require (CLI entrypoint) — lead ACCEPTED.
- **cycle 4 (STABILIZE-2)** — B-020 safety net PASS: +48 tests (342→390 green, stable across 3 seeds), no behaviour change. Net surfaced 2 HIGH (B-027 WebAuthn registration broken via missing `Database::insert()`; FG-013 WebAuthn auth-bypass — unsigned assertions accepted, PoC test) + pinned B-024/B-012/B-011/B-028. Per safety floor, next cycles fix WebAuthn before the feature campaign.
- **cycle 21 (G-001 A1 — OAuth client-registration model)** — Sonnet built the OAuth2/OIDC provider foundation: migration 031 (5 tables — tblOAuthClients [pairwise `subjectType`/`sectorIdentifier`, hashed secret, exact-match redirect allowlist] + redirect-URIs + auth-codes + consents + access-grants; BIGINT FKs; `oidc.issuer=https://signula.id`); `OAuthClientManager` (`isExactRedirectMatch` strict `===`, `verifyClientSecret` hash_equals + public-clients-never-verify, `computeSubject` pairwise=SHA256(sector\|user\|salt)); a partner-admin OAuth-client CRUD page + JSON API (ownership-scoped, cross-partner IDOR rejected — HTTP-smoke-verified). Unit 444→466, integration 90→110 green; mig 031 clean (101 tables). **Surfaced 2 HIGH hidden-broken issues**: **B-051** (`_backend/SessionManager.php` never committed → **74** admin/partner files fatal on require — confirmed) + **B-052** (`loadSettings()` silently aborts on the first undecryptable isSensitive row → fresh install starves all settings). No /oauth endpoints yet. **Next: fix B-051+B-052 (they undermine 'fully working'), then G-001 A2.**
- **cycle 20 (IB0 — SIGNula.id logo)** — Sonnet built the brand system per the owner's spec: animated key-into-lock + checkmark **symbol** (CSS keyframes, `prefers-reduced-motion` freeze), **'SIGNula.id' wordmark**, **combined lockup** — all vector SVG with named layers (`key`/`lock`/`check`/`wordmark`) for easy recolour, light/dark/colour-blind-safe + Base64 fallback. Palette = blue→purple (#667eea→#764ba2, CB-safe, WCAG) over teal. Wired the previously-404 favicons + 8-size PWA icon manifest. Preview: `web/public_html/assets/brand/preview.html`. Fixed a real invisible-checkmark renderer bug. Assets only. **Owner: approve palette; wordmark is live `<text>`; maskable safe-zone first-pass.**
- **cycle 19 (planning)** — Fable 5 deep-planned the partner integration layer (#87) → `.dev-team/specs/G-001-integration-plan.md` (11 model-tagged stages); recorded the model policy (Fable plan / Sonnet-Haiku impl / sequential); owner branding decisions confirmed (brand SIGNula/signula.id, subject_type pairwise, custom logo).
- **cycle 18 (G-003 S5 — docs; G-003 BUILT)** — docs-only final stage: `SECURITY_SETUP.md` JWT API Auth section (all 14 `jwt.*` settings, KeyManager generation/rotation runbook, security-properties table, integrator guidance); `openapi.yaml` +4 auth paths (`/auth/{token,refresh,revoke}` + `/.well-known/jwks.json`) + 7 schemas + `bearerAuth` scheme (42→46 paths, validates); `CHANGELOG` JWT block. No code change (unit 444 + integration 90/90 hold). **✅ G-003 (JWT API authentication, #41) is FULLY BUILT** — S1 crypto → S2 tokens → S3 endpoints → S4 red-team → S5 docs; gate_ledger G-003 → `built`. **First approved epic complete.** Next: **G-001 IdP Phase A** (reuses the G-003 Jwt/KeyManager/JWKS signing foundation).
- **cycle 17 (G-003 S4 — mandatory red-team + BLUE/VERIFY)** — an INDEPENDENT red-team hit the live JWT auth across 12 vectors. **HELD**: alg-confusion/none, signature tamper, key confusion, replay, refresh-reuse (family revoke + alert), temporal, aud/iss, **IDOR-on-issuance** (cross-user mint denied), scope escalation, JWKS private-field leak, X-API-Key. **2 real bypasses found → FIXED → re-attacked closed**: (1) **HIGH** — `kid` path-traversal in `KeyManager` file-fallback (attacker-controlled `kid` → read a placed `.pub` → forge a token for ANY user; also arbitrary-file-read); fixed with `isValidKid()` strict-charset guard + `basename()` + 2 regression tests. (2) **MED (B-049)** — `SecurityUtils::checkRateLimit` per-second window bypassable by pacing → cross-account password spray; fixed to a windowed SUM (single-account lockout already held). Also fixed `getUserByApiKey` (queried non-existent `k.apiKey` → now hashes to `keyHash`). Unit 442→**444**, integration 90/90. Residual **B-050** (getUserByApiKey JOIN `k.userID` — no such column; `tblAPIKeys` is partner-scoped; dead code, fails closed). **Auth entrypoint is now red-team-hardened.** Next S5 = docs + OpenAPI; then G-003 → built.
- **cycle 16 (G-003 S3 — endpoints + Bearer wiring)** — live JWT auth surface: `POST /api/v1/auth/{token,refresh,revoke}` + `GET /.well-known/jwks.json` (public keys only, htaccess-rewritten) via new `JwtAuthController`; `BaseController::getUserByToken()` (was stub) now verifies Bearer via `TokenService::verifyAccessToken` + loads active user, alongside X-API-Key (precedence session→Bearer→key, fails closed). **Primary-credential authz**: token subject resolved ONLY from a server-verified identity (session or `Auth::login`-verified password), never client input (proven: cross-user mint denied); MFA users get `mfa_required`. Rate-limited (+`jwt.rate_limit.*` in mig 030). Integration 81→**90/90 green** (+9), unit 442. **Found+fixed 2 pre-existing API-WIDE bugs**: `Router::executeHandler` never injected the router (every controller dispatch fataled — the REST API only ever ran via reflection in tests, never real HTTP! — B-048) + `APIKeyMiddleware` treated Bearer JWTs as API keys (401'd them). No key material committed (.gitignore hardened). NEEDS-LEAD-REVIEW for S4 red-team: per-second rate-limit window (bursts only — B-049); `Auth::login` mints a browser session on API token issue. **S4 = MANDATORY red-team next.**
- **cycle 15 (G-003 S2 — TokenService)** — access-token JWT + opaque refresh (`bin2hex(random_bytes(32))`, SHA-256-hashed only, family-based) issuance; refresh via ATOMIC single-use spend (`getAffectedRows()===1`) → rotate; **REUSE-DETECTION**: a spent/rotated refresh token → revoke the ENTIRE family + HIGH `session_hijack` SecurityAlert + deny (verified: family live→0, 1 alert row); `revokeAccessJti`/`revokeAllForUser` (bumps `tblUsers.tokensInvalidBefore`); jti-denylist + cutoff wired into `verifyAccessToken`. Integration 71→**81/81 green** (+10), unit **442** green. Also fixed migration 030's errant `USE signula;` (mis-targeted non-signula DBs — B-046 done) + setup_test_db harness strip. Residual B-047 (SecurityAlertManager passes 'high' into activityLog severity ENUM → truncation, same class as B-043). No HTTP endpoints (S3 next).
- **cycle 14 (G-003 S1 — JWT crypto foundation)** — **FEATURE CAMPAIGN STARTED.** Vendored firebase/php-jwt 6.11.1 (BSD-3, self-hosted) into `web/_lib/jwt/` + `JwtLibLoader`; `Jwt` facade with **RS256 pinned** (verify builds `Key(pubPem,'RS256')` → library rejects alg≠RS256 before sig check, defeating alg:none + RS/HS confusion); `KeyManager` (RS256 key encrypted in tblSettings isSensitive + `_private` 0600 fallback, kid rotation, JWKS strips private `d`); migration 030 (tblRefreshTokens + tblRevokedTokens jti-denylist + tblUsers.tokensInvalidBefore; applied clean on scratch DB, idempotent). +32 security tests (407→**439** green): alg-confusion/none/tamper/expired/wrong-iss-aud/revoked-jti all REJECTED; JWK→PEM independently validated. Accepted deviations: lib BSD-3 not MIT (permissive ✓); migration 030 (not 025) with BIGINT FKs + real `settingCategory` cols (spec draft stale). No endpoints yet (S3). Next: S2 issuance + refresh-rotation/reuse-detection.
- **cycle 13 (docs sync + state cleanup)** — **FOUNDATION PHASE CLOSED.** Synced the drifted user docs (CHANGELOG.md, PROJECT_PROGRESS.md, .claude/PROJECT_STATUS.md, _docs/setup/SECURITY_SETUP.md) to the honest state — cycles 3-12 repaired a broken foundation the "100% complete" docs hid. Clean-rewrote `autopilot.json`: STABILIZE `done`, `phase_cursor`→3 (COMPLETE), `next_action` trimmed. Docs/state only — unit 407 + integration green unaffected. **Feature campaign (G-003 JWT → G-001 IdP → G-002 billing TEST-MODE → G-004 compliance → CAP-API #86) begins cycle 14.**
- **cycle 12 (integration-gate reliability)** — fixed the ~2/3-runs flake at root: (1) ENUM truncation from code writing invalid values — `MFA.php` (15) + `NotificationService` (5) had MISORDERED `log()` args dumping the description into the `activityCategory` ENUM slot; `SecurityUtils` wrote `'ip_address'` not `'ip'`; WebAuthn wrote severity `'high'`; fixed the code + widened the `activityCategory` ENUM (mig 029, + installer). (2) the real flake = `ActivityLogger::log()` writes on the Database SINGLETON (outside the test's rolled-back txn) with a FK to `tblUsers`, but tests hard-coded `userID=1` which a prior test truncated → FK fail; fixed with committed-test-user hermeticity + `AuthLoginTest` truncating `tblRateLimits`. **Integration now 5/5 (indep) + 20/20 (builder) green**, unit 407. + B-039 urlencode (4 sites, defensive — tokens are hex). Another 'hidden broken' find: MFA/notification activity logging was writing garbage categories. Residual B-045 (per-file test-subset class-loading, low).
- **cycle 11 (install-wizard + multi-org)** — B-041: the installer recorded only 3 mismatched `tblMigrations` names → the wizard re-applied baked migrations 001-013; fixed by seeding all 17 baked filenames as `completed` (12→0 errors). Also fixed `setup_test_db.sh` silently skipping migration 014 (its 5 security tables were absent from the test DB → now 98 tables). B-034: migration 028 creates the 6 org tables `Organization.php` needs (multi_query-safe, idempotent, smoke-tested every query). Unit 407; integration flaky (B-037 — passes in isolation/some runs). **SURFACED B-044 (HIGH):** the wizard's `mysqli::multi_query` can't parse CLI-only `DELIMITER`, so the installer's stored-proc blocks + mig 018 fail via the *wizard* — the **CLI/phpMyAdmin SQL-load install path is clean**. Foundation install works via the documented CLI path.
- **cycle 10 (B-040 WebAuthn regression)** — root cause was NOT WebAuthn: cycle-8's B-030 was the first code to gate on `Database::getAffectedRows()===1`, exposing that the accessor returned **-1** for every prepared UPDATE (read `affected_rows` after `$stmt->close()` → mysqlnd -1). One-file fix (database.php: capture affected_rows while the statement is open) restores WebAuthn AND repairs **12 other silently-broken callers** (UsageTracker/PasswordlessLogin/WebhookService/NotificationService…). Integration now **71/0 green**, Unit 407, WebAuthnVerifyTest 8/8; FG-013 + B-030 red-team re-verified still closed. Residual B-043 (ActivityLogger/RateLimiter ENUM-width "Data truncated" log noise, low; may relate to B-037 flake).
- **cycle 9 (B-033 fresh-install)** — schema reconciliation: consolidated installer + migrations path now apply with **0 errors** (was ~74), independently verified (74 tables, exit 0). Fixed INT/BIGINT FK drift (96 cols), `tblMigrations`/`tblSettings` column drift, deduped WebAuthn tables, restored missing FK targets, idempotent seeds; **un-ignored the installer** (`_database/*.sql` was excluding it from clean checkout → fresh install doubly broken). Edited 27 source migrations (safe: no prod DB yet; additive/non-destructive; runner won't re-run applied ones). Residuals: **B-040** (cycle-8 B-030 WebAuthn change broke 4 `WebAuthnVerifyTest` integration tests — HIGH regression/security), **B-041** (install WIZARD `install/index.php` still 44 idempotency errors — re-runs mig 001-014), **B-042** (`build-complete-install.sh` generator stale).
- **cycle 8 (SECURE cleanup)** — 3 parallel security workstreams (disjoint files) + a test-isolation fix. Landed B-024 (CORS never `*`+creds, configurable allowlist), #29 (X-Frame:DENY/CSP frame-ancestors/nosniff on all API responses), B-030 (WebAuthn challenge consumption now atomic compare-and-set) + B-031, and 13 deferred email/admin issues (#22-#34). Migration 027 (credential-reset composite indexes). BONUS: root-caused + fixed the long-standing `session_start` flake → suite deterministically clean (407, **0 warnings**, 30+ runs). Also found+fixed a 2nd CSRF hole (rate-limits unblock), surfaced B-039 (4 token-URLs need urlencode), and reinforced B-033 (mig 017/024 write to a `tblMigrations` schema the installer lacks). GitHub #22-#34 addressed-on-branch (not closed — unpushed). Integration re-confirm deferred to cycle 9 (MySQL restart flaky).
- **cycle 7 (B-029 triage)** — MAJOR foundation find. Made the Integration suite runnable (reproducible `_scripts/setup_test_db.sh`) and took it 39 errors/14 failures → green-on-rerun (B-037 flake remains). Fixed 3 genuinely-broken **core flows** hidden from the DB-stubbed unit suite: error logging (`ErrorLogger::log` undefined, 61 call sites), email queue (`EmailService` bind-count → *every* `queueEmail()` failed), backup-code MFA (ENUM mismatch); + additive migrations 025/026 fixed broken user registration (missing `organizationID`) + missing MFA columns. **Reality check: the "100% complete beta" had significant hidden breakage.** Residuals → B-033 (HIGH: broken installer, blocks clean install), B-034 (multi-org tables missing), B-035/B-036/B-037/B-038.

## Feature Specs

Full build-ready specs authored 2026-06-30 (user-approved G-001..G-004); build cycles cite these verbatim. **Migration numbers are conductor-assigned sequentially AT BUILD TIME** (existing run 001–024; several specs tentatively claimed 025/026 — resolve collisions when each builds).

### #spec-G-003 — JWT API Authentication (build FIRST) → `.dev-team/specs/G-003.md`
Vendor `firebase/php-jwt` v6.x into `web/_lib/jwt/` (self-host pattern, MIT, ext-openssl). RS256 + public JWKS; access JWT (15-min, `jti`, `scope`) + opaque SHA-256 refresh token with family rotation + reuse-detection; `jti` denylist (`tblRevokedTokens`); signing key encrypted in tblSettings (`_private` PEM fallback), `kid`-rotated. Endpoints `/api/v1/auth/{token,refresh,revoke}` + `/.well-known/jwks.json`; wire `BaseController::getUserByToken()`/`requireAuth()` to accept Bearer alongside X-API-Key. Stages: S1 crypto foundation → S2 issuance+rotation → S3 endpoints/wiring → S4 JWKS+hardening → S5 docs.

### #spec-G-001 — SIGNula as IdP (OAuth2/OIDC provider; SAML later) → `.dev-team/specs/G-001.md`
Phase A (buildable, reuses G-003 signing): `/oauth/authorize`+consent, `/oauth/token` (auth_code + PKCE-required + refresh), `/oauth/userinfo`, OIDC discovery, `id_token`. RP clients owned by `tblPartners`; new `tblOAuthClients/tblOAuthAuthCodes/tblOAuthConsents/tblOAuthAccessGrants` (secrets hashed like `tblAPIKeys`). Phase B SAML IdP (needs vendored `xmlseclibs`, separately gated). Phase C SCIM → suggested **FG-018**. **Depends on G-003 (ships first).**

### #spec-G-002 — Recurring Billing Engine (⚠️ TEST MODE ONLY) → `.dev-team/specs/G-002.md`
Provider-managed subscriptions (PayPal/Stripe own the recurring clock; SIGNula reacts to webhooks). Lifecycle: pending/trial/active/past_due/**grace**/paused/cancelled/expired. Reuses BillingScheduler/PaymentManager/providers/InvoiceManager. New: grace state, dunning columns, `tblBillingAttempts` (UNIQUE `idempotencyKey`), `changeTier()` bcmath proration. `assertTestMode()` fail-closed; **live activation = user op #70**. BUILD-FIRST FIXES: B-025 schema drift + missing `chargeStoredMethod()` (pivot resolves it).

### #spec-G-004 — Multi-jurisdiction Compliance (data-driven) → `.dev-team/specs/G-004.md`
L1 (build first, autonomy-eligible): `tblConsentRecords`+`tblDataSubjectRequests`+events, DSAR tracker, admin queue, user form — `DSARManager` **delegates** to existing `AccountManager` export/delete (no dup; one additive edit to `permanentlyDeleteAccount()` to anonymise). L2 consent banner/preference-center + GPC. L3 `tblComplianceRegimes` data-driven model (`RegimeResolver` — adding a regime = row inserts, 0 PHP edits). L4 breach log/RoPA/retention/COPPA age-gate. **Anonymise-don't-delete** on erasure. Legal values ship draft/empty (user fills).

## Codebase Map

SIGNula is a "universal" CIAM / single-sign-on identity platform. **PHP 8.4.17** local (min 8.3), **MySQLi + prepared statements**, MariaDB/MySQL. Self-hosted libs (no Composer in prod — Dreamhost shared). Reported version: PROJECT_PROGRESS.md says `2.7.0-beta`, but the `VERSION` file says `2.6.0-beta` (doc inconsistency — see B-021). **271 PHP files** under `web/` (brief said ~295), **~84 DB tables** across 24 migrations (brief/docs said ~49; docs undercount), **24 migrations** (001–024, not 19). PROJECT_PROGRESS.md marks nearly every component "✅ 100% Complete" — but several flows are TODO stubs (JWT, contact-form persistence, multiple email notifications). **Trust code over docs.**

### Directory layout
- `web/public_html/` — web-served document root. Subdirs: `auth/`, `mfa/`, `settings/`, `admin/` (+ `admin/api/`, `admin/security`, `admin/payments`, `admin/rate-limiting`, `admin/webhooks`…), `api/` (`api/v1`, `api/oauth`, `api/webauthn`, `api/docs`), `oauth/`, `partners/` (+ `partners/api`, `partners/admin`), `organization/`, `checkout/`, `pricing/`, `invoices/`, `avatar/`, `cron/`, `email/`, `webhooks/`, `install/`, `error/`, and branded dirs `SIGNula.com/` (marketing "shop window": about/contact/features/pricing/blog/docs/legal/support) + `SIGNula.id/` (hub: admin/support).
- `web/private_html/` — non-served business logic (the class library). Subdirs: `auth/` (+ `auth/providers/`), `security/`, `email/` (+ `email/providers/`), `payments/`, `api/` (+ `api/controllers/`), `utils/`, `notifications/`, `webhooks/`, `i18n/` (+ `i18n/lang/en/`), `admin/`, `layout/`.
- `web/_config/` — `config.php` (bootstrap) + `database.php` (Database singleton). `web/_includes/`, `web/_lib/` (self-hosted `crawlerdetect/`, `tcpdf/`), `web/_private/` (templates/uploads, outside web root, `_`-prefixed per house rule), `web/_backend/`, `web/_scripts/`.
- `web/public_html_dev/` and `web/public_html_landing/` — near-empty stubs (favicons + one index). **No `alpha_html`/`beta_html` dirs exist** despite `ALPHA_DIR`/`BETA_DIR` constants in config.php (brief gap — B-019).
- `_database/migrations/` (001–024 SQL) + consolidated installers `signula_complete_install_v2.2.3.sql` / `_v2.5.0.sql` + `archive/`.
- `_tests/` (PHPUnit: `Unit/`, `Integration/`, `Fixtures/`, `bootstrap.php`, `TestCase.php`, `DatabaseTestCase.php`), `_docs/` (24 docs dirs), `_scripts/` (+ `git-hooks/`). Root: `phpcs.xml`, `phpstan.neon`, `phpunit.xml`, `composer.json/.lock`, bundled `composer` phar, `README.md`, `PROJECT_PROGRESS.md`, `CHANGELOG.md`, `SECURITY.md`, `VERSION`.

### Entry points & URL routing (clean URLs — brief forbids visible `.php`)
Achieved via **Apache `mod_rewrite` in `.htaccess`** (NOT a DB router table). Main `web/public_html/.htaccess`: strips/adds `.php` (`RewriteRule ^(.+?)/?$ $1.php [L]`), exempts real dirs/assets/`_includes`, 301-redirects trailing slashes, blocks dotfiles/`.git`/`composer.json`/`_config`/`_private`, and has query-string SQLi pattern blocks. `web/public_html/api/.htaccess` is a **front-controller**: all `/api/*` → `api/v1/index.php [QSA,L]`. So routing is a hybrid: directory/`.htaccess` rewrite for pages, single front-controller + `Router` class for the REST API. HTTPS-force and www-canonical rules are present but **commented out** (production hardening pending — issue #79). Per-area `.htaccess` also in `SIGNula.com/`, `install/` (locks after `.installed`), `avatar/`, `api/`.

### Bootstrap / config chain (`web/_config/config.php`)
Defines `SIGNULA_INIT` guard, path constants (`ROOT_DIR`, `PRIVATE_DIR`, `LOGS_DIR`, `ALPHA_DIR`, `BETA_DIR`…) using `DIRECTORY_SEPARATOR`, env/`DEBUG_MODE`/`DISPLAY_ERRORS` (the `?debug=true` switch the brief asked for), `require_once` of `database.php`, `loadSettings()` → `$GLOBALS['settings']` from `tblSettings` with `SecurityUtils::decrypt()` for `isSensitive` rows, session init (secure cookie flags, name `SIGNULA_SESSION`, ID regen every 5 min, `SessionGuard`), a directory-scanning autoloader over `_includes/` + `private_html/` subdirs, `SecurityMiddleware::handle('web')` on every request, and global helpers `getSetting`, `getClientIP` (CF/XFF aware), `getUserAgent`, `redirect`, `sanitizeRedirectUrl` (open-redirect guard), `jsonResponse`, `sanitizeInput`, `isValidEmail`.

### Data model (~84 tables)
**Identity/Auth:** tblUsers (core accounts), tblSessions (DB-backed sessions w/ device/MFA flags), tblSessionFingerprints (hijack detection), tblPasswordResetTokens, tblPasswordlessTokens (email-link login), tblEmailVerificationTokens, tblPasswordHistory (reuse prevention). **MFA/Passkeys:** tblUserMFA (TOTP/SMS + backup codes), tblWebAuthnCredentials (FIDO2, signature counter), tblWebAuthnChallenges. **OAuth:** tblOAuthAccounts (linked third-party accounts), tblUserOAuthTokens (delegate-mailbox tokens for Graph/Gmail). **Profile:** tblUserPreferences, tblUserAvatars (per-partner, upload/oauth/url). **Logging:** tblActivityLog (full audit trail incl. IPv4/IPv6 + UA), tblErrorLog (exceptions+stack), tblAdminAuditLog. **Config:** tblSettings (KV, isSensitive encryption). **Email system:** tblEmailTemplates (versioned), tblEmailCampaigns, tblEmailABTests/Variants, tblEmailDripCampaigns/Steps/Subscribers/Progress, tblEmailRecurringSchedules, tblEmailTrackingEvents, tblEmailUnsubscribes, tblEmailProviderHealth. **Contact:** tblContactSubmissions. **Blog:** tblBlogPosts/Categories/Tags/PostTags/Comments. **Support:** tblSupportTickets/Replies/Attachments/Categories. **Security:** tblIPReputationCache (AbuseIPDB/proxycheck, TTL), tblBlockedIPs, tblSecurityAlerts (brute force/impossible travel/hijack), tblCircuitBreaker. **Rate limiting:** tblRateLimits, tblRateLimitConfig (per-tier/endpoint). **Partners/Multi-tenant:** tblPartners, tblPartnerTeamMembers, tblTeamInvitations, tblPartnerFeatures, tblFeatureToggles, tblAPIKeys (test/live, scoped, IP-whitelist), tblAPIKeyUsage, tblAPIKeyAudit. **Webhooks:** tblWebhookEndpoints + tblWebhookDeliveries (partner outbound, HMAC), tblWebhooks + tblWebhookDeliveryLog (self-service outbound), tblInboundWebhooks (payment providers). **Payments/Billing:** tblSubscriptionTiers, tblSubscriptions, tblPayments (multi-provider), tblPaymentMethods (tokenised only), tblDiscountCodes/Assignments, tblProviderDiscounts (incl. crypto discount), tblPartnerPaymentConfig (own-keys vs SIGNula-keys), tblPartnerSubscriptionTiers, tblServiceFees/ServiceFeeTransactions, tblRemittances (partner payouts), tblBillingSchedule, tblInvoices (PDF/line items), tblCreditBalances/CreditTransactions. **Usage:** tblUsageMetrics, tblUsageRates (fixed/usage/hybrid), tblUsageRecords, tblUsageBillingSummary. **Credential reset:** tblCredentialResets, tblCredentialResetUsers, tblSaltRotationHistory. **Notifications/GDPR:** tblNotifications (in-app center), tblDataExportRequests (GDPR portability/erasure). **Meta:** tblMigrations.

### Key classes (`web/private_html/`)
- **Database** (`_config/database.php`) — instantiated singleton; MySQLi + prepared statements w/ auto type-detection (`prepare`/`bind_param`); `query/fetchOne/fetchAll/transaction/escape`. Has SIGNULA_INIT guard; **no** `declare(strict_types=1)`.
- **Auth** (`auth/Auth.php`, static) — register/login/logout/changePassword/isAuthenticated/getCurrentUser/requireAuth. Account lockout after failed attempts (lockout email = TODO L573).
- **AccountManager** (`auth/AccountManager.php`, static) — GDPR deletion + data export (`requestAccountDeletion/exportUserData/serveExportFile/processScheduledDeletion`).
- **OAuth stack:** `OAuth.php` (abstract base), `OAuthFlowHandler.php` (authz code flow, provider switch incl. microsoft_graph/google_workspace), `OAuthTokenManager.php`, plus `auth/providers/`: **GoogleOAuth, MicrosoftOAuth, AppleOAuth, FacebookOAuth, LinkedInOAuth, PayPalOAuth, YahooOAuth, AmazonOAuth, WordPressOAuth (9 wired)**. callback.php loads `{$providerClass}.php` dynamically, so all 9 dispatch generically.
- **Organization** (`auth/Organization.php`) — org accounts, member mgmt, domain verify (invite email = TODO L300).
- **PasswordlessLoginHandler**, **WebAuthnHandler** (FIDO2 register/authenticate ceremonies).
- **security/** (mostly static): **MFA** (TOTP/email-OTP/backup-codes/trusted-device), **TOTP** (RFC 6238), **SecurityUtils** (AES encrypt/decrypt, Argon2id hash, CSRF token gen/verify, sanitizeString/Email, validatePassword), **RateLimiter** (token bucket — *missing SIGNULA_INIT guard*), **CaptchaVerifier** (Turnstile primary + reCAPTCHA v3 fallback, fail-open), **IPReputationChecker** (AbuseIPDB+proxycheck, circuit breaker), **BotDetector** (CrawlerDetect lib + regex), **SessionGuard** (composite fingerprint), **SecurityAlertManager**, **SecurityMiddleware** (`handle('web')` / `handleFormSubmission`), **FormProtection** (honeypot + HMAC timing + JS challenge), **QRCode**.
- **email/** (static utilities): EmailService (`sendTemplateEmail/sendDirectEmail/queueEmail/bulkQueue`), EmailTemplateBuilder (HTML/AMP/dark-mode/MIME assembly), EmailTemplateManager (template CRUD + versioning + preview), EmailQueueProcessor, EmailScheduler, EmailDripCampaign, EmailDripProcessor (*missing guard*), EmailPersonalization, EmailTracker, EmailABTesting, EmailSecurityManager (DKIM/SPF/DMARC), EmailProviderHealthMonitor, EmailWebhookHandler. `email/providers/`: **SMTP, SendGrid, Mailgun, Postmark, AmazonSES, GmailAPI, MicrosoftGraph** (+ `EmailProvider` base) = 7 wired.
- **payments/** (static): PaymentManager (subscription lifecycle, discount validation), **StripeProvider, PayPalProvider, CoinbaseProvider (crypto), KofiProvider, PatreonProvider** (raw cURL, no SDK; webhook signature verify), InvoiceManager (TCPDF), CreditManager (row-lock ledger), UsageTracker, UsageBillingService, BillingScheduler, BillingLazyCheck, ServiceFeeManager, PartnerPaymentService (provider discount incl. crypto).
- **api/** : Router, BaseController (input/validate/requireAuth/paginate/success/error; **getUserByToken = JWT stub returns null**), Response (success/error/validationError/…), Validator, APIKeyManager + APIKeyMiddleware (X-API-Key, IP whitelist, scoped permissions; *both missing guard*), RateLimitMiddleware (*missing guard*), WebhookManager. `api/controllers/`: AuthController, UserController, MFAController, OAuthController, UsageController.
- **utils/**: AvatarService (priority chain upload→oauth→gravatar→generated), ExportService (CSV/XLSX/PDF/Google Sheets/Excel Online), ActivityLogger, ErrorLogger. **notifications/**: NotificationService. **webhooks/**: WebhookService. **i18n/**: I18n (file-based, `lang/en/*`). **admin/**: CredentialResetService (mass reset/salt rotation).

### Integrations actually wired
- **Email/SMTP:** 7 providers (above). **Captcha:** Turnstile + reCAPTCHA v3 (CaptchaVerifier). **Payments:** Stripe, PayPal, Coinbase(crypto), Ko-fi, Patreon — each with a `web/public_html/webhooks/{provider}.php` inbound handler (+ `webhooks/email.php`). Apple Pay / Google Pay = settings + routed through Stripe/PayPal, **no standalone provider class**. **OAuth callbacks:** `oauth/authorize.php` + `oauth/callback.php` (+ `api/oauth/disconnect.php`), generic over the 9 provider classes. **WebAuthn:** `api/webauthn/{register,auth}-{options,verify}.php`.

### Session / auth model
DB-backed sessions (`tblSessions`: hashed token, encrypted data, device/IP/UA, `isMFAVerified`, expiry). Cookies: httponly, `use_only_cookies`, secure-in-prod, SameSite=Lax, name `SIGNULA_SESSION`, ID regen 5-min. SessionGuard fingerprint (IP+UA+Accept-Language+Accept-Encoding+salt) validated each request. CSRF: `$_SESSION['csrf_token']` + FormProtection HMAC-timing/honeypot/JS-challenge. **API auth = API keys** (`X-API-Key` header preferred, `?api_key=` fallback; `sk_live_…`); **JWT path is a non-functional stub** (issue #41). Browser session auth via `BaseController::requireAuth()` → 401.

### Test framework + run commands
PHPUnit 10.5.63. `phpunit.xml` two suites: **Unit Tests** (`_tests/Unit`, no DB) and **Integration Tests** (`_tests/Integration`, needs MySQL `signula_test`). 14 unit test files / **342 tests, 1105 assertions — all green** (2 benign warnings; "Decryption error" lines are negative-path test output, not failures). 6 integration test files (DB-gated). Commands: `php vendor/bin/phpunit --testsuite="Unit Tests"` (verified passing), `php vendor/bin/phpunit` (all), `composer test` / `test:unit` / `test:integration` / `test:coverage`.

### Lint / static-analysis config
- `phpstan.neon` — **level 6**, `bootstrapFiles: _tests/bootstrap.php`, **paths `private_html`/`public_html` (resolve from repo root → DO NOT EXIST; actual dirs are under `web/`)**. So `composer analyze` as documented **fails** (`Path …/private_html does not exist`). Uses deprecated `checkGenericClassInNonGenericObjectType`. See B-002.
- `phpcs.xml` — base **PSR-12** + many Generic/Squiz sniffs, 4-space exact indent, `DisallowTabIndent`, forbidden-functions (eval/var_dump/print_r/die/sizeof), cyclomatic complexity 15/30, nesting 5/10. Same path problem (`<file>private_html</file>`) → `composer check-style` from repo root fails.
- `composer.json` PSR-4 maps `SIGNula\\` → `private_html/` (also wrong relative to repo root; classes are require-based, not namespaced — phpstan can't discover them, see below).

### House-style conventions actually in force
- **DIRECTORY_SEPARATOR:** strong — 208/271 files use it; **0** `require`/`include` lines with a literal `/` path. ✅ compliant with brief.
- **SIGNULA_INIT guard:** 80/93 `private_html` files have it. The 13 without: the 4 layout templates + 4 i18n `lang/en/*` data files (low risk), but **5 real class files lack it**: `security/RateLimiter.php`, `api/APIKeyManager.php`, `api/APIKeyMiddleware.php`, `api/RateLimitMiddleware.php`, `email/EmailDripProcessor.php` (B-005).
- **`declare(strict_types=1)`:** **0/271 files** — the brief's spirit (PHP 8.4, strictness) and phpstan-strict-rules point to strict types, but none are declared. Large but mechanical gap (B-008).
- **Comments/emoji:** heavy emoji-annotated docblocks throughout (matches house style). Prepared statements everywhere via Database wrapper. Modular `require_once` includes with guard checks.

### Brief coverage
Brief-mandated capabilities that are MISSING or PARTIAL in the actual code (grep/file evidence):
1. **alpha_html / beta_html + access control — MISSING.** Brief explicitly asks for `beta_html`/`alpha_html` app folders with access control. `ALPHA_DIR`/`BETA_DIR` constants exist in `config.php` but **the directories do not exist**; only empty `public_html_dev/` (favicons) + `public_html_landing/` stubs. No gating code. (B-019 / FEATURES)
2. **JWT API authentication — PARTIAL/STUB.** Brief mandates a "FULLY SECURE" JSON API. `BaseController::getUserByToken()` is `// TODO: Implement JWT validation; return null;` and **no JWT library is vendored** (`vendor/firebase` absent). Bearer-token auth is non-functional; only API-key auth works. (issue #41 → B-006)
3. **OAuth providers — PARTIAL vs brief list.** 9 of 11 wired (Google, Microsoft, Apple, Facebook/Meta, LinkedIn, Yahoo, Amazon, WordPress, PayPal). **Missing: LastPass and a generic OpenID Connect provider** (brief listed both). Instagram is folded into Facebook/Meta, not separate. (FEATURES)
4. **Email-template *designer* (user/admin-designable) — PARTIAL.** EmailTemplateManager gives full server-side CRUD + versioning + preview over `tblEmailTemplates`, and an admin `email-dashboard.php` exists, but there is **no visual/WYSIWYG/drag-drop designer UI** (no grapesjs/unlayer/tinymce/quill found) — brief wanted users/admins to "design their own templates." (FEATURES)
5. **Crypto-payment support — PRESENT** (Coinbase + `tblProviderDiscounts` with crypto discount, `PartnerPaymentService::getProviderDiscount('crypto',…)`), and **GDPR tooling — PRESENT** (AccountManager export/erasure, tblDataExportRequests, settings/privacy.php). **Apple Pay / Google Pay — PARTIAL** (settings + Stripe/PayPal routing only, no native sheets). Contact form, support/lockout/invitation **email notifications — PARTIAL** (TODO stubs; see Backlog). Biometrics/WebAuthn/passkeys — PRESENT.

### Issue reconciliation
All 40 open GitHub issues confirmed via `gh issue list` (numbers/titles match exactly).

| #NN | title | class | maps-to | note |
|-----|-------|-------|---------|------|
| #85 | Set Up Production Cron Jobs | OPERATIONAL | surface-to-user | needs prod host/crontab; `web/public_html/cron/` scripts exist |
| #84 | Configure Email Service Provider | OPERATIONAL | surface-to-user | 7 providers coded; needs prod creds in tblSettings |
| #83 | Configure Production Database Credentials | OPERATIONAL | surface-to-user | prod secret; user-only |
| #81 | Legal & Compliance Documentation Review | MANUAL-QA | surface-to-user | legal pages exist under SIGNula.com/legal; needs human/legal review |
| #80 | Complete API Documentation (OpenAPI/Swagger) | FEATURE | FEATURES | `api/docs/` present but no OpenAPI spec found |
| #79 | Finalize Production .htaccess Configuration | OPERATIONAL | surface-to-user | HTTPS/www rules commented out in `.htaccess` |
| #78 | Automated Backups & Disaster Recovery | OPERATIONAL | surface-to-user | host-level |
| #77 | Monitoring & Observability | OPERATIONAL | surface-to-user | host-level |
| #76 | Accessibility WCAG AA Audit | MANUAL-QA | surface-to-user | needs assistive-tech testing |
| #75 | Browser & Device Compatibility Testing | MANUAL-QA | surface-to-user | human cross-device |
| #74 | Performance & Load Testing | MANUAL-QA | surface-to-user | needs env + load tooling |
| #73 | Security Penetration Testing & OWASP Audit | SECURITY-DEFERRED | SECURE | full pentest = SECURE phase |
| #72 | E2E Manual Testing (300+ cases) | MANUAL-QA | surface-to-user | human QA |
| #71 | Deploy to Staging Environment | OPERATIONAL | surface-to-user | needs staging host |
| #70 | Configure Live Payment Provider Credentials | OPERATIONAL | surface-to-user | prod secrets |
| #69 | Execute Full Automated Test Suite (342 tests) | MANUAL-QA | surface-to-user | **already runs green locally (unit)**; integration needs DB |
| #67 | Build subscription & recurring billing module | FEATURE | FEATURES | much exists (PaymentManager/BillingScheduler/UsageBilling) — verify scope vs gap |
| #66 | iLyricsDB: checkout redirect & subscription portal | FEATURE | FEATURES | external-integration feature |
| #65 | iLyricsDB: product & tier configuration | FEATURE | FEATURES | external-integration feature |
| #64 | iLyricsDB: subscription & billing webhook events | FEATURE | FEATURES | external-integration feature |
| #41 | TODO: JWT validation in API BaseController | code | **B-006** | stub returns null; no JWT lib vendored |
| #40 | TODO: Replace placeholder payment redirect URLs | code | **B-007** | `partners/api/credit-actions.php:784` |
| #39 | TODO: Authenticated header for support ticket pages | code | **B-009** | `SIGNula.id/support/ticket.php:140` |
| #38 | TODO: Save contact form submissions to DB | code | **B-004** | `SIGNula.com/contact.php:81` — **tblContactSubmissions already exists** (mig 004); just wire it |
| #37 | TODO: Email verification for email changes | code | **B-003** | `account.php:83`, `settings/profile.php:232` |
| #36 | TODO: Email notifications (invite/lockout/support) | code | **B-010** | Auth.php:573, Organization.php:300, ticket.php:106, team-actions.php:173, members.php:188 |
| #35 | Inconsistent error response keys across API | code | **B-011** | mixed `'error'`(63)/`'message'`(30)/`'errors'`(3) keys |
| #34 | SMTP AUTH without encryption check | SECURITY-DEFERRED | SECURE | SMTPEmailProvider |
| #33 | Information disclosure via X-Mailer header | SECURITY-DEFERRED | SECURE | EmailTemplateBuilder.php:563 sets it |
| #32 | AMP component names not validated vs allowlist | SECURITY-DEFERRED | SECURE | EmailTemplateBuilder AMP path |
| #31 | $customCSS injection risk in AMP templates | SECURITY-DEFERRED | SECURE | EmailTemplateBuilder |
| #30 | Debug mode logs SMTP credentials plaintext | SECURITY-DEFERRED | SECURE | debug logging |
| #29 | Missing X-Frame-Options: DENY on API responses | SECURITY-DEFERRED | SECURE | confirmed absent in Response/api index |
| #28 | CSRF not checked on GET-based admin actions | SECURITY-DEFERRED | SECURE | `admin/api/*-actions.php` |
| #27 | URL reset tokens not urlencode()'d in email links | SECURITY-DEFERRED | SECURE | token in mail links |
| #26 | No email validation in queueEmail() | SECURITY-DEFERRED | SECURE/B-012 | EmailService.php:112 |
| #25 | Missing composite indexes on credential reset tables | code/perf | **B-013** | only single-col indexes in mig 017 |
| #24 | No pagination bounds on credential reset history | code/robustness | **B-014** | CredentialResetService LIMIT/OFFSET unbounded |
| #23 | Unsanitized reason text in credential reset ops | SECURITY-DEFERRED | SECURE | free-text reason field |
| #22 | Missing adminUserID validation in credential reset API | SECURITY-DEFERRED | SECURE | user-actions.php reset endpoints |

**Reconciliation counts:** code-actionable B- = **13** (#41,#40,#39,#38,#37,#36,#35,#26[also security],#25,#24) covering B-003,004,006,007,009,010,011,012,013,014; OPERATIONAL = **11** (#85,84,83,79,78,77,71,70 + 83 host); MANUAL-QA = **7** (#81,76,75,74,72,69); FEATURE = **6** (#80,67,66,65,64 + OpenAPI); SECURITY-DEFERRED = **11** (#73,34,33,32,31,30,29,28,27,23,22).

## Backlog

Syntax sweep result: **`php -l` over all 271 `web/` PHP files + `_scripts`/`_tests`/root → ZERO parse errors.** No B-items for syntax. PHPStan level-6 = 4695 errors but the majority are **symbol-discovery artifacts** (1133 "static method on unknown class Database", 415 method-on-unknown-class, 137 instantiated-class-not-found, 124 constant-not-found, 68 function-not-found) caused by phpstan not loading the require-based classes — config issue (B-002), not real bugs. Genuine-signal categories: `empty()` not allowed (1148) + short-ternary (116) + "only booleans allowed" (~400) = strict-rules style (B-016); **"Function invoked with N parameters, N required" (322)** + **"Variable might not be defined" (48)** = real correctness risks (B-015); 151 params with no type (B-008). PHPCS = **4884 errors + 281 warnings / 258 files, 4649 auto-fixable** — top sniffs: scope-indent (1901), space-after-cast (832), control-structure spacing (520/383/168), file-header spacing (257) — almost all whitespace/auto-fixable (B-017); real-signal: cyclomatic complexity >15 (85/17), forbidden functions (113), side-effects-in-class-file (134).

`B-NNN | title | type | impact | effort | blast | gh`

- B-001 | Fix phpstan/phpcs runnability — `composer analyze`/`check-style` error out (paths `private_html` unresolved from repo root) | code-quality | High | S | (phpstan.neon, phpcs.xml; behaviour-touching? no) | —
- B-002 | phpstan can't discover require-based classes (4150+ "unknown class/function" false errors) — needs bootstrap/scan config so real bugs aren't drowned | code-quality | High | M | (phpstan.neon, _tests/bootstrap.php; no) | —
- B-003 | Email verification not sent/enforced on email-address change (account.php:83, settings/profile.php:232) | correctness | High | M | (account.php, profile.php, EmailService, tblEmailVerificationTokens; yes) | gh:#37
- B-004 | Contact form silently discards submissions — not persisted/notified despite tblContactSubmissions existing | correctness | High | S | (SIGNula.com/contact.php; yes) | gh:#38
- B-006 | JWT bearer-auth is a stub returning null — API "FULLY SECURE" brief unmet; no JWT lib vendored | correctness | High | M | (BaseController.php; self-hosted JWT lib; yes) | gh:#41
- B-007 | Placeholder payment redirect URLs in partner credit top-up (credit-actions.php:784) — checkout dead-ends | correctness | High | M | (partners/api/credit-actions.php, payment providers; yes) | gh:#40
- B-010 | Missing email notifications: lockout (Auth.php:573), org invite (Organization.php:300), support ticket (ticket.php:106), team invite (team-actions.php:173), member resend (members.php:188) | correctness | High | M | (5 files + EmailService; yes) | gh:#36
- B-015 | phpstan "function invoked with wrong arg count" (322) + "variable might not be defined" (48) — real call-site/uninitialised-var bugs to triage | correctness | High | L | (many files; yes) | —
- B-005 | 5 real class files missing SIGNULA_INIT guard (RateLimiter, APIKeyManager, APIKeyMiddleware, RateLimitMiddleware, EmailDripProcessor) — direct-access exposure | security | High | S | (5 files; no) | —
- B-009 | Support ticket pages use unauthenticated header (ticket.php:140) — wrong/leaky chrome for logged-in users | robustness | Med | S | (SIGNula.id/support/ticket.php; yes) | gh:#39
- B-011 | Inconsistent API error response keys (`error` vs `message` vs `errors`) — breaks client error handling | robustness | High | M | (api/controllers/*, Response.php, admin/api/*; yes) | gh:#35
- B-012 | queueEmail() does not validate recipient email — malformed addresses enter queue | robustness | Med | S | (EmailService.php:112; yes) | gh:#26
- B-013 | Missing composite indexes on credential-reset tables — slow scans at scale | perf | Med | S | (mig 017 / new migration; no) | gh:#25
- B-014 | Unbounded pagination on credential-reset history queries — potential large result/DoS | robustness | Med | S | (admin/CredentialResetService.php; yes) | gh:#24
- B-016 | ~1660 strict-rules style hits (`empty()` 1148, short-ternary 116, non-bool conditions ~400) — adopt strict comparisons | code-quality | Med | L | (many; mostly no) | —
- B-017 | 4649 auto-fixable PHPCS whitespace violations (scope indent 1901, space-after-cast 832, control-spacing) — run phpcbf | code-quality | Med | M | (258 files; no) | —
- B-008 | No `declare(strict_types=1)` in any of 271 files; 151 params untyped — house-style/strictness gap | code-quality | Med | L | (all files; no) | —
- B-018 | Cyclomatic complexity >15 in 85 functions (17 over absolute 30) — refactor hotspots | code-quality | Low | L | (85 functions; no) | —
- B-019 | alpha_html/beta_html dirs + access control absent (constants defined, no dirs/gating) — brief-mandated staging gating missing | ux | Med | M | (config.php, new dirs/middleware; yes) | —
- B-020 | Test coverage gaps for core flows: no tests for Auth, Database, all payment providers (Stripe/PayPal/Coinbase), EmailService, WebAuthnHandler, AccountManager, Organization, UsageTracker/UsageBillingService, ExportService | test | High | L | (new _tests/*; no) | —
- B-021 | VERSION (2.6.0-beta) disagrees with PROJECT_PROGRESS/docs (2.7.0-beta); progress doc over-claims "100% complete" vs TODO stubs | code-quality | Low | S | (VERSION, PROJECT_PROGRESS.md; no) | —
- B-022 | OpenAPI/Swagger spec missing despite `api/docs/` — API not machine-documented | code-quality | Med | M | (public_html/api/docs; no) | gh:#80
- B-023 | Side-effects in 134 class files & 113 forbidden-function uses flagged by PHPCS (likely die()/print_r in scripts) — triage non-test ones | code-quality | Low | M | (flagged files; no) | —
- B-024 | CORS misconfig: `Response::setCorsHeaders()` sends `Access-Control-Allow-Origin: *` WITH `Allow-Credentials: true` — invalid + insecure for credentialed API/JWT requests; tighten to a configurable origin allowlist | security | High | S | (api/Response.php; yes) | — (found in G-003 spec)
- B-025 | `tblBillingSchedule` schema↔code drift: mig 012 PK/columns/ENUM (`scheduleID`,`completedAt`,`errorMessage`) don't match scheduler reads/writes (`taskID`,`result`,`nextRetryAt` + task types `suspend_account`/`calculate_usage`/`charge_usage`/`archive_usage`) — errors on clean install | correctness | High | S | (mig 012 + new mig, BillingScheduler.php; yes) | gh:#67 (found in G-002 spec; fix as G-002 build-stage 0)
- B-026 | Stale `SessionManager::isLoggedIn` references in 3 API docblock comments (no such class; sessions handled by Auth/SessionGuard) — cosmetic comment cleanup, NOT a runtime bug (verified: 0 executable usages) | code-quality | Low | S | (invoice-actions.php, usage-actions.php, tier-actions.php docblocks; no) | — (phpstan-surfaced cycle 3)
- ✅ **B-027 DONE (c5)** — broader than scoped: `Database::insert()`/`insertId()` were called by **7 files** (WebAuthnHandler, PasswordlessLoginHandler, AccountManager, EmailScheduler/ABTesting/DripCampaign/TemplateManager) and never existed → all fataled. Added the 2 helper methods to database.php; all unbroken. ORIG: `Database::insert()` called by `WebAuthnHandler::storeChallenge()`/`storeCredential()` (lines 420,538) but NO such method exists on Database (no `insert()`, no `__callStatic`) → WebAuthn registration + challenge storage THROW `Error` and are non-functional | correctness | High | S | (WebAuthnHandler.php; maybe add Database::insert helper; yes) | — (PoC-pinned cycle 4; SAFETY FLOOR) |
- ✅ **B-028 DONE (c5)** — premise INVERTED: canonical is lowercase `'active'` (the ENUM + all app code); BaseController was already correct. Real drift was in test fixtures → aligned to lowercase. ORIG (as mis-stated): `BaseController::getUserById()/getUserByApiKey()` filter `accountStatus='active'` (lowercase)
- B-029 | Integration suite ~63 errors when run against a real MySQL DB (unit suite stubs DB so they were invisible): AuthLogin, MFA, RateLimiter, SecurityMiddleware incl. a `RateLimiter::checkLimit()` static-call mismatch (same B-027 "missing/mismatched method" class). Needs: stand up signula_test DB, run integration suite, TRIAGE real-bugs vs schema/harness drift, fix the real ones. BLOCKS "core task works e2e" | correctness | High | L | (many; yes) | — (surfaced cycle 6) |
- B-030 | WebAuthn challenge-consumption TOCTOU race: `verifyChallenge` (SELECT isUsed=0) and `markChallengeUsed` (UPDATE) are non-atomic with the sig-check between → a genuine freshly-signed assertion can authenticate twice under concurrency (PoC). Fix: atomic `UPDATE…SET isUsed=1 WHERE challenge=? AND isUsed=0` + affected_rows===1 before granting; same for sign-count `WHERE signCount<?` | security | Med | S | (WebAuthnHandler.php; yes) | — (red-team cycle 6) |
- B-031 | `WebAuthnHandler::verifyChallenge` uses `if ($userID)` (falsy for userID=0) — harden to `!== null`. Not reachable today (userID≥1) | security | Low | S | (WebAuthnHandler.php; yes) | — (red-team cycle 6) |
- B-032 | Usernameless/discoverable-credential WebAuthn login fails closed: `generateAuthenticationOptions(null)` stores userID=NULL challenge but verify requires the credential owner's userID → valid usernameless assertion rejected (availability, fail-safe) | correctness | Med | M | (WebAuthnHandler.php; yes) | — (red-team cycle 6) |
- B-033 | Consolidated installer `signula_complete_install_v2.5.0.sql` FAILS a fresh install (~71 errors): pervasive INT vs BIGINT FK type drift (userID/partnerID/createdBy), two conflicting tblSettings/tblMigrations schemas, double-defined WebAuthn tables, FK to never-created tblEmailQueue. **Blocks "builds & runs from clean checkout".** Needs regenerated/validated installer (reconcile source migrations first) | correctness | High | L | (installer + many migrations; yes) | gh:#83-adjacent (found cycle 7) |
- B-034 | Multi-org feature half-present: Auth.php/Organization.php reference tblOrganizations/tblOrganizationDomains/tblOrganizationMembers but NO migration creates them → runtime errors on org flows. (mig 025 added only tblUsers.organizationID to unblock registration) | correctness | High | M | (new migration + verify Organization.php; yes) | — (found cycle 7) |
- B-035 | tblUserMFA legacy (`isEnabled`/`totpSecret`) vs new (`mfaEnabled`/`mfaSecret`/`isVerified`) columns coexist after mig 026 (additive) — consolidation (rename+data copy) is a data-migration decision | code-quality | Low | M | (migration; yes-data) | — (cycle 7) |
- B-036 | Source-migration FK type drift INT vs BIGINT across files (e.g. mig 001 tblEmailUnsubscribes.userID INT → tblUsers BIGINT; mig 005 WebAuthn FKs) — widen to BIGINT (non-destructive) in a focused pass | code-quality | Med | M | (many migrations; no) | — (cycle 7; relates B-033) |
- B-037 | `ActivityLoggerTest` intermittent failure (~1 in 3 full Integration runs; green on re-run/isolation) — test-isolation / transaction / order dependency in ActivityLogger::log path; undermines Integration suite as a reliable gate | test | Med | M | (ActivityLoggerTest, DatabaseTestCase, maybe ActivityLogger; yes) | — (confirmed cycle 7) |
- B-038 | Duplicate migration numbers 003–006 (003_email_drip vs 003_oauth, etc.) — ambiguous apply order; non-standalone (001 starts with ALTER). Renumber/reconcile for deterministic fresh installs | code-quality | Low | M | (migrations dir; no) | — (cycle 7; relates B-033) |
- ✅ **DONE cycle 8**: B-024, B-030, B-031, B-012, and GitHub #22–#34 (11 MEDIUMs) addressed-on-branch; `session_start` unit-test flake fixed → suite 407/0-warnings deterministic.
- B-039 | 4 token-URL builders don't `urlencode()` the token (open-redirect/param-hygiene): `UserController.php:536` (verify-email), `AuthController.php:105/399` (verify-email/reset-password), `CredentialResetService.php:643` (reset-password). Email/-scope sites already fixed cycle 8; these controller/service ones remain | security | Med | S | (4 files; yes) | — (cycle 8) |
- ✅ **B-033 DONE cycle 9** (consolidated-installer + migrations path: fresh install 74→0 errors, verified 74 tables); installer now tracked. Residual wizard path = B-041.
- ✅ **B-040 DONE cycle 10**: root cause was a systemic `Database::getAffectedRows()` returning -1 for prepared UPDATEs (read after `$stmt->close()`); fixed in database.php (capture while open) → WebAuthn 8/8 + Integration 71/0 + 12 other callers repaired; FG-013/B-030 re-verified closed.
- B-043 | `ActivityLogger`/`RateLimiter` emit non-fatal "Data truncated for column 'activityCategory'/'identifierType'" during integration (ENUM too narrow for the values written) — swallowed, fails no test; **very likely the source of the B-037 flake** (which now trips ~2/3 of full runs). Widen the ENUM/column (additive migration) + confirm it makes integration deterministic | correctness | **High (was Low — gate reliability)** | S | (ActivityLogger/RateLimiter + a migration; yes) | — (cycle 10; escalated cycle 11) |
- ✅ **B-041 DONE cycle 11** (wizard idempotency: seed 001-013 baked filenames; setup_test_db.sh loop 014; CLI/documented-load install = 0 errors). ✅ **B-034 DONE cycle 11** (migration 028: 6 org tables).
- ✅ **B-043/B-037 DONE cycle 12** (ENUM truncation + test-hermeticity — Integration now 5/5+20/20 green, gate reliable). ✅ **B-039 DONE cycle 12** (rawurlencode 4 token-URL sites).
- B-045 | Running a SUBSET of integration test files (not the full suite) surfaces `Class "EmailService"/"MFA" not found` in `AuthLoginTest` register tests — a cross-file `requireSource` class-loading dependency; absent in the full suite. Make per-file runs self-loading | test | Low | S | (AuthLoginTest/bootstrap; no) | — (cycle 12) |
- ✅ **B-046 DONE cycle 15**: removed errant `USE signula;` from migration 030 (mis-targeted non-`signula` DBs in the wizard/runner) + setup_test_db.sh strips any stray `USE`.
- B-047 | `SecurityAlertManager::create()` passes the alert severity (`'high'`/`'critical'`) into `ActivityLogger::log()`'s `severity` ENUM which only allows `info/notice/warning/error/critical` → `'high'` truncates (swallowed; same class as B-043). Map alert severities → activity-log severities | correctness | Low | S | (SecurityAlertManager; yes) | — (cycle 15) |
- ✅ **B-048 DONE cycle 16** (HIGH, was hidden): `Router::executeHandler()` never called `setRouter()` on the controller → every controller reached via HTTP dispatch fataled on `$this->router` uninitialised — the REST API's controller routing had NEVER worked end-to-end (only via test reflection). Fixed. Also fixed `APIKeyMiddleware` 401'ing Bearer JWTs.
- ✅ **B-049 DONE cycle 17** (red-team-confirmed spray-able → fixed to a windowed SUM in `SecurityUtils::checkRateLimit`; paced spray now hits 429).
- B-050 | `BaseController::getUserByApiKey()` JOINs `k.userID = u.userID` but `tblAPIKeys` has NO `userID` column (keys are partner-scoped: `partnerID`) → returns nothing even after the cycle-17 `keyHash` fix. Dead/fails-closed (not on the live API-key path — the middleware is). Decide user- vs partner-scoped resolution OR remove the method | code-quality | Low | S | (BaseController.php; yes) | — (cycle 17) |
- ✅ **B-051 DONE cycle 22** (HIGH): created `web/_backend/SessionManager.php` — a facade with 3 **static** methods (`isLoggedIn`→`Auth::isAuthenticated`, `getUserID`→`Auth::getCurrentUserID`, `getUserInfo`→`Auth::getCurrentUser`; static so both `SessionManager::isLoggedIn()` and `$sm->isLoggedIn()` call sites work), constructor takes+ignores the `$db` arg all 72 sites pass. Fixed `AccessControl::loadPartnerMemberships()` SELECT: `p.partnerName AS companyName, p.accountTier AS tier, p.isActive AS partnerStatus` (real columns, aliased to keep consumers unchanged). Unit 466→473, integration 110→113 green. **⚠️ but the 74 pages STILL don't load e2e** — they fatal one require later on **B-054** (`_backend/Database.php` also never committed) and are mis-authorized by **B-053** (admin flags absent from the user array).
- ✅ **B-052 DONE cycle 22** (HIGH): `config.php::loadSettings()` now wraps the per-row `SecurityUtils::decrypt()` in its own try/catch — a blank/undecryptable `isSensitive` row is logged and skipped (value→`''`) and the loop **continues**; outer try (DB-unavailable) unchanged. Empirically: buggy code loaded 10/369 settings on a fresh install, fixed code loads all 369. Integration test proves a blank sensitive row no longer blanks the rest.
- B-055 | **Login silently never creates a session** (HIGH, core-auth — violates definition-of-done "core auth flow works e2e"). `Auth::completeLogin()` binds the `tblUserSessions` INSERT with type string `'sissssssssii'` — the final `i` binds `$expiresAt` (a `Y-m-d H:i:s` **string**) as an integer → mysqli coerces it to `2026` → on a strict-mode MySQL (Dreamhost/prod default) the INSERT throws "Incorrect datetime value" → `completeLogin()` returns `false`, but `login()` (line 397) **ignores the return** and still reports `success:true`. Fix: type string `…ii`→`…is` (1 char); THEN verify a session row is actually created with a valid `expiresAt` and that `login()` surfaces a `completeLogin()` failure instead of a false success | correctness/security | **High** | S | (web/private_html/auth/Auth.php; yes) | ✅ **DONE cycle 23**: bind `'sissssssssii'`→`'sissssssssis'`; `login()` now returns `success:false` when `completeLogin()` fails; ALSO guarded the 3 other callers via `loginOAuth()` (oauth/callback.php ×3 + mfa/verify.php ×1 — they redirected to /dashboard on a phantom login). **Root-cause note: `database.php::connect()` forces `sql_mode=STRICT_ALL_TABLES` on every connection, so this broke EVERY login (not just prod) — the old `AuthLoginTest` passed only because it asserted the phantom `success===true` without checking a session row existed.** New integration test asserts a real `tblUserSessions` row with a correct future `expiresAt` + failure-propagation; conductor independently proved it fails on the pre-fix bind. Unit 473 + integration 113→**115** green |
- ✅ **B-054 DONE cycle 24** (HIGH): added `Database::getInstance()` to the existing static `Database` class (`web/_config/database.php`) returning a new `DatabaseConnection` adapter (proxies `prepare`/`query`/`begin_transaction`/`commit`/`rollback`/`ping` via `__call`, `insert_id`/`error` via `__get`, real `getConnection():mysqli`) — resolves the 262 `$db->prepare` + 79 `getInstance()` call sites without a redeclare. Created `web/_backend/Database.php` as a collision-free shim (guarded `require_once` of `_config/database.php`, declares no class). Independently smoke-verified (getInstance→DatabaseConnection, prepare round-trip, insert_id via `__get`) + sample pages load past both requires.
- ✅ **B-056 DONE cycle 24** (HIGH, surfaced during B-054): FOUR more never-committed `_backend/*` shims the 74 pages require (same class as B-051/B-054) — `ActivityLogger.php` (10 dependents; real class `private_html/utils/`), `APIKeyManager.php` (1; `private_html/api/`), `RateLimiter.php` (1; `private_html/security/`), `ServiceFeeManager.php` (1; `private_html/payments/`). Created all 4 as guarded require-shims. Now ALL 7 `_backend/*` require targets resolve → the admin/partner UI finally loads past every require (conductor-verified: all 6 `_backend` files present, shims declare no class, full bootstrap chain loads with no fatal). Unit 481→**489**, integration 120→**125** green. **Suggestion (low, for-consideration): add a CI/audit grep of `require .*_backend/<X>.php` targets vs files present in `web/_backend/` to catch the next missing shim before prod.**
- ✅ **B-053 DONE cycle 25** (HIGH — LAST safety-floor item): `Auth::getCurrentUser()` now enriches the tblUsers row with the 4 admin/partner-context keys the 50 sites read. Source of truth (confirmed): `isSuperAdmin` = REAL tblUsers column (migration 009, cast to bool) added to the SELECT; `partnerID` = the user's PRIMARY active `tblPartnerTeamMembers` membership (root-admin-preferred, else lowest partnerID; null if none) via one extra prepared query; `isPartnerAdmin` = has-any-active-membership; `isAdmin` = `isSuperAdmin || has-membership` (mirrors `AccessControl::isAdmin`). ADDITIVE (no existing key/consumer touched); null-safe; enriched array is what's cached in `self::$currentUser`. Deliberately did NOT wire in AccessControl (avoids an Auth↔SessionManager↔AccessControl load cycle). Builder also found migration 009's `ADD COLUMN isSuperAdmin AFTER isAdmin` references a non-existent `isAdmin` column (installer drops the AFTER clause) → confirms `isAdmin` is correctly derived-only. 4 new integration tests (super-admin / partner-member / multi-membership ordering / plain user + cache-consistency). Unit 489, integration 125→**129** green. **→ SAFETY FLOOR CLOSED: admin/partner UI + login now work e2e (B-051+B-052+B-055+B-054+B-056+B-053).**
- B-057 | **Open redirect on the MFA login path** (MED, surfaced cycle 26 during A2). `Auth::login()` (`web/private_html/auth/Auth.php:377`) stores `$_SESSION['redirect_after_login'] = $_GET['redirect'] ?? 'dashboard.php'` WITHOUT `sanitizeRedirectUrl()`, and `web/public_html/mfa/verify.php:99-102` reads it and `redirect($redirect)` unsanitized → an attacker `?redirect=https://evil.com` survives to a post-MFA redirect (MFA-enabled users only). The NON-MFA path (`login.php:20,75`) DOES sanitize — so only the MFA branch is exposed. Fix: run `redirect_after_login` through `sanitizeRedirectUrl()` at set-time (Auth.php:377) and/or consume-time (mfa/verify.php) — allowlist relative paths only. Verify `redirect()` doesn't already guard. Consider folding into G-001 A5 hardening (open-redirect is in that red-team's scope) | security/correctness | **Med** | S | (Auth.php + mfa/verify.php; yes) | ✅ **DONE cycle 29** — `redirect_after_login` now `sanitizeRedirectUrl()`-guarded at set-time (Auth.php) + consume-time (mfa/verify.php), with `is_scalar()` guards against a `redirect[]=` TypeError |
- B-058 | **`TokenService::refresh()` does not bind a refresh token to a client** (HIGH, surfaced cycle 27 during A3). RP refresh tokens now carry `clientID` (via A3's `issueForClient`), but `refresh()` doesn't verify the presenter is that client → once the OAuth `refresh_token` grant is exposed on `/oauth/token`, Client B (with its own valid creds) could redeem Client A's refresh token; and the existing `/api/v1/auth/refresh` (JwtAuthController) rotates an RP refresh token WITHOUT client auth. **Because of this, A3 deliberately did NOT wire the `refresh_token` grant into `/oauth/token`** (authorization_code only). Fix before shipping the refresh grant: `refresh()` must require + verify client auth when the row's `clientID` is non-null (confidential: verifyClientSecret; public: same token-binding), and refuse cross-client redemption. Also: refresh tokens are currently always minted (not gated behind `offline_access`) — add a mint-skip lever to TokenService. Do in A4/A5 | security | **High** | M | (TokenService.php + JwtAuthController + /oauth/token; yes) | ✅ **DONE cycle 29** — `refresh(refreshToken, ?authenticatedClientID)` now enforces `$authenticatedClientID !== $clientID` BEFORE spend (covers cross-client, RP-token-at-first-party-endpoint, no-auth; security alert, no family-revoke on a pure authz mismatch); refresh minting gated behind `offline_access` (mintPair `withRefresh` lever); the `refresh_token` grant wired into /oauth/token (client-bound) + advertised in discovery. First-party /api/v1/auth/refresh now correctly rejects RP-bound tokens |
- B-059 | **`/oauth/token` has no rate limiting** (Low/Med). Unlike `/api/v1/auth/token` (JwtAuthController uses RateLimiter), the new `web/public_html/oauth/token.php` is a public endpoint with no throttle → code-guessing / client-secret-spray surface. Add RateLimiter (per-IP + per-client) — fold into A5 hardening | security | **Low** | S | (oauth/token.php / OAuthTokenService; yes) | ✅ **DONE cycle 29** — `SecurityUtils::checkRateLimit` per-IP + per-client on /oauth/token + /oauth/revoke (429 + Retry-After), reusing `jwt.rate_limit.*` settings under new buckets |
- B-060 | **The entire user account-management UI (`settings/`) fatals on load** (HIGH, surfaced cycle 28 — same 'hidden broken' class as B-051/B-054). **13** pages under `web/public_html/settings/` (profile, mfa, passkeys, privacy, notifications, connected-accounts, email-accounts, activity, …) call `requireLogin()` (NEVER defined anywhere) and `include SIGNULA_ROOT . '…/header.php'`/`footer.php` where **`SIGNULA_ROOT` is never `define()`d** AND those `web/private_html/layout/header.php`/`footer.php` files **don't exist** → every one of these 13 pages is a fatal on load. This directly breaks the owner's requirement to "change/reset email/passwords, enable/disable MFA, manage passkeys within SIGNula's web interface". The cycle-28 `connected-apps.php` deliberately used the working `Auth::` gate + `ROOT_DIR` + `settings-sidebar.php` instead of the broken pattern — copy THAT. Fix: define/require a real `requireLogin()` (or migrate these to `Auth::requireAuth`), define `SIGNULA_ROOT` (or use `ROOT_DIR`), and provide the missing header/footer (or the self-contained shell). Verify each of the 13 loads with no fatal | correctness | **High** | M | (13 settings/ pages + layout; yes) | — (found cycle 28, code-confirmed) |
- B-044 | Install WIZARD `install/index.php` executes SQL via `mysqli::multi_query()` which can't parse the CLI-only `DELIMITER` directive → the installer's stored-proc/event blocks + migration 018_usage_billing fail via the wizard (base install: ~18/74 tables, 0 procedures). The CLI/phpMyAdmin `mysql < file.sql` load path is clean. Fix: a DELIMITER-aware SQL splitter in the wizard OR remove stored procedures/events from the installer + mig 018 | correctness | High | M | (install/index.php or installer+mig 018; yes) | gh:#83-adjacent (cycle 11) |
- B-041 | Install WIZARD path `install/index.php` still errors ~44 on a fresh run — it seeds `tblMigrations` with only 3 mismatched names so migrations 001-014 re-run and hit idempotency errors. Fix: fully idempotency-guard 001-014 OR have the wizard mark baked migrations applied. (Consolidated-installer path is 0 errors; this is the click-through path) | correctness | High | M | (install/index.php + migs 001-014; yes) | gh:#83-adjacent (cycle 9) |
- B-042 | `_scripts/build-complete-install.sh` generator is STALE (old v2.0.1 base, only bundles migs 001-013, has a `sed` bug that produced duplicate-INSERT corruption) — can't regenerate the current installer. Fix or retire it; the tracked installer is now canonical | code-quality | Low | M | (build-complete-install.sh; no) | — (cycle 9) | while app/fixtures use `'Active'` — API auth path would fail to resolve users on a case-sensitive (`_bin`) collation | robustness | Low | S | (BaseController.php; yes) | — (found cycle 4) |

## Run record
Bootstrap DISCOVER — codebase audit complete 2026-06-30
- cycle 3 STABILIZE-1 (B-001/B-002/B-005): `composer analyze`/`check-style` now RUN (paths fixed, `scanDirectories` added, `--memory-limit=1G`); unknown-class false-positives 647→74 (74 genuine: SessionManager comment-refs, uninstalled TCPDF); **real phpstan baseline ≈3632 errors** (322 wrong-arg-count + ~1148 strict `empty()` + missing helpers `requireLogin`/`timeAgo`/`isLoggedIn` — fodder for B-015/B-016); 5 SIGNULA_INIT guards added; 342 tests still green.

## Proposed-Features ledger
see FEATURES.md

## Trajectory ledger
| cycle | phase | move | result | commit |
|-------|-------|------|--------|--------|
| 1 | DISCOVER | bootstrap: codebase map + 23-item backlog + 17 feature gaps + 40-issue reconciliation | docs written; 0 parse errors, 342 tests green; tooling-config bug found (B-001) | b6128b4 |
| 2 | COMPLETE-prep | author full specs for user-approved G-001/G-003/G-002/G-004 | 4 build-ready specs written; surfaced B-024 (CORS) + B-025 (billing schema drift) + suggested FG-018 (SCIM) | 39b0bcd |
| 3 | STABILIZE | B-001/B-002/B-005: fix phpstan/phpcs runnability + class discovery + 5 SIGNULA_INIT guards | tools run; unknown-class FPs 647→74; phpstan baseline ≈3632 real errors; 342 tests green; no behaviour change | e5376fd |
| 4 | STABILIZE | B-020 safety-net: +48 characterization tests for API-auth/Auth/WebAuthn/Email seams | 342→390 tests green; surfaced HIGH B-027 (WebAuthn registration broken) + FG-013 (WebAuthn auth-bypass, PoC) + pinned B-024/B-012/B-011/B-028 | 850a9b8 |
| 5 | STABILIZE | B-027 + B-028: add `Database::insert()`/`insertId()` (missing, fataling 7 callers); align fixtures to canonical lowercase accountStatus | 390 green; UNBROKE PasswordlessLogin + 4 email features + AccountManager + WebAuthn storage with one helper; BaseController already correct (premise inverted) | 4387288 |
| 6 | SECURE(safety-floor) | FG-013: implement real WebAuthn verification (minimal CBOR/COSE→PEM, openssl_verify, sign-count clone-detect) + 2 pre-existing bug fixes (storeChallenge bind-type, ActivityLogger::log signature) | 401 green (+11 crypto tests); RED-TEAM verdict HOLDS across 7 vectors; auth-bypass closed. Residuals → B-030/B-031/B-032. Builder's DB run exposed ~63 integration errors → B-029 | a54aa03 |
| 7 | STABILIZE | B-029: stand up signula_test DB (new `_scripts/setup_test_db.sh`) + triage Integration suite | Integration 39err/14fail → green on re-run (B-037 flake remains); FIXED 3 systemic prod bugs — ErrorLogger::log (61 sites), EmailService bind→queueEmail() failing every call, MFA `backup_code` ENUM; +2 additive migrations (025/026) unblock registration (missing organizationID) + MFA columns. Residuals B-033..B-038 | 1a16b29 |
| 8 | SECURE | 3 parallel workstreams: B-024 CORS allowlist + #29 X-Frame/CSP, B-030 WebAuthn TOCTOU atomic + B-031, 13 deferred MEDIUMs (#22-#34: email validation/AMP allowlist/customCSS/X-Mailer/SMTP-enc/redact-creds/urlencode + admin CSRF/adminUserID/reason/pagination) + mig 027 indexes; + fixed the session_start test flake | 407 green, **0 warnings** (was 2; 30+ runs deterministic); found+fixed a 2nd CSRF hole (rate-limits unblock); B-039 (4 token-URL urlencode sites) + tblMigrations drift → B-033. Integration re-confirm deferred (MySQL restart flaky) | 7d07045 |
| 9 | STABILIZE | B-033: fresh install 74 errors → **0** (dedup INSERTs, INT→BIGINT FK widen 96 cols, tblMigrations/tblSettings column-drift remap, dedupe WebAuthn tables, restore missing FK-target tables, idempotent seeds) + un-ignore the installer (was excluded from clean checkout) | fresh install 0 errors/74 tables (independently verified); unit 407 green; 27 migrations+installer+setup_test_db reconciled (non-destructive, no prod DB). Surfaced B-040 (cycle-8 B-030 broke 4 WebAuthn integration tests — HIGH), B-041 (install-wizard path still 44 idempotency errors) | 3a4e3ea |
| 10 | STABILIZE | B-040: the 4 WebAuthn integration failures = cycle-8 B-030 exposed a SYSTEMIC bug — `Database::getAffectedRows()` returned **-1** for every prepared UPDATE (read `affected_rows` AFTER `$stmt->close()`). One-file fix in database.php (capture while stmt open) | WebAuthnVerifyTest 8/8, full Integration **71/0 green**, Unit 407 green; ALSO repairs 12 other broken getAffectedRows callers; FG-013+B-030 red-team re-verified still closed. Residual B-043 (ENUM-width log noise) | 77f3498 |
| 11 | STABILIZE | B-041 (wizard idempotency: seed the 17 baked migration filenames 001-013 in installer; setup_test_db.sh loop 015→014) + B-034 (migration 028: 6 org tables, multi_query-safe/idempotent, smoke-tested) | wizard/CLI install 12→**0 errors**; setup_test_db 0 err/98 tables (014 security + org now present); unit 407 green; integration flaky (B-037). Surfaced **B-044** (wizard `multi_query` can't parse `DELIMITER`) | d84cb7b |
| 12 | STABILIZE | B-043/B-037 flake fix — (a) ENUM truncation: MFA.php 15 + NotificationService 5 MISORDERED log() calls (description→category slot), SecurityUtils 'ip_address'→'ip', WebAuthn severity 'high'→'warning', widen activityCategory ENUM (mig 029); (b) real flake = ActivityLogger singleton writes outside test txn + FK to hard-coded userID=1 → test hermeticity. + B-039 urlencode 4 sites | Integration **5/5 (indep) + 20/20 (builder) green** (was ~7/15 fail), 0 truncation; unit 407. **Gate now RELIABLE.** More 'hidden broken' fixed (MFA logging garbage categories) | 61d4a7c |
| 13 | STABILIZE→COMPLETE | user-docs sync (CHANGELOG/PROJECT_PROGRESS/.claude PROJECT_STATUS/SECURITY_SETUP → honest state of cycles 3-12) + clean rewrite of autopilot.json (STABILIZE done, phase_cursor→COMPLETE, next_action trimmed) | docs/state only, no code; **FOUNDATION PHASE CLOSED**; feature campaign G-003→G-004 begins cycle 14 | 4479a88 |
| 14 | COMPLETE (G-003 S1) | JWT crypto foundation: vendor firebase/php-jwt 6.11.1 → web/_lib/jwt/ + JwtLibLoader; `Jwt` facade (RS256 PINNED — rejects alg:none + RS/HS confusion) + `KeyManager` (key encrypted in tblSettings, rotation, JWKS) + migration 030 (tblRefreshTokens + tblRevokedTokens) | +32 tests (407→**439** green); alg-confusion/none/tamper/expired/revoked all REJECTED (verified). No endpoints yet (S3) | 84864f0 |
| 15 | COMPLETE (G-003 S2) | TokenService: access+opaque-refresh issuance (SHA-256 hashed, family-based), ATOMIC refresh ROTATION + REUSE-DETECTION (spent token → revoke whole family + HIGH session_hijack alert), revoke/revokeAllForUser (tokensInvalidBefore cutoff), jti-denylist+cutoff wired into verify. + fixed mig 030 errant `USE signula;` (B-046) | Integration 71→**81/81 green** (+10, reuse-detection verified), unit **442** green. No endpoints (S3). Residual B-047 (alert severity ENUM) | 9e320e3 |
| 16 | COMPLETE (G-003 S3) | endpoints /api/v1/auth/{token,refresh,revoke} + /.well-known/jwks.json (JwtAuthController) + wire BaseController Bearer auth (getUserByToken via TokenService, alongside X-API-Key); **primary-credential authz** on issuance (subject from verified identity only); rate-limited | Integration 81→**90/90 green** (+9; cross-user-mint-denied/tamper/refresh/revoke/JWKS verified), unit 442. **Fixed 2 API-wide bugs**: Router never injected router (all dispatch fataled — B-048) + APIKeyMiddleware 401'd Bearer | 9cc8f4e |
| 17 | COMPLETE (G-003 S4) | MANDATORY independent RED-TEAM on the JWT auth entrypoint (12 vectors) — crypto/claims/replay/IDOR/scope/JWKS all HELD; found 2 real bypasses → FIXED (BLUE) + re-attacked (VERIFY): **kid path-traversal→forgery** (isValidKid guard + basename) [HIGH] + **B-049 spray-able per-second rate-limit** (windowed SUM) [MED]; + fixed broken getUserByApiKey | both bypasses closed (PoCs now fail); unit 442→**444** (+hostile-kid tests), integration 90/90. Auth entrypoint red-team-hardened | 688f9bd |
| 18 | COMPLETE (G-003 S5 → BUILT) | docs: SECURITY_SETUP JWT section (jwt.* settings + KeyManager rotation + security-properties table) + OpenAPI +4 auth paths (/auth/{token,refresh,revoke} + jwks) & +7 schemas (42→46 paths, validates) + bearerAuth scheme + CHANGELOG | docs-only (unit 444 + integration 90/90 hold). **✅ G-003 JWT API auth FULLY BUILT (S1-S5) — first approved epic (#41) complete** | fb74f08 |
| 19 | COMPLETE (planning) | **Fable 5** deep-plan of the partner integration layer (#87) on top of G-001 → `.dev-team/specs/G-001-integration-plan.md`: 11 model-tagged stages (A1-A5 provider · IB0-2 button · IG1-2 guides · II1 iHymns); caught 3 G-003 build-contract gotchas (Jwt `typ` header, TokenService clientID/aud unpopulated, /userinfo aud); corrections (mig 031, BIGINT FKs, /oauth/authorize-idp) | plan-only, no code; **model policy recorded** (Fable plan / Sonnet-Haiku impl / sequential). Owner branding decisions surfaced | 765a23c |
| 20 | COMPLETE (IB0 logo) | **SIGNula.id brand SVGs** (Sonnet): animated symbol (key→lock + checkmark, reduced-motion) + 'SIGNula.id' wordmark + lockup — all named-layer SVG, light/dark/colour-blind variants + Base64 fallback; wired the 404 favicons + 8-size PWA icon manifest; `preview.html` | assets only (no .php); palette=blue→purple #667eea→#764ba2 (CB-safe, WCAG); fixed an invisible-checkmark renderer bug. **Owner: approve palette (vs teal)** | 289b511 |
| 21 | COMPLETE (G-001 A1) | OAuth2/OIDC client-registration model (Sonnet): migration 031 (tblOAuthClients + redirect-URIs + auth-codes + consents + access-grants; pairwise subjectType/sectorIdentifier) + OAuthClientManager (EXACT-match redirect-URI, hashed secret shown once, computeSubject pairwise) + partner-admin OAuth-client CRUD (IDOR-checked) | unit 444→**466**, integration 90→**110** green; mig 031 clean (101 tables); redirect-uri reject matrix + cross-partner IDOR verified. **Surfaced HIGH B-051 (SessionManager.php missing → 74 admin/partner files fatal, confirmed) + B-052 (loadSettings starves on blank isSensitive)** | e11bff0 |
| 22 | COMPLETE (safety floor) | Fix B-052 (loadSettings per-row decrypt try/catch → skip+log+continue; 10/369→369/369 on fresh install) + B-051 (new `_backend/SessionManager.php` 3 static facade methods over Auth + `AccessControl` SELECT real-columns-aliased) (Sonnet) | unit 466→**473**, integration 110→**113** green (conductor re-ran both). **Surfaced 3 MORE HIGH (code-confirmed): B-055 (login silently creates no session — `$expiresAt` bound as `i`), B-054 (`_backend/Database.php` also missing + `Database::getInstance()` absent → 74 pages still fatal), B-053 (admin flags absent from getCurrentUser → admins 403'd)** | 331255d |
| 23 | COMPLETE (safety floor) | Fix B-055 (Sonnet): login's tblUserSessions INSERT bound `$expiresAt` as int → INSERT died on forced STRICT sql_mode → `completeLogin()` false but `login()` ignored it → **login created no session (every login, silently)**. Bind `…ii`→`…is` + `login()` now surfaces the failure + guarded 3 other callers (oauth/callback ×3, mfa/verify ×1) | unit 473, integration 113→**115** green; new integration test asserts a real session row + valid future `expiresAt` + failure-propagation; **conductor independently proved it fails on the pre-fix bind**. Root cause: old AuthLoginTest only checked phantom `success===true` | e2096ba |
| 24 | COMPLETE (safety floor) | B-054 + B-056 (Sonnet): the 74 admin/partner pages require SIX never-committed `_backend/*` files. Added `Database::getInstance()`+`DatabaseConnection` adapter (proxies mysqli via `__call`/`__get`, no redeclare) + `Database.php` shim; created 4 more shims (`ActivityLogger` ×10, `APIKeyManager`, `RateLimiter`, `ServiceFeeManager` → real classes under `private_html/`) | unit 481→**489**, integration 120→**125** green; conductor verified all 7 `_backend` requires resolve + full bootstrap chain loads with no fatal. **Admin/partner UI + login now work e2e.** Next: B-053 (admin flags) | 7b4c459 |
| 25 | COMPLETE (safety floor CLOSED) | B-053 (Sonnet): `Auth::getCurrentUser()` enriched with the 4 admin/partner keys the 50 sites read — `isSuperAdmin` (real col, migration 009) + derived `partnerID`/`isPartnerAdmin`/`isAdmin` from `tblPartnerTeamMembers` (one prepared query, additive, null-safe, no AccessControl cycle) | unit 489, integration 125→**129** green; 4 new persona tests; conductor reviewed diff (prepared/additive/null-guarded) + re-ran both suites. **SAFETY FLOOR CLOSED — admin/partner UI + login work e2e.** Next: resume G-001 A2 | 327d002 |
| 26 | COMPLETE (G-001 A2) | OAuth2/OIDC AUTHORIZATION endpoint (Sonnet): new `/oauth/authorize-idp` (thin controller, does NOT clobber RP-side authorize.php) + `OAuthAuthorizeService` engine — exact-match redirect_uri (fatal-before-redirect), mandatory PKCE-S256, CSRF consent form, never-trust-POST re-validation, hashed single-use auth code, state echo, consent persistence | unit 489→**515**, integration 129→**140** green; +26 unit +11 integration; conductor adversarially reviewed the redirect-ordering/PKCE/CSRF paths + re-ran both. Surfaced B-057 (MED open-redirect on MFA path, queued for A5). NO token issuance yet (A3) | 5b9a2d4 |
| 27 | COMPLETE (G-001 A3) | OAuth2/OIDC TOKEN endpoint + id_token (Sonnet): `/oauth/token` (OAuthTokenService engine) code→tokens; 3 gotcha fixes (Jwt::signIdToken typ=JWT/no-scope, TokenService::issueForClient aud+clientID, refresh preserves them); atomic single-use + family-scoped replay-revoke (mig 032 issuedFamilyID), PKCE-S256 + redirect_uri via hash_equals, PAIRWISE sub | unit 515→**518**, integration 140→**156** green; +3 unit +16 integration (id_token verifies iss/sub[pairwise]/aud/nonce; replay→revoke; all RFC6749 negatives); conductor reviewed consume/replay/PKCE/bind-type diffs + re-ran both. Surfaced B-058 (HIGH refresh client-binding → refresh_token grant deferred) + B-059 (rate-limit) | 9ca75f7 |
| 28 | COMPLETE (G-001 A4) | OIDC userinfo + discovery + revocation + Connected-Apps (Sonnet): /oauth/userinfo (peek-aud→verify, scope-gated claims, pairwise sub matches id_token), /.well-known/openid-configuration (dir-index trick), RFC7009 /oauth/revoke (client-owned only) + revokeClientForUser, Connected-Apps settings page (consent+family revoke) | unit 518→**524**, integration 156→**179** green; +6 unit +23 integration; conductor reviewed no-over-disclosure + cross-client-revoke + re-ran both. Surfaced B-060 (HIGH: 13 settings/ account pages fatal — undefined requireLogin/SIGNULA_ROOT + missing header/footer) | 978dcc0 |
| 29 | COMPLETE (G-001 A5 harden) | Fix B-058 (refresh client-binding — reject cross-client/no-auth before spend; offline_access-gated refresh mint) + wire refresh_token grant into /oauth/token + advertise; B-059 (rate-limit /oauth/token+/oauth/revoke); B-057 (sanitize MFA redirect) (Sonnet) | unit 524, integration 179→**201** green; +22 tests; conductor reviewed binding-check placement + re-ran both. Red-team (cycle 30) next | cycle-29 commit |
