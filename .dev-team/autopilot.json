{
  "mission": "production-ready + feature-complete (in scope)",
  "aim": "SIGNula — a universal all-in-one CIAM / single-sign-on identity platform (PHP 8.4, MySQLi + prepared statements, MariaDB; Dreamhost shared hosting, no Composer in prod) providing internal accounts, MFA, passwordless, OAuth account-linking, passkeys/WebAuthn, a secure JSON REST API for first- and third-party service integration, multi-tier paid subscriptions, and full activity logging + data-protection tooling.",
  "definition_of_done": [
    "builds & runs from clean checkout (install wizard/CLI-load completes)",
    "core auth flow works e2e (register -> login -> MFA -> session -> logout)",
    "tests cover core flows & pass (unit 407 + integration 71, reliable gate)",
    "no unresolved Critical/High security finding",
    "no High correctness/UX backlog item remains",
    "docs enough to run it (README/install; user docs synced)",
    "feature-complete in scope (every High in-scope FG built or queued); approved epics G-001..G-004 built"
  ],
  "out_of_code_scope_surfaced": ["#83 prod DB creds", "#84 email provider creds", "#85 cron", "#70 live payment creds", "#71 staging deploy", "#79 HTTPS/.htaccess finalize", "#77 monitoring", "#78 backups", "#72 manual E2E", "#75 cross-device", "#74 load test", "#81 legal review", "B-044 install-wizard multi_query/DELIMITER (CLI/phpMyAdmin load path works)"],
  "plan": [
    {"phase": "DISCOVER",  "mode": "iterate-audit+featurefind", "exit": "work queue seeded", "status": "done"},
    {"phase": "STABILIZE", "mode": "iterate",  "exit": "no High correctness/test gap; reliable gate", "status": "done"},
    {"phase": "SECURE",    "mode": "security", "exit": "no unresolved Critical/High finding; rounds dry", "status": "in-progress", "note": "13 deferred MEDIUMs #22-#34 + WebAuthn FG-013 (auth-bypass, red-teamed) + CORS/CSRF/SMTP-enc done; a full Phase-0 threat-model/scanner purple-team sweep across all surfaces is deferred (interleaved with feature red-teams + revisited after COMPLETE)."},
    {"phase": "COMPLETE",  "mode": "orchestrator(feature)", "exit": "every High in-scope gap built-or-queued; G-001..G-004 built", "status": "in-progress"},
    {"phase": "POLISH",    "mode": "iterate",  "exit": "no High UX/perf item", "status": "pending"},
    {"phase": "VERIFY",    "mode": "review",   "exit": "complete review pass: overall PASS @ last_commit", "status": "pending"}
  ],
  "phase_cursor": 3,
  "state": "RUNNING",
  "cursor": null,
  "next_action": "cycle 49 DONE (G-004 COMPLIANCE L2 — consent-management surface, Sonnet): server-side granular consent replacing the localStorage-only banner. Migration 041 (idempotent): tblConsentCategories (4 seeded — necessary[strictly-necessary+granted]/functional/analytics/marketing) + tblPolicyVersions (3 DRAFT v1.0 anchors, isCurrent=1, NO policy text) + 7 consent.* settings. New ConsentCategoryService (delegates to the single ConsentManager path): getActiveCategories/getCurrentPolicyVersion, issueOrGetVisitorID (UUID v4, SIGNULA_VID cookie, SameSite=Lax, httponly OFF per spec), recordBannerChoices (necessary FORCED granted server-side, not just UI), userNeedsReconsent/maybeReconsentRedirect (checks consent.reconsent.enforce FIRST before any DB touch), honorGpcSignalIfPresent (session-guarded + DB-idempotent). Rewrote public-footer.php banner: data-driven categories from getActiveCategories, real <form POST /api/v1/consent> (works with zero JS via native POST+303; JS is progressive enhancement), necessary disabled, <noscript> -> /legal/consent full-page fallback. New consent POST endpoint api/v1/consent/index.php (dirname __DIR__,4 verified resolves) + full-page /legal/consent + public /legal/do-not-sell + /legal/reconsent interstitial. Do-Not-Sell toggle (set_do_not_sell action, CSRF, config-gated) on settings/privacy.php. GPC hook in config.php bootstrap — cheap: a request with no Sec-GPC header costs a single array lookup (no class load, no DB); only when Sec-GPC:1 + consent.gpc.honor does it record a do_not_sell opt-out, session-flag + getCurrent-idempotent (no per-request row spam). Re-consent gate wired into login.php (after verified-email/no-MFA) + mfa/verify.php (after MFA success) — GATED by consent.reconsent.enforce (ships '0'/OFF) so it is INERT for the existing login test baseline. CONDUCTOR INDEPENDENTLY VERIFIED: php -l clean; config.php GPC hook cost-guarded (common path = 1 array lookup); maybeReconsentRedirect returns null before any DB when enforce=0 (login tests inert — Auth::login returns userID so the login.php key is right; change sits after the requiresMFA branch); necessary forced-granted server-side; GPC session+DB idempotent; migration 041 applied to signula_test (4 categories/3 anchors/8 consent.* settings [7 new + proof_hash from 040]); CSRF on the consent endpoint + reconsent + /legal/consent + set_do_not_sell; ZERO fabricated legal/statutory text; consent endpoint depth resolves; footer no-JS form + noscript present; Unit 687->711 (+24) 0F/0E, Integration 421->446 (+25) 0F/0E (separate testsuites; issues = pre-existing warnings/deprecations/skips). GATE GREEN. ===>> NEXT = cycle 50 = G-004 L3 (spec §4) = the DATA-DRIVEN REGIME MODEL — the core that makes '18+ regimes without code sprawl' true (PHP never names a country; it reads rows). Migration ~042 (idempotent, house rules, real tblSettings cols): tblComplianceRegimes (regimeID PK, regimeCode UNIQUE, displayName, jurisdiction, dsarSlaDays, dsarExtensionDays, breachNotifyHours, breachNotifyAuthority, minAge, honorsGPC, requiresDoNotSell, lawfulBasisRequired, isActive DEFAULT 0, isDraft DEFAULT 1, notes) + tblRegimeRights (regimeID,right PK; right in access|portability|erasure|rectification|restriction|object|do_not_sell|withdraw_consent|automated_decision_optout) + tblRegimeDisclosures (regimeID, disclosureKey, title, body — body USER-SUPPLIED, ships EMPTY) + tblRegimeResolution (matchType country|region|partner|default, matchValue, regimeID, priority). SEED the 18+ regimes from the brief (GDPR, UK_GDPR, CCPA, CPRA, VCDPA, CPA, CTDPA, UCPA, HIPAA, COPPA, LGPD, PIPEDA, APPI, PIPA, POPIA, DPDP, PIPL, AU_PA88, NZ_PA20) as **isActive=0, isDraft=1** rows with **NULL/placeholder** SLA/age/breach values + notes='WARNING: values pending legal confirmation' — the migration ships the SKELETON (one row per named regime as a checklist); the VALUES are a user/legal task. Common public anchors (GDPR 30d/72h, CCPA 45d, COPPA age 13, GDPR-K 13-16) MAY be offered as SUGGESTED draft values in notes, clearly labelled 'confirm with legal', NEVER auto-activated. New RegimeResolver (static, web/private_html/compliance/): resolve(?userID,?partnerID,?countryHint): array (walk tblRegimeResolution by priority -> active regime row or the MOST-PROTECTIVE default), slaDaysFor/minAgeFor/breachWindowHours/rightsFor/honorsGPC; MOST-PROTECTIVE fallback (if no regime resolves or a value is NULL -> fall back to dsar.default_sla_days=30 / strictest — NEVER fail open). Then RETRO-WIRE the single integration point: DSARManager::submit() and ConsentManager::record() call RegimeResolver to stamp regimeCode/dueDate (currently they hardcode the default SLA — make it regime-aware, still falling back to the default). Admin CRUD web/public_html/admin/compliance/regimes/index.php (super-admin, same gate/pattern as the DSAR queue) with a draft->active workflow: a regime CANNOT be activated until SLA+minAge+breach are non-NULL (forces the user to fill the legal values); logAdminAction on every change. GREP-GATE (spec §4.4 AC): after this, NO regime code LITERAL (no 'GDPR'/'CCPA'/'30 days'/'72 hour' hardcoded) anywhere in web/private_html PHP logic — the resolver reads rows; adding the 19th regime is 4 row inserts, zero PHP edits. CONDUCTOR RECON FIRST: DSARManager::submit's current dueDate computation + ConsentManager::record's regimeCode handling (both currently use the flat default) so the retro-wire is surgical; the admin queue gate/layout to mirror. SAFETY: regime VALUES are user/legal input (ship draft/NULL, never auto-activate); NO legal text in tblRegimeDisclosures.body (ships empty). Migration ~042. Tests: RegimeResolver SLA/age/most-protective-fallback math (unit, DB-free where possible); resolve() by country/partner/default priority; a DSAR dueDate is computed from the resolved regime's dsarSlaDays falling back to the default; inactive/draft regimes are NEVER used to resolve a live request; grep proves no regime literal in web/private_html. Keep Unit 711 + Integration 446 green (+ new). CONDUCTOR INDEPENDENTLY VERIFIES the grep-gate (no regime literal) + most-protective fallback + draft-cant-activate + retro-wire didn't break L1 DSAR/consent tests. RUNWAY: cap 60, at 50 -> ~10 left; L3 may span 2 cycles (regime model+resolver+retro-wire in 50, admin CRUD in 51 if needed); L4 breach/RoPA/retention+cron (mig ~043). Near ~cycle 56: STOP starting new stages, run a final VERIFY pass, SURFACE built-vs-remaining to the owner to decide on raising the cap. THEN CAP-API (#86 + #80 OpenAPI). MODEL POLICY: Fable plan/red-team, Sonnet/Haiku build, SEQUENTIAL one-agent. Conductor RE-RUNS every gate itself (separate --testsuite; 0F/0E=GREEN) + reviews ACTUAL source (not just source-sentinel tests). When editing THIS field use SINGLE quotes for code snippets (double-quotes break the JSON) + re-validate with python json.load.",
  "_next_action_orig": "cycle 18 DONE: G-003 S5 docs (SECURITY_SETUP jwt.* + KeyManager rotation + security properties; openapi.yaml +4 auth paths/+7 schemas/bearerAuth, 42->46 paths validates; CHANGELOG). ===G-003 JWT API AUTH FULLY BUILT (S1-S5, gate_ledger status=built) — first approved epic (#41) complete.=== NEXT = cycle 19 = G-001 IdP Phase A STAGE A1 (read .dev-team/specs/G-001.md; REUSES the G-003 Jwt/KeyManager/JWKS signing foundation): OAuth2/OIDC PROVIDER, stage A1 = client registration data model + admin — migration 031 tblOAuthClients (client_id, client_secret HASHED like tblAPIKeys.keyHash, redirect_uri allowlist, allowed_scopes, grant_types, owner partnerID, PKCE-required flag) + tblOAuthAuthCodes + tblOAuthConsents + tblOAuthAccessGrants per the spec's §data-model; a client-registration/management path (RP clients owned by tblPartners); unit + integration tests. NO authorize/token endpoints yet (later A-stages). Then A2 /oauth/authorize + PKCE code issuance + consent, A3 /oauth/token (authorization_code+PKCE+refresh, reuses Jwt for id_token+access), A4 /oauth/userinfo + /.well-known/openid-configuration + JWKS reuse + revocation, A5 hardening + RED-TEAM (redirect-uri exact-match, PKCE downgrade, code replay, mix-up, consent/scope bypass) + docs. One stage per cycle, one commit per stage, checker + security review + integration gate. THEN G-002 billing TEST-MODE (fix B-025 first; specs/G-002.md; NEVER live creds) -> G-004 compliance L1 (specs/G-004.md) -> CAP-API #86. Migrations 001-030; next 031. [PRIOR] cycle 17 DONE: G-003 S4 MANDATORY red-team on the JWT auth entrypoint — 12 vectors, core HELD (alg-confusion/none, tamper, key-confusion, replay, refresh-reuse, temporal, aud/iss, IDOR-on-issuance, scope, JWKS, X-API-Key); found+FIXED+re-attacked-closed 2 bypasses: kid path-traversal->forgery [HIGH] (isValidKid strict-charset guard + basename) + B-049 spray-able per-second rate-limit [MED] (windowed SUM) + fixed broken getUserByApiKey; unit 442->444, integration 90/90. NEXT = cycle 18 = G-003 STAGE S5 (docs — FINAL stage, docs-only no behaviour change): document the JWT auth in _docs/setup/SECURITY_SETUP.md (the jwt.* policy settings, KeyManager key generation/rotation, admin config) + add the /api/v1/auth/{token,refresh,revoke} + /.well-known/jwks.json endpoints to the OpenAPI spec web/public_html/api/docs/openapi.yaml (request/response schemas + a bearerAuth security scheme) + a short client usage note; also update CHANGELOG. Keep unit 444 + integration 90/90 green. THEN set gate_ledger G-003 status='built' (all stages S1-S5 done). THEN G-001 IdP Phase A (specs/G-001.md, reuses this JWT signing foundation) -> G-002 billing TEST-MODE (fix B-025 first; specs/G-002.md; assertTestMode fail-closed; NEVER live creds) -> G-004 compliance L1 (specs/G-004.md) -> CAP-API capstone (#86). Migrations 001-030; next 031. [PRIOR] cycle 16 DONE: G-003 S3 endpoints + Bearer wiring built (POST /api/v1/auth/{token,refresh,revoke} + GET /.well-known/jwks.json via JwtAuthController; BaseController::getUserByToken now verifies Bearer via TokenService alongside X-API-Key; primary-credential authz on issuance — subject from verified identity only, cross-user mint denied; rate-limited; integration 90/90 green, unit 442; fixed 2 pre-existing API-wide bugs: Router never injected itself into controllers [B-048, dispatch fataled] + APIKeyMiddleware 401'd Bearer). NEXT = cycle 17 = G-003 STAGE S4 = MANDATORY RED-TEAM (live auth entrypoint), per .dev-team/specs/G-003.md: spawn an INDEPENDENT red-team agent to attack /api/v1/auth/* + Bearer verify — alg confusion (RS256->HS256/none), signature/kid manipulation, key confusion, token replay, refresh reuse (must revoke family), IDOR/privilege on issuance (mint for another user must FAIL), scope escalation, rate-limit bypass (B-049 per-second-window weakness), JWKS private-key leak, expired/tampered acceptance, X-API-Key regression. Produce PoCs; fix any real bypass (BLUE) then re-attack (VERIFY). Address B-049 (windowed limiter for /auth/token) if red-team confirms exploitable. THEN S5 = docs + OpenAPI stub for the new endpoints. THEN G-001 IdP Phase A (specs/G-001.md, reuses this signing) -> G-002 billing TEST-MODE (fix B-025 first; specs/G-002.md) -> G-004 compliance L1 (specs/G-004.md) -> CAP-API #86. Migrations 001-030; next 031. [PRIOR] cycle 15 DONE: G-003 S2 TokenService built — access+opaque-refresh issuance, atomic refresh rotation + REUSE-DETECTION (spent token revokes whole family + HIGH session_hijack alert), revoke/revokeAllForUser (tokensInvalidBefore), jti-denylist+cutoff wired into verifyAccessToken; integration 71->81/81 green, unit 442; also fixed migration 030's errant USE signula (B-046). NEXT = cycle 16 = G-003 STAGE S3 (endpoints + Bearer wiring, per .dev-team/specs/G-003.md): create REST endpoints POST /api/v1/auth/token (issue tokens via TokenService after a PRIMARY auth — verify password or an existing authenticated session; do NOT let anyone mint tokens for any user), /api/v1/auth/refresh (rotate), /api/v1/auth/revoke, and GET /.well-known/jwks.json (KeyManager::getJwks). Then WIRE web/private_html/api/BaseController.php getUserByToken()/requireAuth() to accept a Bearer JWT (TokenService::verifyAccessToken -> load the user) ALONGSIDE the existing X-API-Key. Follow the existing api/ Router+Response+front-controller patterns; rate-limit the token endpoint (RateLimiter); consistent error keys. Integration tests: issue -> call a protected endpoint with the Bearer -> refresh -> revoke -> denied. Keep unit 442 + integration green. THEN S4 hardening + RED-TEAM (alg confusion, none, replay, refresh reuse, key confusion, IDOR on token issuance), S5 docs. THEN G-001 IdP Phase A (reuses this) -> G-002 billing TEST-MODE -> G-004 compliance L1 -> CAP-API #86. Migrations 001-030; next 031. [PRIOR] cycle 14 DONE: G-003 S1 crypto foundation built (firebase/php-jwt 6.11.1 vendored to web/_lib/jwt/, RS256-pinned Jwt facade rejecting alg:none + RS/HS confusion, KeyManager with encrypted key + rotation + JWKS, migration 030 tblRefreshTokens/tblRevokedTokens; +32 tests -> 439 green). NEXT = cycle 15 = G-003 STAGE S2 (TokenService, per .dev-team/specs/G-003.md): access-token ISSUANCE (short exp, jti, scope, sub=userID) + opaque refresh-token issuance (SHA-256 hashed into tblRefreshTokens) + refresh-token ROTATION with REUSE-DETECTION (reuse of a rotated token -> revoke the whole token family + security alert). Unit tests issue/refresh/rotate/reuse/revoke. Still NO endpoints (S3). Then S3 endpoints /api/v1/auth/{token,refresh,revoke} + /.well-known/jwks.json + wire BaseController::getUserByToken()/requireAuth() for Bearer + integration tests; S4 hardening + RED-TEAM; S5 docs. Migrations now 001-030; next 031. [ORIGINAL CAMPAIGN PLAN] ===FOUNDATION COMPLETE (cycles 3-12)===. cycle 13 = user-docs sync + this state cleanup. NEXT = cycle 14 START THE FEATURE CAMPAIGN with G-003 JWT API auth (safety floor clear; integration gate reliable). Read .dev-team/specs/G-003.md and build stage S1 first: vendor firebase/php-jwt v6 SOURCE into web/_lib/jwt/ (self-host pattern like _lib/tcpdf), add the SIGNula\\Security\\Jwt facade + KeyManager (RS256, kid, private key encrypted in tblSettings isSensitive with _private PEM fallback) + migration 030 (tblRevokedTokens jti denylist + refresh-token table); unit tests for sign/verify/expiry/alg-confusion/none-rejection. Then S2 issuance+refresh-rotation, S3 endpoints /api/v1/auth/{token,refresh,revoke} + JWKS + wire BaseController::getUserByToken()/requireAuth() for Bearer, S4 hardening + RED-TEAM (alg confusion, none, replay), S5 docs. One spec stage per cycle, one commit per stage, checker + integration gate each. THEN G-001 IdP Phase A (specs/G-001.md, reuses G-003 signing) -> G-002 billing TEST-MODE (fix B-025 first; specs/G-002.md; assertTestMode fail-closed; NEVER live creds) -> G-004 compliance L1 (specs/G-004.md). THEN CAP-API capstone (#86). Migrations 001-029; next 030.",
  "queue": {"backlog": ".dev-team/PROJECT.md#Backlog", "feature_gaps": ".dev-team/FEATURES.md", "findings": ".dev-team/PROJECT.md#Backlog"},
  "gate_ledger": [
    {"id": "G-001", "kind": "feature", "fg": "FG-001", "title": "SIGNula as IdP — SAML/OIDC provider + OAuth2 authorization server", "spec_ref": ".dev-team/specs/G-001.md", "status": "approved", "approved_by": "user", "reason": "Largest capability gap; Phase A (OAuth2/OIDC provider) reuses G-003 signing; Phase B SAML later; multi-cycle, security-reviewed per stage."},
    {"id": "G-002", "kind": "feature", "fg": "FG-003", "title": "Recurring billing / subscription lifecycle engine (#67)", "spec_ref": ".dev-team/specs/G-002.md", "status": "built", "built_cycle": 45, "approved_by": "user", "reason": "TEST MODE only; live-payment credential wiring (#70) stays operational/user. BUILT S1-S5 (foundation+fail-closed guard on ALL money-movers, proration, webhook->state [fixed B-072], dunning+CreditManager repair [B-071], red-team-passed [F-B01 CRITICAL guard-bypass closed]) + finish (B-070/B-073/B-074, docs). Going live = owner #70 (live keys + billing.test_mode_guard=0). Residual: B-078 (PayPal recurring not wired e2e — Stripe subs flow e2e), B-077 (webhook dedup race, low)."},
    {"id": "G-003", "kind": "feature", "fg": "FG-004", "title": "JWT API authentication (#41)", "spec_ref": ".dev-team/specs/G-003.md", "status": "built", "built_cycle": 18, "approved_by": "user", "reason": "BUILD FIRST. Vendored firebase/php-jwt, RS256+JWKS+refresh rotation. Shares signing foundation with G-001."},
    {"id": "G-004", "kind": "feature", "fg": "FG-002", "title": "Multi-jurisdiction compliance tooling", "spec_ref": ".dev-team/specs/G-004.md", "status": "approved", "approved_by": "user", "reason": "Data-driven regime model; DSARManager delegates to existing AccountManager; L1 consent/DSAR first. Legal values ship draft/user-filled."}
  ],
  "capstone_tasks": [
    {"id": "CAP-API", "title": "Comprehensive API completeness + security pass + complete OpenAPI/Swagger docs", "source": "user directive 2026-06-30", "when": "AFTER the G-001..G-004 feature campaign", "scope": "Every feature has secure (authn+authz/rate-limited/validated/no-IDOR/consistent-error-keys) REST endpoints; complete openapi.yaml + Swagger UI.", "github_issue": "#86 + #80 (OpenAPI)"}
  ],
  "residual_backlog_note": "Deferred to POLISH/later (all Med/Low or operational): B-035/B-036/B-038/B-042 migration hygiene, B-044 wizard DELIMITER (CLI-load works), B-045 per-file test isolation, #76 WCAG a11y, #80/#86 API+OpenAPI (=CAP-API). Operational (user): #70/#71/#77/#78/#79/#83/#84/#85. See .dev-team/PROJECT.md#Backlog for the full B-001..B-045 ledger (most High items DONE cycles 3-12).",
  "model_policy": {"planning": "Fable 5 (Agent model:fable) for deep planning/architecture/red-team reasoning; fallback Opus if fable unavailable, re-check fable each planning round", "implementation": "Sonnet or Haiku (most appropriate) for code changes; Opus ONLY if absolutely necessary", "parallelism": "SEQUENTIAL — one agent at a time, NO parallel fan-outs (usage efficiency)", "set_by": "user 2026-07-02"},
  "integration_deliverables": {"issue": "#87", "when": "delivered on top of G-001 (OAuth2/OIDC provider)", "items": ["Sign in with SIGNula.id button (branded, light/dark + variants, WCAG, PKCE drop-in snippet)", "integration guides: quickstart + web/PWA + native + server-side + security best-practice", "iHymns first-party reference integration (github.com/MWBMPartners/iHymns, web/PWA + native)", "SDK/copy-paste snippets per platform"], "note": "Deep-planned via Fable 5 (.dev-team/specs/G-001-integration-plan.md), then implemented via Sonnet/Haiku. Extends the G-001 provider spec to the partner-facing layer.", "owner_decisions_2026-07-02": {"brand": "SIGNula / signula.id is canonical (matches repo+code); OIDC issuer = https://signula.id; button label 'Sign in with SIGNula.id'", "logo": "custom vector SVG set: SYMBOL = a horizontal key entering a lock + a checkmark, WITH alpha transparency + animation; WORDMARK 'SIGNula.id'; COMBINED lockup; all SVGs with appropriately-NAMED layers/groups for easy recolour; light-mode + dark-mode + colour-blind-friendly variants; Base64 fallback; ref web/public_html_landing/index.php (rework/scrap OK)", "subject_type": "PAIRWISE — per-relying-party pseudonymous sub. A1 tblOAuthClients gets subject_type + sector_identifier columns + a global pairwise salt setting; A3/A4 compute sub = hash(sector || local_user_id || salt) for id_token + userinfo"}},
  "blocked": {"is": false, "reason": null, "since": null},
  "cycle": 49,
  "cap": 60,
  "verify": {"commit": null, "claim_groups": [], "integration": "pending", "completeness_critic": "pending"},
  "verified_at_commit": null,
  "lock": {"holder": null, "ts": null, "stale_after_min": 90},
  "branch": "autopilot/2026-06-30",
  "last_commit": "a365f53",
  "trajectory_ref": ".dev-team/PROJECT.md#Trajectory-ledger",
  "user_docs_to_keep_current": ["CHANGELOG.md", "PROJECT_PROGRESS.md", ".claude/PROJECT_STATUS.md", "_docs/setup/SECURITY_SETUP.md"],
  "terminal": false,
  "updated": "2026-07-11T14:00:00Z"
}
