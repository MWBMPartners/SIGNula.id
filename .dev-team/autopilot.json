{
  "mission": "production-ready + feature-complete (in scope)",
  "aim": "SIGNula — a universal all-in-one CIAM / single-sign-on identity platform (PHP 8.4, MySQLi + prepared statements, MariaDB; Dreamhost shared hosting, no Composer in prod) providing internal accounts, MFA, passwordless, OAuth account-linking, passkeys/WebAuthn, a secure JSON REST API for first- and third-party service integration, multi-tier paid subscriptions, and full activity logging + data-protection tooling.",
  "definition_of_done": [
    "builds & runs from clean checkout (install wizard/CLI-load completes)",
    "core auth flow works e2e (register -> login -> MFA -> session -> logout)",
    "tests cover core flows & pass (unit 407 + integration 71, reliable gate)",
    "no unresolved Critical/High security finding",
    "no High correctness/UX backlog item remains",
    "docs enough to run it (README/install; user docs synced)",
    "feature-complete in scope (every High in-scope FG built or queued); approved epics G-001..G-004 built"
  ],
  "out_of_code_scope_surfaced": ["#83 prod DB creds", "#84 email provider creds", "#85 cron", "#70 live payment creds", "#71 staging deploy", "#79 HTTPS/.htaccess finalize", "#77 monitoring", "#78 backups", "#72 manual E2E", "#75 cross-device", "#74 load test", "#81 legal review", "B-044 install-wizard multi_query/DELIMITER (CLI/phpMyAdmin load path works)"],
  "plan": [
    {"phase": "DISCOVER",  "mode": "iterate-audit+featurefind", "exit": "work queue seeded", "status": "done"},
    {"phase": "STABILIZE", "mode": "iterate",  "exit": "no High correctness/test gap; reliable gate", "status": "done"},
    {"phase": "SECURE",    "mode": "security", "exit": "no unresolved Critical/High finding; rounds dry", "status": "in-progress", "note": "13 deferred MEDIUMs #22-#34 + WebAuthn FG-013 (auth-bypass, red-teamed) + CORS/CSRF/SMTP-enc done; a full Phase-0 threat-model/scanner purple-team sweep across all surfaces is deferred (interleaved with feature red-teams + revisited after COMPLETE)."},
    {"phase": "COMPLETE",  "mode": "orchestrator(feature)", "exit": "every High in-scope gap built-or-queued; G-001..G-004 built", "status": "in-progress"},
    {"phase": "POLISH",    "mode": "iterate",  "exit": "no High UX/perf item", "status": "pending"},
    {"phase": "VERIFY",    "mode": "review",   "exit": "complete review pass: overall PASS @ last_commit", "status": "pending"}
  ],
  "phase_cursor": 3,
  "state": "RUNNING",
  "cursor": null,
  "next_action": "cycle 39 DONE (B-062 + B-063, Sonnet, additive): B-062 — migration 035 replaced tblOAuthSubjects global UNIQUE(subjectHash) with composite UNIQUE(clientID,subjectHash) so public-subject clients no longer collide (upsert + userinfo already key on both); B-063 — verifyAccessToken now resolves an RP token's pairwise sub->userID via tblOAuthSubjects WHERE subjectHash=sub AND clientID=getClient(aud) and applies the tokensInvalidBefore mass-revoke cutoff (FAIL-SOFT: unresolvable -> skip, never newly-rejects a valid token). Conductor independently verified: composite key applied (old uk_subject gone), B-063 additive, php -l clean; +7 tests; unit 527, integration 240->247 green. ===>> SECURITY BACKLOG CLEAR (all red-team F-01..F-05 + B-057/058/059/061/062/063/064/067/068 resolved). NEXT = cycle 40 = START G-002 RECURRING-BILLING (TEST-MODE ONLY — the 2nd approved epic). CONDUCTOR: read .dev-team/specs/G-002.md + decompose into stages (like G-001's A1-A5), and check the existing payments code first (web/private_html/payments/* already has UsageTracker/UsageBillingService/BillingScheduler/ServiceFeeManager from earlier work + migration 018/019 tier system + PayPalProvider) — REUSE + EXTEND, don't rebuild. STAGE S1 = fix **B-025** FIRST (tblBillingSchedule schema drift — the billing schedule table/columns the scheduler expects vs what migrations create; grep the drift + reconcile via a migration or code fix so BillingScheduler runs clean) THEN the billing-engine foundation stage per the spec: subscription lifecycle (create/renew/cancel/past-due/grace) data model + a provider-managed-subscription abstraction. SAFETY RAILS (NON-NEGOTIABLE): TEST-MODE ONLY — an `assertTestMode()`/`isTestMode()` gate that FAILS CLOSED (no live charge path reachable); NEVER touch/require live payment credentials (live wiring stays operational, owner's #70); PayPal/Stripe/etc calls go to sandbox endpoints only; every payment mutation is idempotent + logged. One spec stage per cycle, one commit per stage, checker + integration gate + a security review each (payment code = high-value target). Keep unit 527 + integration 247 green. Migrations 001-035; next 036. THEN G-004 compliance L1 (specs/G-004.md; data-driven regime model; consent + DSAR; DSARManager delegates to the existing AccountManager; legal text ships draft/user-filled) -> CAP-API (#86 comprehensive API completeness + security pass across every feature's REST endpoints + #80 complete OpenAPI/Swagger). ===[PRIOR] cycle 38 DONE (B-061 + B-064 security, Sonnet): rewrote getClientIP() — DEFAULT = REMOTE_ADDR only (spoofed X-Forwarded-For/CF-Connecting-IP IGNORED), forwarded headers honoured ONLY when the immediate peer (REMOTE_ADDR) is in a configured `security.trusted_proxies` CIDR allowlist (IPv4+IPv6, EMPTY by default; migration 034 seeds it) via a from-scratch ipInCidr/ipInAnyCidr matcher (inet_pton packed-byte) + rightmost-non-trusted XFF walk; fails safe to REMOTE_ADDR; dev/private REMOTE_ADDR preserved. Centralized (21 callers unchanged: rate limiters incl. B-059, Auth::login, JWT, IP-reputation, audit). B-064: removed the PHP-8.4-deprecated E_STRICT from config.php's error_reporting mask + error-name map (could leak bytes onto responses). Conductor INDEPENDENTLY verified: spoofed XFF+CF ignored by default -> REMOTE_ADDR; private REMOTE_ADDR preserved; ipInCidr correct across 6 IPv4/IPv6 cases; grep E_STRICT empty; migration 034 clean; +4 tests; unit 527, integration 236->240 green (both re-run properly with setup_test_db). [PRIOR cycle 39 — DONE, see head]: B-062 + B-063 (SONNET, both LOW, OAuth-token layer — quick): (B-062) `tblOAuthSubjects.subjectHash` is globally UNIQUE, which for `subjectType='public'` clients (non-default, unused today) collides (two public clients for the same user share sub=userID -> UPSERT overwrites -> userinfo breaks for one). Fix: change the UNIQUE to (clientID, subjectHash) [migration 035, idempotent — drop the old unique add the composite, guarded] + make OAuthUserInfoService's lookup key on BOTH clientID AND subjectHash (it may already filter clientID — verify) + TokenService's resolveAndRecordSubject upsert key on the composite. (B-063) after F-01, verifyAccessToken's `tokensInvalidBefore` mass-revoke cutoff only applies to first-party tokens (RP pairwise sub isn't the userID); extend it to RP access tokens by resolving the pairwise sub->userID via a tblOAuthSubjects lookup (the claim carries aud=client_id; look up WHERE subjectHash=sub AND clientID=the aud's client) then apply the cutoff — so 'log out everywhere' reaches RP access tokens too (fail-closed, additive). Add tests (public-subject clients don't collide; a mass-revoke invalidates an RP access token issued before the cutoff). Keep unit 527 + integration 240 green. Migrations 001-034; next 035 (B-062). THEN G-002 billing TEST-MODE (fix B-025 tblBillingSchedule drift FIRST; specs/G-002.md; assertTestMode fail-closed; NEVER live payment creds; provider-managed subscriptions/webhooks in test mode) -> G-004 compliance L1 (specs/G-004.md; consent + DSAR; DSARManager delegates to the existing AccountManager; legal text ships draft/user-filled) -> CAP-API (#86 comprehensive API completeness + security pass across every feature's REST endpoints + #80 complete OpenAPI/Swagger). ===[PRIOR] cycle 37 DONE (II1 iHymns reference integration, Sonnet): self-contained copy-ready 'Sign in with SIGNula.id' reference for first-party iHymns under web/public_html/docs/examples/ihymns/ — README (end-to-end), button-embed.html, login-start.php (server PKCE/state/nonce init), callback.php (single-use state check -> /oauth/token exchange -> id_token verified vs JWKS [RS256, iss=https://signula.id, aud=IHYMNS_CLIENT_ID, exp, hash_equals nonce] -> pairwise sub->iHymns user, first-login auto-provision keyed on signula_sub UNIQUE not email), callback.js (SPA + backend-exchange recommended), native-notes.md, schema.sql. Accurate to real endpoints; each file headed 'reference — not a live route'. Conductor added an .htaccess so the reference php/js/sql serve as VIEWABLE SOURCE + never execute (webroot hygiene). Conductor verified: example dir only (no SIGNula source touched), php -l/node clean, gates re-run PROPERLY (setup_test_db first) -> unit 527, integration 236 green. ===>> G-001 INTEGRATION LAYER (#87) COMPLETE: provider A1-A5 (red-teamed+documented) + button IB1 + guides IG1-2 + iHymns reference II1 — the full 'Sign in with SIGNula.id' partner integration is delivered. [PRIOR cycle 38 — DONE, see head]: SECURITY FOLLOW-UPS B-061 + B-064 (SONNET, both in web/_config/config.php + callers): (B-061, MED/High for rate-limit+audit integrity — app-wide, CAREFUL) `getClientIP()` currently trusts the first valid public IP from CF-Connecting-IP -> X-Forwarded-For -> X-Real-IP -> Client-IP -> REMOTE_ADDR with NO trusted-proxy allowlist, so on the direct-Apache Dreamhost target an attacker spoofs these to defeat every per-IP rate limit + skew IP-reputation/impossible-travel/audit. Fix: default to REMOTE_ADDR ONLY; honour forwarded headers ONLY when the immediate peer (REMOTE_ADDR) is in a configured `security.trusted_proxies` CIDR allowlist (a new tblSettings row, EMPTY by default). Add the setting (migration 034, idempotent) + parse CIDR (IPv4+IPv6). VERIFY: with no trusted proxies, getClientIP returns REMOTE_ADDR regardless of XFF; with a trusted proxy configured, it honours XFF. This touches EVERY getClientIP caller (rate limiters, audit, IP reputation) — run the FULL suites + confirm no regression; add tests for spoofed-XFF-ignored + trusted-proxy-honoured. (B-064, MED — trivial, same file) remove the PHP-8.4-deprecated `E_STRICT` constant from config.php's `error_reporting()` mask (it's a no-op in 8.4 and can emit a deprecation notice as LEADING BYTES on responses before display_errors is set off — corrupts JSON/headers). Keep unit 527 + integration 236 green (+ new B-061 tests). THEN cycle 39 = B-062 (subjectHash per-client uniqueness for public-subject clients — migration + TokenService/UserInfo) + B-063 (extend the tokensInvalidBefore mass-revoke cutoff to RP access tokens via the tblOAuthSubjects lookup) — both Low, OAuth-token layer. THEN G-002 billing TEST-MODE (fix B-025 tblBillingSchedule drift FIRST; specs/G-002.md; assertTestMode fail-closed; NEVER live creds; provider-managed subscriptions) -> G-004 compliance L1 (specs/G-004.md; consent/DSAR; DSARManager delegates to AccountManager) -> CAP-API (#86 comprehensive API completeness+security + #80 OpenAPI/Swagger). Migrations 001-033; next 034 (B-061 trusted_proxies setting). ===[PRIOR] cycle 36 DONE (IG1-2 partner integration guides, Haiku, docs-only): 5 new guide pages beside the cycle-33 base guide (matching sibling docs format) — oidc-quickstart.php (5-step), oidc-web.php (SPA/PWA drop-in + redirect handler + token-storage trade-offs), oidc-native.php (iOS ASWebAuthenticationSession / Android Custom Tabs, public client, custom-scheme/App-Link redirect, Keychain/Keystore), oidc-server.php (confidential client_secret_basic + server exchange + client-bound refresh + revoke-on-logout), oidc-security.php (checklist: exact redirect_uri, mandatory S256 PKCE, state+nonce, full id_token/JWKS verification incl. pairwise-sub caveat, 8 pitfalls); wired all 5 into docs-sidebar nav. Accurate to real A1-A5 endpoints. NOTE: the Haiku builder FALSELY reported '160 integration errors' (it had skipped setup_test_db -> DB-unavailable) and mislabeled them pre-existing; the conductor RE-RAN properly -> integration 236 green (0/0), unit 527 green, docs-only, php -l clean (independent re-run caught the bogus claim). [PRIOR cycle 37 II1 — DONE, see head]: iHymns REFERENCE INTEGRATION (SONNET; first-party github.com/MWBMPartners/iHymns — we CANNOT push there, so produce a self-contained REFERENCE IMPLEMENTATION in THIS repo under a docs/examples path, e.g. `_docs/integrations/ihymns/` or `web/public_html/docs/examples/ihymns/`): a worked 'Sign in with SIGNula.id' integration the iHymns team copies — (a) the client-registration values iHymns needs (public client for the PWA/native app + optional confidential for a server; the exact redirect_uris; scopes openid email profile); (b) the button embed (the assets/signin-button snippet with iHymns' client_id placeholder); (c) a WORKED redirect/callback handler (PHP + a JS/SPA variant) that reads code+state, validates state, POSTs code+code_verifier to /oauth/token, VERIFIES the id_token against /.well-known/jwks.json (iss=https://signula.id, aud=iHymns client_id, exp, nonce), reads the pairwise sub + email/profile from userinfo, and ESTABLISHES an iHymns session (map SIGNula sub -> iHymns local user, first-login provisioning); (d) native-app notes (system browser + PKCE, secure token storage, refresh); (e) a step-by-step README the iHymns team follows end to end. Accurate to the real endpoints + the button asset + the cycle-33/36 guides (cross-link them). It's a reference/example (not wired into SIGNula's own runtime) — keep it self-contained; if any PHP example file is included, it must php -l clean and NOT be a live SIGNula route. Docs/example only; gates stay green (527/236). THEN cycle 38 = clear the security follow-ups B-061 (getClientIP trusted-proxy — app-wide, own careful cycle) + B-064 (E_STRICT) + B-062 + B-063. THEN G-002 billing TEST-MODE (fix B-025 first; specs/G-002.md; NEVER live creds) -> G-004 compliance L1 (specs/G-004.md) -> CAP-API (#86 + #80 OpenAPI/Swagger). Migrations 001-033; next 034 only if needed. ===[PRIOR] cycle 35 DONE (IB1 'Sign in with SIGNula.id' BUTTON, Sonnet): partner drop-in under web/public_html/assets/signin-button/ — signula-signin.css (framework-free, namespaced .signula-btn; light/dark [prefers-color-scheme + [data-theme]]/brand-gradient/mono-CB variants; sizes; WCAG AA; focus-visible; reduced-motion) + signula-signin.js (dependency-free PKCE-S256 initiator: crypto.subtle SHA-256->base64url, random state+nonce, sessionStorage, redirect to /oauth/authorize-idp with data-* config; auto-wire + window.SignulaSignin.start() API; graceful on insecure context) + demo.html + README.md; added an inert button example to the cycle-33 partner guide. Icon = STATIC id-free brand-mark render (avoids SVG-id collisions on repeat paste). Palette = established #667eea->#764ba2 (owner re-tint trivial; surfaced non-blocking). Conductor INDEPENDENTLY verified: node --check OK + RFC 7636 Appendix-B S256 vector MATCH (crypto correct) + authorize-URL params correct + only assets dir + 1 docs page changed (no PHP logic) + gates unchanged (unit 527, integration 236). [PRIOR cycle 36 IG1-2 — DONE, see head]: INTEGRATION GUIDES (HAIKU, docs-only — extend the cycle-33 signin-with-signula.php + the assets/signin-button/README): write partner integration guides covering (1) QUICKSTART (register a client -> drop in the button -> handle the redirect -> exchange the code -> you're done), (2) WEB/PWA (the JS button drop-in + the redirect handler + token storage guidance + PWA notes), (3) NATIVE/MOBILE (authorization-code + PKCE with a system browser / ASWebAuthenticationSession / Android Custom Tabs, custom-scheme or https redirect_uri, no client secret = public client), (4) SERVER-SIDE / confidential client (client_secret_basic, server-side code exchange, refresh_token grant, token storage), (5) SECURITY BEST-PRACTICE (exact redirect_uri, mandatory S256 PKCE, validate state+nonce, verify id_token vs JWKS [iss=https://signula.id, aud=client_id, exp, pairwise sub], short-lived access tokens, revoke on logout). Match the existing docs page format (public-header/sidebar/public-footer like the sibling docs) + keep ACCURATE to A1-A5 + the real button asset. Place beside signin-with-signula.php (new pages OR sections). Docs-only; gates stay green (527/236). THEN cycle 37 = II1 iHymns REFERENCE INTEGRATION (SONNET; github.com/MWBMPartners/iHymns — first-party): a worked 'Sign in with SIGNula.id' integration reference for iHymns (web/PWA + native) — since we can't push to that repo, produce it as a reference implementation IN THIS repo under a docs/examples path (e.g. _docs/integrations/ihymns/ or public_html/docs/examples/ihymns/): the client-registration values, the button embed, the redirect/callback handler that exchanges the code + verifies the id_token + establishes the iHymns session, native-app notes, and a step-by-step README the iHymns team follows. Accurate to the real endpoints. THEN B-061 (getClientIP trusted-proxy) + B-064 (E_STRICT) + B-062/B-063 + link the docs pages into the docs nav. THEN G-002 billing TEST-MODE (fix B-025 first) -> G-004 compliance L1 -> CAP-API (#86 + #80). Migrations 001-033; next 034 only if needed. ===[PRIOR] cycle 34 DONE (B-068 + B-067 security fixes, Sonnet): repaired 2 non-functional security features. B-068a SessionGuard::getSalt() INSERT dropped the phantom `createdAt` column (tblSettings has none — it was throwing every time) -> the fingerprint salt now PERSISTS + is stable across requests (fingerprinting works; test proves it). B-068b SecurityAlertManager::notify() no longer `json_decode`s an already-decoded array (`is_array` guard) -> session-hijack alerting works; SAME double-decode also fixed in CaptchaVerifier + IPReputationChecker. B-067 mfa.php+notifications.php ActivityLogger::log named-arg fatals mapped to the real signature (description/metadata). +7 tests; unit 524->527, integration 229->236 green. IMPORTANT: the builder's return was GARBLED (never reported cleanly) so the conductor verified the ENTIRE change set FROM SCRATCH — all fixes present, php -l clean, both suites re-run green. ===>> Safety floor + account UI + red-team-passed+documented OAuth/OIDC provider + these security repairs all done. [PRIOR cycle 35 IB1 — DONE, see head]: 'Sign in with SIGNula.id' BUTTON (partner drop-in): reuse web/public_html/assets/brand/ (the cycle-20 SVG brand set) with the ESTABLISHED palette blue->purple #667eea->#764ba2 (this is the sensible default already used across the whole brand — do NOT hard-block the autonomous loop on a palette question; build with it and the conductor surfaces a non-blocking 'confirm/re-tint?' note to the owner in the status update; the named-layer SVGs make a re-tint trivial if they want teal). BUILD (Haiku for the CSS/markup, Sonnet for the JS): (a) a framework-free, drop-in 'Sign in with SIGNula.id' button — HTML+CSS with light/dark (both prefers-color-scheme AND [data-theme] hooks) + colour-blind-friendly variants, WCAG AA contrast, min 44px tap target, the SIGNula symbol (base64-SVG fallback for no-SVG clients per AvatarService's precedent), sizes/variants like the Google/Apple/Microsoft buttons; NO Bootstrap/FA dependency so partners can paste it anywhere; (b) a PKCE(S256) JS drop-in that: generates code_verifier + S256 code_challenge, builds state+nonce, stores them (sessionStorage), and redirects to /oauth/authorize-idp with the partner's client_id/redirect_uri/scope — a tiny, dependency-free snippet a partner drops in with their client_id. Place under web/public_html/assets/ (e.g. assets/signin-button/ with the css + js + a demo.html) + document usage. Add a demo/preview page. Keep it accurate to the A2-A4 endpoints. Tests where sensible (JS is hard to unit-test in PHPUnit — at least lint/validate + a demo that works; verify the generated authorize URL params are correct). Keep unit 527 + integration 236 green (no PHP behaviour change expected). THEN IG1-2 integration guides (Haiku: quickstart + web/PWA + native + server-side + security best-practice — build on the cycle-33 signin-with-signula.php guide), II1 iHymns reference integration (Sonnet; github.com/MWBMPartners/iHymns — web/PWA + native, a worked 'Sign in with SIGNula.id' integration). THEN B-061 (getClientIP trusted-proxy) + B-064 (E_STRICT) + B-062/B-063 + the guide-nav-link follow-up. THEN G-002 billing TEST-MODE (fix B-025 first) -> G-004 compliance L1 -> CAP-API (#86 + #80). Migrations 001-033; next 034 only if needed. ===[PRIOR] cycle 33 DONE (G-001 provider DOCS, Sonnet, docs-only): openapi.yaml +5 provider paths (/oauth/authorize-idp, /oauth/token, /oauth/userinfo, /oauth/revoke, /.well-known/openid-configuration) + 7 schemas + 2 security schemes (46->51 paths, YAML validates; jwks.json referenced not duplicated) — verified vs the real controllers; SECURITY_SETUP.md OIDC-Provider section (10 oidc.* settings + oauth.pairwise_salt, RS256 key/rotation shared w/ G-003, pairwise tblOAuthSubjects, + prominently the oidc.enabled master-switch OWNER ENABLE step: Backend DB->tblSettings->oidc.enabled=1); partner integration guide web/public_html/SIGNula.com/docs/signin-with-signula.php (client reg, auth-code+PKCE-S256 flow, id_token verify vs JWKS, userinfo, refresh+revoke, security reqs). Conductor re-validated YAML (51 paths) + confirmed NO source touched + gates unchanged (unit 524, integration 229). Minor follow-ups: guide not yet linked in docs-sidebar/index (reachable at /docs/signin-with-signula); verify admin/settings/*.php SessionManager ref vs the B-051 fix. ===>> G-001 OAuth/OIDC provider = BUILT + RED-TEAM-PASSED + DOCUMENTED. [PRIOR cycle 34 B-068/B-067 — DONE, see head]: FIX B-068 (SONNET, HIGH — security-first, BEFORE the button): two security features are non-functional as shipped. (a) `SessionGuard` fingerprint-salt persistence references a nonexistent `createdAt` column (find the real column on its table via information_schema — likely the table lacks it or uses a different name; fix the INSERT/SELECT so the fingerprint salt actually persists + session fingerprinting works). (b) `SecurityAlertManager::notify()` throws a `TypeError` on session-hijack detection — it calls `json_decode(<array>, …)` passing an array where a string is expected (find the call, pass the correct JSON string or skip the decode if already an array). VERIFY both by re-enabling `security.session_fingerprinting.enabled` and driving a session (fingerprint persists, no TypeError; a simulated hijack raises an alert cleanly). ALSO fold in B-067 (the same `ActivityLogger::log()` named-arg bug — `activityResult:`/`activityDetails:` -> the real `description:`/`success:` params — still on mfa.php + notifications.php POST handlers; fix so those POSTs don't fatal). Add regression tests (SessionGuard salt persists; SecurityAlertManager::notify on a hijack event succeeds; the mfa/notifications POST paths log without the named-arg fatal). Keep unit 524 + integration 229 green (+ new). THEN cycle 35 = IB1 'Sign in with SIGNula.id' BUTTON: Haiku builds the CSS/markup reusing web/public_html/assets/brand/ (light/dark/CB variants, WCAG, framework-free drop-in) + Sonnet builds the PKCE(S256) JS drop-in snippet that initiates the /oauth/authorize-idp flow — **PAUSE and ask the OWNER to sign off on the logo palette (blue->purple #667eea->#764ba2 vs teal) before finalizing button visuals** (this is the standing owner decision). THEN IG1-2 integration guides (Haiku: quickstart + web/PWA + native + server-side + security best-practice), II1 iHymns reference integration (Sonnet; github.com/MWBMPartners/iHymns — web/PWA + native). THEN B-061 (getClientIP trusted-proxy) + B-064 (E_STRICT) + B-062/B-063. THEN G-002 billing TEST-MODE (fix B-025 first) -> G-004 compliance L1 -> CAP-API (#86 + #80). Migrations 001-033; next 034 only if a fix needs schema. ===[PRIOR] cycle 32 DONE (B-066 — account UI works e2e, Sonnet): fixed the 7 remaining settings pages' own drifted-schema query bugs against the REAL schema (profile drop phantom isActive on tblOAuthAccounts; security/activity/index loggedAt->createdAt + activityResult/activityDetails->success/description; index tblOAuthTokens->tblUserOAuthTokens; connected-accounts retargeted missing tblUserLinkedAccounts->tblOAuthAccounts; privacy EAV tblUserPreferences + tblAPIKeys[no userID]->tblOAuthConsents join; usage a dozen column renames) + 2 stragglers (email-accounts->standard prologue; email-webhooks->shared shell + tblErrorLog cols). Builder also correctly ADDED missing CSRF to connected-accounts+privacy (state-changing POSTs had none) + defined 2 more undefined globals timeAgo()/isAdmin(). ALL 13 settings pages + admin/email-webhooks render HTTP 200 with full layout for a REAL Auth::login session (14/14 smoke). +16 tests; unit 524, integration 213->229 green (conductor re-ran + confirmed fixes landed [residual greps were comments] + smoke). No migration. Surfaced B-067 (ActivityLogger named-arg bug persists on mfa/notifications POST handlers, Med) + B-068 (HIGH — SessionGuard fingerprint-salt refs a nonexistent createdAt col + SecurityAlertManager::notify() TypeErrors on session-hijack via json_decode(array) -> both security features non-functional as shipped). ===>> The safety floor + account UI + the red-team-passed OAuth/OIDC provider are all working e2e. [PRIOR cycle 33 docs — DONE, see head]: G-001 PROVIDER DOCS (HAIKU — docs-only, no behaviour change): (1) extend web/public_html/api/docs/openapi.yaml with the OAuth2/OIDC provider paths — /oauth/authorize-idp (GET/POST consent), /oauth/token (grant_type=authorization_code + refresh_token; client_secret_basic/post/none), /oauth/userinfo (Bearer), /oauth/revoke (RFC7009), /.well-known/openid-configuration, and reference /.well-known/jwks.json — with request/response schemas + the security schemes (OAuth2 authorizationCode flow + PKCE note); validate the YAML parses (the file already validated at 46 paths in G-003 S5 — keep it valid). (2) SECURITY_SETUP.md: an OIDC-provider section documenting the oidc.* settings (issuer, authcode_ttl, id_token_ttl, access_ttl, require_pkce, allow_plain_pkce, pairwise_salt handling) + KeyManager key generation/rotation for the RS256 signing key + the pairwise-subject model + **the oidc.enabled master switch (default 0) and the exact owner step to enable the provider in production (tblSettings oidc.enabled=1)**. (3) a partner PROVIDER integration guide (a new doc under public_html/SIGNula.com/docs/ or _docs/ — match where the other guides live): how a relying party registers a client (partner-admin OAuth-clients page), the authorization-code+PKCE flow end-to-end against SIGNula's endpoints, verifying the id_token against JWKS, calling userinfo, refresh + revoke, and the security requirements (exact redirect_uri, S256 PKCE, state/nonce). Keep it accurate to what A1-A5 actually built. No code changes; unit 524 + integration 229 stay green (docs-only). THEN cycle 34 = IB1 'Sign in with SIGNula.id' BUTTON (Haiku CSS reusing web/public_html/assets/brand/ + Sonnet PKCE JS drop-in) — PAUSE for owner logo-palette sign-off before finalizing button visuals; then IG1-2 integration guides (Haiku), II1 iHymns reference integration (Sonnet). THEN the security follow-ups: B-068 (broken SessionGuard/SecurityAlertManager, HIGH) + B-061 (getClientIP trusted-proxy) + B-067 + B-064 (E_STRICT) + B-062/B-063. THEN G-002 billing TEST-MODE (fix B-025 first) -> G-004 compliance L1 -> CAP-API (#86 + #80). Migrations 001-033; next 034 only if needed. ===[PRIOR] cycle 31 DONE (B-060 + B-065 — account-UI plumbing, Sonnet): the 'hidden broken' cluster was 37 files (SIGNULA_ROOT undefined path-prefix across webhooks/webauthn-API/passwordless/checkout/pricing/admin/settings) + ~15 (requireLogin/isLoggedIn/getCurrentUser undefined). Fixed the SHARED plumbing ONCE (Option A): define('SIGNULA_ROOT', ROOT_DIR) + the 3 global auth helpers in config.php + created the missing web/private_html/layout/header.php+footer.php shell (logged-in top-right avatar/name/dropdown). Caught+fixed a coupled CRITICAL: web/_config/database.php had bytes before <?php -> headers_sent() true every request -> redirect() AND every security header (CSP/HSTS/X-Frame) silently NEVER sent app-wide; removed the leading whitespace. B-065 (folded in): settings pages read $_SESSION['userID'] but Auth set only user_id -> added the additive camelCase alias at all 4 establishment sites. Result: all 37 load past the plumbing; 5 owner-critical settings pages render FULLY (MFA/passkeys/notifications/notification-center/recovery-keys, real Auth::login over php -S). +8 tests; unit 524, integration 202->213 green (conductor re-ran both + verified headers_sent byte-0 + the session mismatch). Surfaced B-066 (HIGH). [PRIOR cycle 32 B-066 — DONE, see head]: (SONNET, HIGH — finish the account UI): the OTHER 7 settings pages now get PAST the shared breakage but 500 on their OWN column-name query bugs — fix each against the REAL schema (grep the actual migration columns): profile.php:283 queries tblOAuthAccounts WHERE isActive=1 (isActive is only on tblUserAvatars — use the right table/column or drop the predicate; profile.php is OWNER-CRITICAL = email/password change, do it FIRST), security.php:87 ORDER BY loggedAt on tblActivityLog (real col createdAt), + index/privacy/activity/connected-accounts/usage (same class — each has its own wrong column/table; verify against migrations). ALSO the 2 stragglers: settings/email-accounts.php requires a missing web/private_html/bootstrap.php + calls isUserLoggedIn()/getCurrentUserID() (define those or migrate it to the working pattern); admin/email-webhooks.php includes missing admin-header.php/admin-footer.php (create them mirroring header/footer, or point it at the shared shell). GOAL: all 13 settings pages + the 2 stragglers render with NO fatal/500 for a logged-in user (smoke each over php -S or the harness with a real Auth::login session; paste a per-page 200/render table). ADDITIVE per-page fixes; keep unit 524 + integration 213 green (+ coverage). Migrations 001-033; next 034 only if a page genuinely needs a column that SHOULD exist. THEN cycle 33 = G-001 docs (openapi.yaml oauth/oidc paths + SECURITY_SETUP.md oidc.* settings/rotation + partner PROVIDER guide; document oidc.enabled + owner enablement). THEN IB1 'Sign in with SIGNula.id' button (Haiku CSS reusing web/public_html/assets/brand/ + Sonnet PKCE JS) — PAUSE for owner logo-palette sign-off; IG1-2 guides (Haiku), II1 iHymns (Sonnet). THEN security follow-ups B-061 (getClientIP trusted-proxy)+B-064 (E_STRICT)+B-062/B-063. THEN G-002 billing TEST-MODE (fix B-025 first)->G-004 compliance L1->CAP-API (#86+#80). ===[PRIOR] cycle 30 DONE (G-001 A5 RED-TEAM + BLUE, Fable red-team + Sonnet fixes): an INDEPENDENT Fable 5 red-team attacked the whole provider surface across 13 vector families — VERDICT: NO Critical/High auth-bypass or token-forgery (crypto core HELD: alg-confusion/none/kid-traversal, redirect_uri exact-match, PKCE downgrade/strip, code replay+family-revoke, cross-client refresh [B-058 holds], userinfo aud/over-disclosure, pairwise-salt reversibility, jti/reuse, JWKS leak, SQLi/timing all REFUTED with PoCs). 5 findings; BLUE-fixed 4 in-surface (Sonnet), F-04->B-061: F-01 (HIGH — RP access-token sub was raw userID -> now PAIRWISE via new tblOAuthSubjects store [mig 033], resolved at userinfo by lookup; FIRST-PARTY tokens UNCHANGED sub=userID), F-03 (auth-code consume now client-bound `AND clientID=?` -> foreign client can't burn a victim's code), F-02 (refresh intersects scope with client's CURRENT allowedScopes), F-05 (oidc.enabled master switch now enforced on all 5 provider endpoints, NOT JWKS) + fixed a coupled regression (OAuthRevocationService cast sub to int -> FK-broke revocation). Red-team register in SECURITY.md; 3 exploit PoCs converted to regression tests asserting the attack is BLOCKED (_tests/Security/OAuthProviderRedTeamTest.php 7/7). Unit 524, integration 201->202 green; mig 033 clean (conductor re-ran both suites + PoC suite + reviewed the F-01 subject-store/first-party split + F-03 UPDATE + F-05 gating). New low/med follow-ups B-062 (subjectHash per-client uniqueness for public clients)/B-063 (mass-revoke reach for RP tokens)/B-064 (E_STRICT deprecation leaks bytes). ===>> G-001 PROVIDER (A1-A5) IS RED-TEAM-PASSED — feature-complete, hardened, independently attacked. ⚑ OWNER ACTION: set oidc.enabled=1 in production to turn the provider ON (default OFF for safety). [PRIOR cycle 31 B-060 — DONE, see head]: (SONNET, HIGH — same 'hidden broken' class as the safety floor): repair the 13 web/public_html/settings/ account-management pages that FATAL on load (they call an undefined requireLogin() and `include SIGNULA_ROOT . 'layout/header.php'`/footer.php where SIGNULA_ROOT is never defined + those files don't exist — the owner-required account UI [profile, email/password, MFA, passkeys, privacy, notifications, connected-accounts, email-accounts, activity] is completely broken). Fix pattern: COPY what cycle-28 connected-apps.php did (it works) — use the real `Auth::` gate (Auth::requireAuth / Auth::isAuthenticated + redirect to /login) instead of requireLogin(); use the defined `ROOT_DIR` constant instead of SIGNULA_ROOT; include the real existing `web/private_html/layout/settings-sidebar.php` (NOT the nonexistent header/footer) inside a self-contained page shell. EITHER (preferred, least churn) define a real global `requireLogin()` helper + define `SIGNULA_ROOT` (aliased to ROOT_DIR) + create the missing `web/private_html/layout/header.php`+`footer.php` (a proper shared shell) so ALL 13 pages work unchanged, OR migrate each of the 13 pages to the connected-apps.php pattern. Pick whichever is cleaner/least-risk and justify. SMOKE all 13 pages load past their requires with NO fatal (php -l + a php-load of each with SIGNULA_INIT + config, asserting no 'undefined function requireLogin'/'undefined constant SIGNULA_ROOT'/'failed opening required'). Add coverage. Keep unit 524 + integration 202 green. THEN cycle 32 = G-001 docs (openapi.yaml oauth/oidc paths [authorize/token/userinfo/revoke/discovery] + SECURITY_SETUP oidc.* settings + key rotation + a partner PROVIDER integration guide; note oidc.enabled + owner setup). THEN IB1 'Sign in with SIGNula.id' button (Haiku CSS reusing web/public_html/assets/brand/ + Sonnet PKCE JS) — PAUSE for owner logo-palette sign-off before finalizing button visuals; IG1-2 guides (Haiku), II1 iHymns reference (Sonnet); then B-061 (getClientIP trusted-proxy), B-064 (E_STRICT); then G-002 billing TEST-MODE, G-004 compliance, CAP-API #86. Migrations 001-033; next 034 only if needed. ===[PRIOR] cycle 29 DONE (G-001 A5 hardening, Sonnet): fixed the 3 queued findings before the red-team. B-058 (HIGH): TokenService::refresh(refreshToken, ?authenticatedClientID) now enforces client-binding via `$authenticatedClientID !== $clientID` placed BEFORE reuse-detection/spend — covers all 4 cases (first-party<->first-party OK; cross-client, RP-token-at-first-party-endpoint, first-party-token-at-RP-endpoint all REJECT with a MED security alert; NO family-revoke on a pure authz mismatch to avoid a replay-DoS); first-party /api/v1/auth/refresh now refuses RP-bound tokens; refresh minting gated behind offline_access (mintPair withRefresh lever); WIRED the refresh_token grant into /oauth/token (client-bound) + advertised in discovery. B-059: rate-limit (SecurityUtils::checkRateLimit per-IP + per-client) on /oauth/token + /oauth/revoke (429+Retry-After, reused jwt.rate_limit.* buckets, no migration). B-057: sanitizeRedirectUrl() on the MFA redirect_after_login at set+consume time (is_scalar guards); added a verbatim sanitize stub to _tests/bootstrap.php (conductor functionally verified it rejects https:/// //  /javascript:). +22 tests; unit 524, integration 179->201 green (conductor reviewed binding placement + offline_access gating + re-ran both). ===>> G-001 A1-A5(hardening) done; provider surface is feature-complete + hardened. [PRIOR cycle 30 red-team — DONE/PASSED, see head]: G-001 A5 MANDATORY RED-TEAM (this is a SECURITY GATE — do NOT skip). Spawn ONE INDEPENDENT red-team agent (model: **Fable 5** per model_policy for red-team reasoning; fallback **Opus** if Fable unavailable — check availability) that ATTACKS (does not build) the WHOLE live OAuth2/OIDC provider surface: /oauth/authorize-idp, /oauth/token (authorization_code + refresh_token grants), /oauth/userinfo, /oauth/revoke, the id_token, JWKS. Vectors (produce a concrete PoC or a reasoned refutation for EACH): alg-confusion (RS256->HS256/none/unknown-kid), signature/kid tamper, redirect_uri exact-match bypass (substring/trailing-slash/encoding/case/userinfo-in-authority), PKCE downgrade/strip/plain-injection/verifier-mismatch, auth-code replay (verify the atomic consume + family-revoke holds) / injection / fixation / cross-client redemption, IdP mix-up + code cut-and-paste, consent bypass / scope escalation (request more at token/userinfo than consented; verify scope is bound to the code), CROSS-CLIENT TOKEN + REFRESH redemption (verify the B-058 binding fix actually holds — try client B redeeming client A's refresh at /oauth/token and at /api/v1/auth/refresh), userinfo aud-confusion + scope OVER-DISCLOSURE (can a token get claims beyond its scope? can a first-party/at+jwt token hit userinfo?), open-redirect (verify B-057 on the MFA path + any authorize error redirect), PAIRWISE sub reversibility / cross-client correlation (can two colluding clients link the same user? is sub guessable without the salt?), jti/refresh reuse, rate-limit bypass (paced spray, IP spoof via XFF — note getClientIP trust). Also probe the A5 focus items flagged by the builder: exchangeRefreshToken doesn't re-verify granted scope vs the client's current allowed set; the binding-mismatch alert type (TYPE_SESSION_HIJACK) noise-filtering; offline_access-gating assumptions (any code assuming a refresh row per access token). PROTOCOL: the red-team returns a ranked findings list (severity + PoC + fix sketch). For each CONFIRMED real bypass: a SONNET builder fixes it (BLUE), then the SAME red-team (or a fresh independent pass) RE-ATTACKS to verify closed (VERIFY). If the surface HOLDS across all vectors (no real bypass), that is a valid PASS — record it. Keep unit 524 + integration 201 green (+ any regression/PoC tests). ONE commit for the cycle (BLUE fixes + red-team report into SECURITY.md/PROJECT.md; if zero bypasses, commit the red-team PASS record + any hardening test additions). THEN cycle 31 = B-060 (SONNET, HIGH — repair the 13 broken settings/ account pages). THEN cycle 32 = A5 docs (openapi.yaml oauth/oidc paths + SECURITY_SETUP oidc.* + partner PROVIDER guide). THEN IB1 'Sign in with SIGNula.id' button (Haiku CSS reusing web/public_html/assets/brand/ + Sonnet PKCE JS) — PAUSE for owner logo-palette sign-off; IG1-2 guides (Haiku), II1 iHymns (Sonnet); then G-002 billing TEST-MODE, G-004 compliance, CAP-API #86. Migrations 001-032; next 033 only if needed. ===[PRIOR] cycle 28 DONE (G-001 A4, Sonnet): SIGNula is now a DISCOVERABLE OIDC provider. Built /oauth/userinfo (OAuthUserInfoService — auth = untrusted-peek of the token's aud -> TokenService::verifyAccessToken(['aud'=>peeked]) [RS256 sig proves WE minted it; first-party/forged tokens refused] -> claims GATED strictly by granted scope [always sub=pairwise computeSubject matching the id_token; email/email_verified only for 'email'; profile claims only for 'profile'; NO over-disclosure]); /.well-known/openid-configuration (directory+index.php+.htaccess dir-index trick like jwks.json; advertises real endpoints, pairwise+public, RS256, S256, authorization_code only [refresh_token withheld pending B-058]); RFC7009 /oauth/revoke (OAuthRevocationService reusing authenticateClient; revokes ONLY the authenticated client's own tokens — cross-client revoke is a no-op 200; + TokenService::revokeClientForUser); Connected-Apps settings page (lists active tblOAuthConsents, CSRF per-app revoke -> revokedAt + kills that client's token family via OAuthAuthorizeService::revokeConsent). No migration. +6 unit +23 integration; unit 518->524, integration 156->179 green (conductor reviewed no-over-disclosure gating + cross-client-revoke ownership + peek-then-verify + re-ran both). Surfaced B-060 (HIGH: 13 settings/ account-management pages fatal on load via undefined requireLogin()/SIGNULA_ROOT + missing header/footer — the owner-required account UI is broken; connected-apps.php avoided the broken pattern). ===>> G-001 A1-A4 built: the full OAuth2/OIDC provider surface (register client -> authorize+consent+PKCE -> token+id_token -> userinfo+discovery+revoke) works e2e with pairwise subjects. NEXT = [PRIOR cycle 29 A5-hardening — DONE, see head]: (i) B-058 — TokenService::refresh() must require+verify client auth when the row's clientID is non-null (confidential: verifyClientSecret; reject cross-client redemption), and gate refresh minting behind offline_access; THEN wire the refresh_token grant into /oauth/token (client-authenticated, rotation+reuse-detection) AND add grant_types_supported+=refresh_token to discovery. (ii) FIX B-059 — RateLimiter on /oauth/token + /oauth/revoke (per-IP + per-client). (iii) FIX B-057 — sanitizeRedirectUrl() the MFA redirect_after_login (Auth.php:377 set-time + mfa/verify.php consume-time). Add integration tests for each. Keep unit 524 + integration 179 green. THEN cycle 30 = G-001 A5 MANDATORY RED-TEAM — spawn an INDEPENDENT red-team agent (Fable 5 per model policy; fallback Opus) to attack the WHOLE provider surface (/oauth/authorize-idp, /oauth/token, /oauth/userinfo, /oauth/revoke, id_token/JWKS): alg-confusion (RS256->HS256/none), redirect_uri exact-match bypass, PKCE downgrade/strip/plain-injection, auth-code replay/injection/fixation, IdP mix-up + cut-and-paste, consent/scope escalation, cross-client token+refresh redemption (post-B-058), userinfo aud-confusion + scope over-disclosure, open-redirect (incl B-057), pairwise sub reversibility/cross-client correlation, jti/refresh reuse. Produce PoCs; FIX any real bypass (BLUE) then re-attack (VERIFY). THEN cycle 31 = B-060 (SONNET, HIGH — same class as safety floor): repair the 13 settings/ account-management pages (define a real requireLogin or migrate to Auth::requireAuth; define SIGNULA_ROOT/use ROOT_DIR; provide the missing layout header/footer or a self-contained shell like connected-apps.php); smoke each of the 13 loads with no fatal; the owner-required account UI (profile/email/password/MFA/passkeys/privacy) must work e2e. THEN cycle 32 = A5 docs (openapi.yaml + the oauth/oidc paths; SECURITY_SETUP oidc.* settings + key rotation; a partner PROVIDER integration guide). THEN IB1 'Sign in with SIGNula.id' button (Haiku CSS reusing web/public_html/assets/brand/ + Sonnet PKCE JS) — PAUSE for owner logo-palette sign-off before finalizing button visuals; IG1-2 guides (Haiku), II1 iHymns reference (Sonnet); then G-002 billing TEST-MODE, G-004 compliance, CAP-API #86. Migrations 001-032; next 033 only if needed. ===[PRIOR] cycle 27 DONE (G-001 A3, Sonnet): built the OAuth2/OIDC TOKEN endpoint /oauth/token (web/public_html/oauth/token.php thin controller + web/private_html/auth/OAuthTokenService.php engine) — exchanges an A2 auth code for an access token + OIDC id_token (+ refresh). Applied the 3 G-003 gotcha fixes: (a) new Jwt::signIdToken() [header typ=JWT, NO scope claim, iss=oidc.issuer, aud=client_id forced, exp=oidc.id_token_ttl]; (b) new TokenService::issueForClient() [access token aud=client_id + refresh row records clientID; threaded optional clientID through mintPair, INSERT+bind-types 'ississss' extended additively so first-party issueTokens is unchanged/clientID NULL]; (c) refresh() now SELECTs clientID + re-derives aud so a rotated RP token keeps aud=client_id. Security (conductor adversarially reviewed): atomic single-use (UPDATE consumedAt WHERE consumedAt IS NULL + getAffectedRows()===1), replay->family-scoped revoke (new tblOAuthAuthCodes.issuedFamilyID, migration 032, links code->exact family it minted + security alert), PKCE-S256 constant-time (hash_equals on base64url(sha256(verifier))), exact redirect_uri (hash_equals), code<->client binding, expiry, Cache-Control:no-store, RS256 via the foundation; sub=PAIRWISE per client (computeSubject). +3 unit +16 integration (id_token verifies iss/sub[pairwise, diff per client]/aud/nonce/auth_time; replay rejected + first tokens revoked; expired/bad-PKCE/redirect-mismatch/cross-client/bad-secret rejected; public client no secret). Unit 515->518, integration 140->156 green; migration 032 clean (conductor re-ran both + reviewed the consume/replay/PKCE/bind-type diffs). Surfaced B-058 (HIGH: refresh() lacks client-binding -> the refresh_token GRANT was deliberately NOT wired into /oauth/token yet [authorization_code only]; fix refresh() to require+verify client auth when the row's clientID is non-null before shipping the grant) + B-059 (Low: no rate-limit on /oauth/token). [PRIOR cycle 28 A4 — DONE, see head]: /userinfo + discovery + revocation + Connected-Apps: (1) GET/POST /oauth/userinfo (JSON handler; Bearer access token verified via TokenService::verifyAccessToken; aud MUST equal the token's client; return claims GATED by the token's granted scope — sub[PAIRWISE via computeSubject, MUST match the id_token's sub], + email/email_verified for 'email', name/given_name/family_name/etc for 'profile'; 401 with WWW-Authenticate on bad/expired token); (2) /.well-known/openid-configuration discovery doc (standalone handler using the SAME directory-index trick as .well-known/jwks.json/index.php — a directory + index.php + .htaccess; advertise issuer=oidc.issuer, authorization_endpoint=/oauth/authorize-idp, token_endpoint=/oauth/token, userinfo_endpoint=/oauth/userinfo, jwks_uri=/.well-known/jwks.json, response_types_supported=[code], grant_types_supported=[authorization_code(,refresh_token when B-058 fixed)], subject_types_supported=[pairwise,public], id_token_signing_alg_values_supported=[RS256], scopes_supported, code_challenge_methods_supported=[S256], claims_supported); (3) token/consent REVOCATION — RFC7009 /oauth/revoke for RP tokens (revoke access jti / refresh family; require client auth) AND a user-facing 'Connected Apps' management view under public_html/settings/ (list tblOAuthConsents where revokedAt IS NULL with client name/logo/scopes/granted date; a revoke action that sets tblOAuthConsents.revokedAt=NOW() AND revokes the user's active token families for that client via TokenService). Reuse Auth:: + CSRF + the settings-page layout. Integration tests: userinfo returns scope-gated claims with the matching pairwise sub + rejects wrong-aud/expired/insufficient-scope; discovery doc is valid JSON advertising the real endpoints; revoke kills the consent + the token family (subsequent userinfo with that token 401s). Keep unit 518 + integration 156 green. Migrations 001-032; next 033 only if needed. THEN cycle 29 = A5 = hardening + fix B-058 (refresh client-binding) + B-059 (rate-limit /oauth/token) + B-057 (MFA open-redirect) + MANDATORY RED-TEAM (Fable/Opus: alg-confusion, redirect exact-match, PKCE downgrade/strip, code replay/injection, mix-up, consent/scope escalation, open-redirect, cross-client token/refresh redemption, pairwise sub non-reversibility) + provider docs. THEN IB1 'Sign in with SIGNula.id' button (Haiku CSS reusing web/public_html/assets/brand/ + Sonnet PKCE JS) — PAUSE for owner logo-palette sign-off; IG1-2 guides (Haiku), II1 iHymns (Sonnet); then G-002 billing TEST-MODE, G-004 compliance, CAP-API #86. ===[PRIOR] cycle 26 DONE (G-001 A2, Sonnet): built the OAuth2/OIDC AUTHORIZATION endpoint (SIGNula-as-IdP) on A1's model — new web/public_html/oauth/authorize-idp.php (thin GET/POST controller at /oauth/authorize-idp via the existing .htaccess clean-URL rewrite; deliberately a NEW file, does NOT clobber the RP-side oauth/authorize.php) + testable engine web/private_html/auth/OAuthAuthorizeService.php (validateAuthorizeRequest / issueAuthorizationCode / hasCoveringConsent / buildRedirectUrl). Security (conductor adversarially reviewed the #1 OAuth-IdP surface): EXACT-match redirect_uri with FATAL-before-redirect ordering (unknown/inactive client + redirect_uri mismatch => LOCAL error page, never a redirect; only after redirect_uri is a proven exact match do protocol errors 302 back with state), MANDATORY PKCE-S256 (rejects missing/plain for public/pkceRequired clients), CSRF on the consent POST, NEVER-TRUST-POST (full re-validation from $_POST; tampered redirect_uri re-fails to the local page), fail-closed deny, single-use auth code stored HASHED (sha256 -> tblOAuthAuthCodes.codeHash; raw code only in the redirect), state echoed everywhere, consent persisted in tblOAuthConsents (auto-skip on covering consent). Return-after-login uses the relative /login?redirect= pattern. +26 unit +11 integration; unit 489->515, integration 129->140 green (conductor re-ran both + reviewed redirect-ordering/PKCE/CSRF/never-trust-POST). Surfaced B-057 (MED open-redirect on the MFA login path: Auth.php:377 stores $_GET['redirect'] unsanitized -> mfa/verify.php redirects to it; NON-MFA path sanitizes; pre-existing, queued for A5 hardening). NO token issuance yet. [PRIOR cycle 27 A3 detail — DONE, see head]: /oauth/token + id_token — exchange the auth code for tokens at a /oauth/token endpoint (place per the app's URL convention; likely web/public_html/oauth/token.php as a JSON handler — mirror the JWKS/api handlers). Requirements: accept grant_type=authorization_code (+ later refresh_token); authenticate the client (confidential = client_secret via OAuthClientManager::verifyClientSecret [Basic auth header or POST body]; public = no secret, PKCE only); look up the code by hash('sha256',code) in tblOAuthAuthCodes; ENFORCE single-use ATOMICALLY (UPDATE ... SET consumedAt=NOW() WHERE codeHash=? AND consumedAt IS NULL; getAffectedRows()===1 else REPLAY -> reject AND revoke any tokens already issued for that code, per RFC6749 10.5); verify not expired; verify redirect_uri EXACTLY matches the one bound to the code; PKCE: verify base64url(sha256(code_verifier)) === stored codeChallenge (S256); scopes come from the code. Then MINT: an access token + an OIDC id_token via the G-003 Jwt/TokenService foundation — APPLY THE 3 GOTCHA FIXES: (1) add Jwt::signIdToken() that sets header typ=JWT and does NOT put scope in the id_token; (2) TokenService::issueForClient() must populate clientID + aud=client_id on the access token; (3) refresh must PRESERVE clientID/aud. id_token claims: iss=oidc.issuer(https://signula.id), sub=OAuthClientManager::computeSubject(userID,client) [PAIRWISE], aud=client_id, iat/exp(oidc.id_token_ttl), auth_time (from code.authTime), nonce (from code.nonce if present), plus standard profile claims per granted scopes. Issue a refresh_token (rotation + reuse-detection via the existing TokenService) if offline_access/refresh grant allowed. Return the RFC6749 token JSON (access_token, token_type=Bearer, expires_in, id_token, refresh_token?, scope). Integration tests: happy exchange (code->tokens; id_token verifies against JWKS with correct iss/sub[pairwise]/aud/nonce/auth_time; access token has aud=client_id); code REPLAY rejected + prior tokens revoked; expired code rejected; wrong/missing PKCE code_verifier rejected; redirect_uri mismatch rejected; confidential client bad secret rejected; public client needs no secret. Keep unit 515 + integration 140 green. Migrations 001-031; next 032 only if needed (A1 tables likely suffice). ===[PRIOR] cycle 25 DONE (safety floor CLOSED, Sonnet): FIXED B-053 — Auth::getCurrentUser() now enriches the current-user array with the 4 admin/partner keys the 50 sites gate on: isSuperAdmin (REAL tblUsers column from migration 009, added to SELECT + cast bool), partnerID (primary active tblPartnerTeamMembers membership — root-preferred, else lowest id, null if none, via one extra prepared query), isPartnerAdmin (has-active-membership), isAdmin (isSuperAdmin || has-membership). ADDITIVE + null-safe + enriched-array cached; deliberately NO AccessControl wire-in (dodges an Auth<->SessionManager<->AccessControl cycle). 4 new integration persona tests; unit 489, integration 125->129 green (conductor reviewed the diff [prepared/additive/null-guarded] + re-ran both suites). ===>> SAFETY FLOOR CLOSED: the whole cluster cycle-21 opened — B-051 (SessionManager) + B-052 (settings load) + B-055 (login session) + B-054/B-056 (six missing _backend/* files) + B-053 (admin flags) — is fully fixed; the admin/partner UI + login now work e2e (a big chunk of the owner's 'fully working SIGNula.id' goal). [PRIOR] cycle 24 DONE (safety floor, Sonnet): FIXED B-054 + B-056 — the 74 admin/partner pages required SIX never-committed web/_backend/*.php files (B-051 fixed SessionManager; this cycle fixed the rest, so the admin/partner UI finally loads). B-054: the pages call Database::getInstance()->prepare(...) (raw-mysqli style, 262 sites) but the real Database class is 100%-static with no getInstance(), and a 2nd 'class Database' would redeclare-fatal against config.php's -> added getInstance() to the EXISTING static class returning a NEW DatabaseConnection adapter (proxies the shared mysqli via __call/__get + real getConnection()); web/_backend/Database.php is a collision-free require-shim (declares no class). B-056 (surfaced mid-cycle, FOLDED IN so the cycle delivers the whole 'pages load' outcome): 4 MORE missing _backend shims — ActivityLogger (10 dependents), APIKeyManager, RateLimiter, ServiceFeeManager (real classes under private_html/) — all created as guarded require-shims; all 4 real classes have zero internal requires (no deeper rabbit hole). ALL 7 _backend/* requires now resolve; conductor INDEPENDENTLY verified all shims declare no class + the full bootstrap chain (config->Database->SessionManager->AccessControl->ActivityLogger->...) loads with no fatal + re-ran both suites. Unit 481->489, integration 120->125 green. ADMIN/PARTNER UI + LOGIN NOW WORK E2E (B-051+B-052+B-055+B-054+B-056 together). [cycle 23 done: B-055 login-session bind fix. cycle 22 done: B-052 loadSettings resilience + B-051 SessionManager facade + AccessControl stale-column fix.] LAST open safety-floor HIGH item before resuming G-001 A2 = B-053 (now DONE, see head). [cycle 25 B-053 detail, DONE]: confirm which of isSuperAdmin/partnerID are real tblUsers columns vs which of isAdmin/isPartnerAdmin are DERIVED (AccessControl), then extend Auth::getCurrentUser() SELECT for the real columns + populate the derived flags so getUserInfo() carries the admin flags the 50 sites gate on. After this floor the admin/partner UI + login actually work e2e (delivers a big chunk of 'fully working SIGNula.id'). [PRIOR cycle 26 A2 detail — DONE, see head]: /oauth/authorize-idp + PKCE(S256) + consent screen — build the authorization endpoint on top of A1's tblOAuthClients/OAuthClientManager. Requirements: require an authenticated SIGNula session (Auth::requireAuth / redirect to login preserving the request); validate client_id (OAuthClientManager::getClient), EXACT-match redirect_uri (OAuthClientManager::isExactRedirectMatch — never substring), response_type=code, requested scopes ⊆ client.allowedScopes; MANDATORY PKCE — require code_challenge + code_challenge_method=S256 (reject plain/missing for public clients per A1 pkceRequired); render a consent screen (Auth:: directly, CSRF-protected, honoring prior tblOAuthConsents to allow skip); on approve, mint a single-use, short-TTL authorization code into tblOAuthAuthCodes bound to (clientID,userID,redirect_uri,scope,code_challenge,S256) and 302 back to redirect_uri with code+state; on deny, redirect with error=access_denied+state. Errors follow RFC6749 (invalid_request/unauthorized_client/invalid_scope) — but NEVER redirect to an unvalidated redirect_uri. NO token issuance yet (A3). Integration tests: happy path issues a code; bad redirect_uri/invalid client/missing-PKCE/over-broad-scope all rejected; consent persists. Migrations 001-031; next 032 only if A2 needs schema (A1's tables likely suffice).Then A3 /oauth/token+id_token (Sonnet — 3 gotcha fixes [Jwt::signIdToken typ=JWT, TokenService::issueForClient populates clientID/aud, refresh preserves] + PAIRWISE sub via OAuthClientManager::computeSubject), A4 /userinfo(aud=client_id)+/.well-known/openid-configuration+revoke, A5 hardening+MANDATORY RED-TEAM(Fable/Opus); then IB1 'Sign in with SIGNula.id' button (Haiku CSS reusing web/public_html/assets/brand/ + Sonnet PKCE JS), IG1-2 guides (Haiku), II1 iHymns (Sonnet). Model policy: Fable plan / Sonnet-Haiku impl / SEQUENTIAL. Migrations 001-031; next 032. [cycle 20] cycle 20 DONE (IB0 SIGNula.id LOGO, Sonnet): animated symbol (key->lock + checkmark) + wordmark 'SIGNula.id' + lockup, all named-layer SVG, light/dark/colour-blind + Base64 fallback; palette blue->purple #667eea->#764ba2 (CB-safe, WCAG); wired 404 favicons + 8-size PWA icons; preview at web/public_html/assets/brand/preview.html. OWNER FOLLOW-UPS: approve palette (vs teal); wordmark is live <text> not outlined; maskable safe-zone first-pass. NEXT = cycle 21 = G-001 A1 (SONNET, brand-agnostic): OAuth2/OIDC client-registration data model + admin per specs/G-001.md + specs/G-001-integration-plan.md — migration 031 (tblOAuthClients [client_id, HASHED client_secret, exact-match redirect_uri allowlist, allowed_scopes, grant_types, pkce_required, isFirstParty, owner partnerID, + subject_type + sector_identifier for PAIRWISE per owner] + tblOAuthAuthCodes + tblOAuthConsents + tblOAuthAccessGrants; BIGINT UNSIGNED FKs; + a global pairwise salt setting) + OAuthClientManager + partner-admin client management; unit+integration tests. NO /oauth/authorize|token yet. Then A2 /oauth/authorize-idp+PKCE(S256)+consent (Sonnet), A3 /oauth/token+id_token (Sonnet — 3 gotcha fixes [Jwt::signIdToken typ=JWT, TokenService::issueForClient populates clientID/aud, refresh preserves them] + PAIRWISE sub=hash(sector||local_user_id||salt)), A4 /userinfo+/.well-known/openid-configuration+revoke (Sonnet; userinfo aud=client_id), A5 hardening+MANDATORY RED-TEAM(Fable/Opus)+docs; then IB1 'Sign in with SIGNula.id' button (Haiku CSS reusing the logo + Sonnet PKCE JS), IG1/IG2 guides (Haiku), II1 iHymns reference (Sonnet). Model policy: Fable plan / Sonnet-Haiku impl / SEQUENTIAL. Migrations 001-030; next 031. [cycle 19] cycle 19 DONE (Fable 5 planning): deep-planned the partner integration layer (#87) -> .dev-team/specs/G-001-integration-plan.md — 11 model-tagged stages (A1-A5 OAuth2/OIDC provider / IB0-2 'Sign in with SIGNula' button / IG1-2 integration guides / II1 iHymns reference); 3 G-003 gotcha fixes noted for A3. MODEL POLICY (user 2026-07-02): Fable for planning, Sonnet/Haiku for implementation (Opus only if necessary), SEQUENTIAL (no parallel agents). NEXT = cycle 20 = G-001 A1 (SONNET; brand-agnostic — proceed regardless of the pending branding decisions): client-registration data model + admin per specs/G-001.md + specs/G-001-integration-plan.md — migration 031 (tblOAuthClients [client_id, client_secret HASHED, EXACT-MATCH redirect_uri allowlist, allowed_scopes, grant_types, pkce_required, isFirstParty, owner partnerID] + tblOAuthAuthCodes + tblOAuthConsents + tblOAuthAccessGrants; BIGINT UNSIGNED FKs) + OAuthClientManager (register/rotate-secret/validate-redirect/list) + a partner-admin client-management path; unit + integration tests. NO /oauth/authorize or /oauth/token endpoints yet (A2/A3). Subsequent stages: A2 /oauth/authorize-idp + PKCE(S256) + consent (Sonnet), A3 /oauth/token + id_token (Sonnet — apply the 3 gotcha fixes: Jwt::signIdToken with typ=JWT no-scope, TokenService::issueForClient populating clientID/aud, refresh preserving them), A4 /userinfo + /.well-known/openid-configuration + revoke (Sonnet), A5 hardening + MANDATORY RED-TEAM (Fable/Opus) + provider docs; then IB0 brand mark (Haiku), IB1 button (Haiku CSS + Sonnet PKCE JS), IG1/IG2 guides (Haiku), II1 iHymns (Sonnet). PENDING owner decisions (being asked): brand spelling SIGNula vs SIGNulo (iss claim + button label + docs), placeholder brand-mark approval, subject_type public vs pairwise. Migrations 001-030; next 031. [PRIOR] cycle 18 DONE: G-003 S5 docs",
  "_next_action_orig": "cycle 18 DONE: G-003 S5 docs (SECURITY_SETUP jwt.* + KeyManager rotation + security properties; openapi.yaml +4 auth paths/+7 schemas/bearerAuth, 42->46 paths validates; CHANGELOG). ===G-003 JWT API AUTH FULLY BUILT (S1-S5, gate_ledger status=built) — first approved epic (#41) complete.=== NEXT = cycle 19 = G-001 IdP Phase A STAGE A1 (read .dev-team/specs/G-001.md; REUSES the G-003 Jwt/KeyManager/JWKS signing foundation): OAuth2/OIDC PROVIDER, stage A1 = client registration data model + admin — migration 031 tblOAuthClients (client_id, client_secret HASHED like tblAPIKeys.keyHash, redirect_uri allowlist, allowed_scopes, grant_types, owner partnerID, PKCE-required flag) + tblOAuthAuthCodes + tblOAuthConsents + tblOAuthAccessGrants per the spec's §data-model; a client-registration/management path (RP clients owned by tblPartners); unit + integration tests. NO authorize/token endpoints yet (later A-stages). Then A2 /oauth/authorize + PKCE code issuance + consent, A3 /oauth/token (authorization_code+PKCE+refresh, reuses Jwt for id_token+access), A4 /oauth/userinfo + /.well-known/openid-configuration + JWKS reuse + revocation, A5 hardening + RED-TEAM (redirect-uri exact-match, PKCE downgrade, code replay, mix-up, consent/scope bypass) + docs. One stage per cycle, one commit per stage, checker + security review + integration gate. THEN G-002 billing TEST-MODE (fix B-025 first; specs/G-002.md; NEVER live creds) -> G-004 compliance L1 (specs/G-004.md) -> CAP-API #86. Migrations 001-030; next 031. [PRIOR] cycle 17 DONE: G-003 S4 MANDATORY red-team on the JWT auth entrypoint — 12 vectors, core HELD (alg-confusion/none, tamper, key-confusion, replay, refresh-reuse, temporal, aud/iss, IDOR-on-issuance, scope, JWKS, X-API-Key); found+FIXED+re-attacked-closed 2 bypasses: kid path-traversal->forgery [HIGH] (isValidKid strict-charset guard + basename) + B-049 spray-able per-second rate-limit [MED] (windowed SUM) + fixed broken getUserByApiKey; unit 442->444, integration 90/90. NEXT = cycle 18 = G-003 STAGE S5 (docs — FINAL stage, docs-only no behaviour change): document the JWT auth in _docs/setup/SECURITY_SETUP.md (the jwt.* policy settings, KeyManager key generation/rotation, admin config) + add the /api/v1/auth/{token,refresh,revoke} + /.well-known/jwks.json endpoints to the OpenAPI spec web/public_html/api/docs/openapi.yaml (request/response schemas + a bearerAuth security scheme) + a short client usage note; also update CHANGELOG. Keep unit 444 + integration 90/90 green. THEN set gate_ledger G-003 status='built' (all stages S1-S5 done). THEN G-001 IdP Phase A (specs/G-001.md, reuses this JWT signing foundation) -> G-002 billing TEST-MODE (fix B-025 first; specs/G-002.md; assertTestMode fail-closed; NEVER live creds) -> G-004 compliance L1 (specs/G-004.md) -> CAP-API capstone (#86). Migrations 001-030; next 031. [PRIOR] cycle 16 DONE: G-003 S3 endpoints + Bearer wiring built (POST /api/v1/auth/{token,refresh,revoke} + GET /.well-known/jwks.json via JwtAuthController; BaseController::getUserByToken now verifies Bearer via TokenService alongside X-API-Key; primary-credential authz on issuance — subject from verified identity only, cross-user mint denied; rate-limited; integration 90/90 green, unit 442; fixed 2 pre-existing API-wide bugs: Router never injected itself into controllers [B-048, dispatch fataled] + APIKeyMiddleware 401'd Bearer). NEXT = cycle 17 = G-003 STAGE S4 = MANDATORY RED-TEAM (live auth entrypoint), per .dev-team/specs/G-003.md: spawn an INDEPENDENT red-team agent to attack /api/v1/auth/* + Bearer verify — alg confusion (RS256->HS256/none), signature/kid manipulation, key confusion, token replay, refresh reuse (must revoke family), IDOR/privilege on issuance (mint for another user must FAIL), scope escalation, rate-limit bypass (B-049 per-second-window weakness), JWKS private-key leak, expired/tampered acceptance, X-API-Key regression. Produce PoCs; fix any real bypass (BLUE) then re-attack (VERIFY). Address B-049 (windowed limiter for /auth/token) if red-team confirms exploitable. THEN S5 = docs + OpenAPI stub for the new endpoints. THEN G-001 IdP Phase A (specs/G-001.md, reuses this signing) -> G-002 billing TEST-MODE (fix B-025 first; specs/G-002.md) -> G-004 compliance L1 (specs/G-004.md) -> CAP-API #86. Migrations 001-030; next 031. [PRIOR] cycle 15 DONE: G-003 S2 TokenService built — access+opaque-refresh issuance, atomic refresh rotation + REUSE-DETECTION (spent token revokes whole family + HIGH session_hijack alert), revoke/revokeAllForUser (tokensInvalidBefore), jti-denylist+cutoff wired into verifyAccessToken; integration 71->81/81 green, unit 442; also fixed migration 030's errant USE signula (B-046). NEXT = cycle 16 = G-003 STAGE S3 (endpoints + Bearer wiring, per .dev-team/specs/G-003.md): create REST endpoints POST /api/v1/auth/token (issue tokens via TokenService after a PRIMARY auth — verify password or an existing authenticated session; do NOT let anyone mint tokens for any user), /api/v1/auth/refresh (rotate), /api/v1/auth/revoke, and GET /.well-known/jwks.json (KeyManager::getJwks). Then WIRE web/private_html/api/BaseController.php getUserByToken()/requireAuth() to accept a Bearer JWT (TokenService::verifyAccessToken -> load the user) ALONGSIDE the existing X-API-Key. Follow the existing api/ Router+Response+front-controller patterns; rate-limit the token endpoint (RateLimiter); consistent error keys. Integration tests: issue -> call a protected endpoint with the Bearer -> refresh -> revoke -> denied. Keep unit 442 + integration green. THEN S4 hardening + RED-TEAM (alg confusion, none, replay, refresh reuse, key confusion, IDOR on token issuance), S5 docs. THEN G-001 IdP Phase A (reuses this) -> G-002 billing TEST-MODE -> G-004 compliance L1 -> CAP-API #86. Migrations 001-030; next 031. [PRIOR] cycle 14 DONE: G-003 S1 crypto foundation built (firebase/php-jwt 6.11.1 vendored to web/_lib/jwt/, RS256-pinned Jwt facade rejecting alg:none + RS/HS confusion, KeyManager with encrypted key + rotation + JWKS, migration 030 tblRefreshTokens/tblRevokedTokens; +32 tests -> 439 green). NEXT = cycle 15 = G-003 STAGE S2 (TokenService, per .dev-team/specs/G-003.md): access-token ISSUANCE (short exp, jti, scope, sub=userID) + opaque refresh-token issuance (SHA-256 hashed into tblRefreshTokens) + refresh-token ROTATION with REUSE-DETECTION (reuse of a rotated token -> revoke the whole token family + security alert). Unit tests issue/refresh/rotate/reuse/revoke. Still NO endpoints (S3). Then S3 endpoints /api/v1/auth/{token,refresh,revoke} + /.well-known/jwks.json + wire BaseController::getUserByToken()/requireAuth() for Bearer + integration tests; S4 hardening + RED-TEAM; S5 docs. Migrations now 001-030; next 031. [ORIGINAL CAMPAIGN PLAN] ===FOUNDATION COMPLETE (cycles 3-12)===. cycle 13 = user-docs sync + this state cleanup. NEXT = cycle 14 START THE FEATURE CAMPAIGN with G-003 JWT API auth (safety floor clear; integration gate reliable). Read .dev-team/specs/G-003.md and build stage S1 first: vendor firebase/php-jwt v6 SOURCE into web/_lib/jwt/ (self-host pattern like _lib/tcpdf), add the SIGNula\\Security\\Jwt facade + KeyManager (RS256, kid, private key encrypted in tblSettings isSensitive with _private PEM fallback) + migration 030 (tblRevokedTokens jti denylist + refresh-token table); unit tests for sign/verify/expiry/alg-confusion/none-rejection. Then S2 issuance+refresh-rotation, S3 endpoints /api/v1/auth/{token,refresh,revoke} + JWKS + wire BaseController::getUserByToken()/requireAuth() for Bearer, S4 hardening + RED-TEAM (alg confusion, none, replay), S5 docs. One spec stage per cycle, one commit per stage, checker + integration gate each. THEN G-001 IdP Phase A (specs/G-001.md, reuses G-003 signing) -> G-002 billing TEST-MODE (fix B-025 first; specs/G-002.md; assertTestMode fail-closed; NEVER live creds) -> G-004 compliance L1 (specs/G-004.md). THEN CAP-API capstone (#86). Migrations 001-029; next 030.",
  "queue": {"backlog": ".dev-team/PROJECT.md#Backlog", "feature_gaps": ".dev-team/FEATURES.md", "findings": ".dev-team/PROJECT.md#Backlog"},
  "gate_ledger": [
    {"id": "G-001", "kind": "feature", "fg": "FG-001", "title": "SIGNula as IdP — SAML/OIDC provider + OAuth2 authorization server", "spec_ref": ".dev-team/specs/G-001.md", "status": "approved", "approved_by": "user", "reason": "Largest capability gap; Phase A (OAuth2/OIDC provider) reuses G-003 signing; Phase B SAML later; multi-cycle, security-reviewed per stage."},
    {"id": "G-002", "kind": "feature", "fg": "FG-003", "title": "Recurring billing / subscription lifecycle engine (#67)", "spec_ref": ".dev-team/specs/G-002.md", "status": "approved", "approved_by": "user", "reason": "TEST MODE only; live-payment credential wiring (#70) stays operational/user. Fix B-025 (tblBillingSchedule drift) first. Provider-managed subscriptions."},
    {"id": "G-003", "kind": "feature", "fg": "FG-004", "title": "JWT API authentication (#41)", "spec_ref": ".dev-team/specs/G-003.md", "status": "built", "built_cycle": 18, "approved_by": "user", "reason": "BUILD FIRST. Vendored firebase/php-jwt, RS256+JWKS+refresh rotation. Shares signing foundation with G-001."},
    {"id": "G-004", "kind": "feature", "fg": "FG-002", "title": "Multi-jurisdiction compliance tooling", "spec_ref": ".dev-team/specs/G-004.md", "status": "approved", "approved_by": "user", "reason": "Data-driven regime model; DSARManager delegates to existing AccountManager; L1 consent/DSAR first. Legal values ship draft/user-filled."}
  ],
  "capstone_tasks": [
    {"id": "CAP-API", "title": "Comprehensive API completeness + security pass + complete OpenAPI/Swagger docs", "source": "user directive 2026-06-30", "when": "AFTER the G-001..G-004 feature campaign", "scope": "Every feature has secure (authn+authz/rate-limited/validated/no-IDOR/consistent-error-keys) REST endpoints; complete openapi.yaml + Swagger UI.", "github_issue": "#86 + #80 (OpenAPI)"}
  ],
  "residual_backlog_note": "Deferred to POLISH/later (all Med/Low or operational): B-035/B-036/B-038/B-042 migration hygiene, B-044 wizard DELIMITER (CLI-load works), B-045 per-file test isolation, #76 WCAG a11y, #80/#86 API+OpenAPI (=CAP-API). Operational (user): #70/#71/#77/#78/#79/#83/#84/#85. See .dev-team/PROJECT.md#Backlog for the full B-001..B-045 ledger (most High items DONE cycles 3-12).",
  "model_policy": {"planning": "Fable 5 (Agent model:fable) for deep planning/architecture/red-team reasoning; fallback Opus if fable unavailable, re-check fable each planning round", "implementation": "Sonnet or Haiku (most appropriate) for code changes; Opus ONLY if absolutely necessary", "parallelism": "SEQUENTIAL — one agent at a time, NO parallel fan-outs (usage efficiency)", "set_by": "user 2026-07-02"},
  "integration_deliverables": {"issue": "#87", "when": "delivered on top of G-001 (OAuth2/OIDC provider)", "items": ["Sign in with SIGNula.id button (branded, light/dark + variants, WCAG, PKCE drop-in snippet)", "integration guides: quickstart + web/PWA + native + server-side + security best-practice", "iHymns first-party reference integration (github.com/MWBMPartners/iHymns, web/PWA + native)", "SDK/copy-paste snippets per platform"], "note": "Deep-planned via Fable 5 (.dev-team/specs/G-001-integration-plan.md), then implemented via Sonnet/Haiku. Extends the G-001 provider spec to the partner-facing layer.", "owner_decisions_2026-07-02": {"brand": "SIGNula / signula.id is canonical (matches repo+code); OIDC issuer = https://signula.id; button label 'Sign in with SIGNula.id'", "logo": "custom vector SVG set: SYMBOL = a horizontal key entering a lock + a checkmark, WITH alpha transparency + animation; WORDMARK 'SIGNula.id'; COMBINED lockup; all SVGs with appropriately-NAMED layers/groups for easy recolour; light-mode + dark-mode + colour-blind-friendly variants; Base64 fallback; ref web/public_html_landing/index.php (rework/scrap OK)", "subject_type": "PAIRWISE — per-relying-party pseudonymous sub. A1 tblOAuthClients gets subject_type + sector_identifier columns + a global pairwise salt setting; A3/A4 compute sub = hash(sector || local_user_id || salt) for id_token + userinfo"}},
  "blocked": {"is": false, "reason": null, "since": null},
  "cycle": 39,
  "cap": 60,
  "verify": {"commit": null, "claim_groups": [], "integration": "pending", "completeness_critic": "pending"},
  "verified_at_commit": null,
  "lock": {"holder": null, "ts": null, "stale_after_min": 90},
  "branch": "autopilot/2026-06-30",
  "last_commit": "99dfd6c",
  "trajectory_ref": ".dev-team/PROJECT.md#Trajectory-ledger",
  "user_docs_to_keep_current": ["CHANGELOG.md", "PROJECT_PROGRESS.md", ".claude/PROJECT_STATUS.md", "_docs/setup/SECURITY_SETUP.md"],
  "terminal": false,
  "updated": "2026-07-11T02:25:00Z"
}
